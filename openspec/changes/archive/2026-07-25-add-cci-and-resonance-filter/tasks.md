## 1. Core Logic — `_eval_overbought` function

- [x] 1.1 Extract per-indicator severity helper: `_classify` inner function with thresholds parameter
- [x] 1.2 Define CCI thresholds table: [(150, 1), (200, 2), (300, 3)]
- [x] 1.3 Define tightened RSI6 thresholds: [(75, 1), (85, 2), (95, 3)]
- [x] 1.4 Define tightened KDJ-J thresholds: [(95, 1), (105, 2), (120, 3)]
- [x] 1.5 Implement `_eval_overbought(rsi6, kdj_j, cci)` function with resonance detection (≥2 indicators severity ≥1 → shift up), returns `(LayerResult, multiplier, hard_block, hard_block_reasons, downgrade_reason)`
- [x] 1.6 Replace inline Layer 3 code in `check_entry_filters` (~70 lines) with 5-line call to `_eval_overbought`
- [x] 1.7 CCI extracted from `indicators_data["historical"][0].cci` (Tushare stk_factor_pro confirmed data); realtime path has no CCI

## 2. API Response & Details

- [x] 2.1 CCI detail line generated by `_classify` inner function with format matching RSI/KDJ style
- [x] 2.2 Resonance detail line: `"🔗 共振: RSI6 + KDJ-J + CCI 中 ≥2 项偏高 → 联合降级"`
- [x] 2.3 `downgrade_multiplier` propagation verified: normal=1.0, probe=0.5, blocked=0.0, hard_block=0.0; `LayerResult` fields unchanged

## 3. Downstream Compatibility

- [x] 3.1 `LayerResult` Pydantic model unchanged (passed, grade, details, downgrade_reason, downgrade_action)
- [x] 3.2 `tools.ts` live path already iterates `l3.details` — no changes needed; backtest path updated to use same format
- [x] 3.3 `candidate_pool_monitor.py` consumes `layer3_overbought.passed` — field name unchanged

## 4. Validation

- [x] 4.1 Logic test: 紫金矿业 case (RSI6=83.9, J=102.8, CCI=250.2) → `HARD_BLOCK, mult=0.0` (was "PASS" before)
- [x] 4.2 Spot-check: 8 cases tested covering normal, single-warn, resonance, blocked, hard-block boundaries — all correct
