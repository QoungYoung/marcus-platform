# ① 条件化分层排序 v3 · 第一版离线验收（2026-09-15）

> 实现：`apps/main_line/wolf_pick_rank_v3.py`（**单一实现**，生产将复用同一函数，避免"两条路两套口径"）
> 验收：`jobs/eval_pick_rank_v3.py`；尺子与 `docs/leg-metrics-spec.md` / `docs/wolf-pick-selection-eval.md` 完全一致
> （入场 = 次日收盘，持 5/20 日，超额 = 个股 − **同主题同日等权篮子**，块状 t 按 ISO 周，H1/H2 按 theme-day 中位日切）。
> 样本：**2,119 主题×日 / 164 交易日（2026-01-05→09-04）**；资金流底座 182 天/954k 行（覆盖 99.2%）。
> **本页只做离线验收，尚未接生产**（开关名预留 `WOLF_PICK_RANK_V3`，默认 0）。

---

## 0 结论

| 配置 | 5 日超额 | Δ vs 现行 leader | 配对块状 t | H1 | H2 | 20 日超额 |
|---|---|---|---|---|---|---|
| **现行 leader**（tier1） | −0.982% | — | −1.42 | −0.133% | −1.879% | **−1.201%** |
| **v3 第一版**（域=cand∩LOW/MID，排序=LOW优先+低位横盘次键） | **−0.198%** | **+0.719pp** | **2.07** | **+0.044%** | **−0.431%** | −1.685% |

- ✅ **5 日口径通过"两段都不劣"**：H1/H2 都优于现行（H1 由 −0.13% 转正 +0.04%，H2 由 −1.88% 收到 −0.43%）。
- ⚠️ **20 日口径未通过**：−1.685% vs 现行 −1.201%（Δ −0.48pp，配对 t ≈ −0.13 不显著）。
  → 说明这套排序**改善的是入场质量（短窗口）**，但拿得久并不占优；持有期的优劣要靠兑现层，不能靠选股层兜。
- ⚠️ 绝对水平仍是**负超额**（−0.198%）：第一版只是"少亏 0.72pp"，**不是**"已经找到 alpha"。

## 1 消融：收益主要来自**域变更**，不是排序

同一批 theme-day、同尺子（hold=5；`--domain` 控制候选域，`--components` 控制打分项）：

| 配置 | 超额 | Δ vs 现行 | 块状 t | H1 | H2 |
|---|---|---|---|---|---|
| 现行 leader（域=lowmid，排序=leader） | −0.982% | — | −1.42 | −0.133% | −1.879% |
| v3 排序，但**沿用旧域**(lowmid) | −1.189% | **−0.075** | −0.40 | −0.513% | −1.868% |
| v3 排序 + **新域**(cand∩LOW/MID) | −0.300% | +0.595 | 2.11 | −0.206% | −0.391% |
| 新域 + 仅 `pos`（LOW 优先） | **−0.110%** | **+0.819** | **2.40** | **+0.275%** | −0.481% |
| 新域 + 仅 `dist`（贴前低） | −0.252% | +0.504 | 2.01 | +0.119% | −0.610% |
| 新域 + 仅 `flat`（低位横盘多时） | −0.575% | +0.437 | 1.51 | −0.258% | −0.881% |
| 新域 + 仅 `hist`（老票） | −0.464% | +0.452 | 1.66 | −0.005% | −0.907% |
| 新域 + pos+flat+hist | −0.358% | +0.584 | 1.85 | −0.141% | −0.568% |
| 新域 + pos+dist+flat+hist | −0.300% | +0.595 | 2.11 | −0.206% | −0.391% |

**读法**：
1. **域变更（去掉 `r20≥0` 与 `rs≥0` 两个闸）是主要收益来源**：旧域上换排序 Δ ≈ 0（−0.075，t −0.40）；
   换域后无论怎么排都 +0.50pp 以上。这与闸门分档实测一致（`r20≥0` −0.369% vs `<0` −0.055%；`rs≥0` −0.205% vs `<0` **+0.055%**）。
2. **打分项越少越好**：只留 `pos`（LOW 优先）最优；加 `dist`/`flat`/`hist` 反而变差 → **不做多因子加权**（也避免了拟合）。
3. `mf`（跌+资金流入）与 `squeeze`（缩量）**没有增益**（V2 反而更差）——与 `docs/wolf-buy-moneyflow-check.md` 的结论一致：
   它是"全成分层面为正、叠到已选池内为负"的因子。

## 2 并列次键的稳健性（防止"靠代码序"的伪结论）

`pos` 只有 0/1 两档 → 并列很多，次键必须经得起检验（用代码序正是我们批判过的 legacy 扫描序）：

| 次键 | 5 日超额 | Δ | 块状 t | H1 | H2 | 20 日超额 | Δ(20日) |
|---|---|---|---|---|---|---|---|
| ts（代码序，仅作稳定性对照） | −0.110% | +0.819 | 2.40 | +0.275% | −0.481% | −1.027% | +0.272 |
| **flat（低位横盘多时）** | **−0.198%** | **+0.719** | **2.07** | **+0.044%** | **−0.431%** | −1.685% | −0.550 |
| dist（贴前低） | −0.292% | +0.682 | 2.36 | −0.118% | −0.459% | −1.805% | −0.593 |
| rs（相对抗跌） | −0.758% | +0.317 | 1.50 | −0.255% | −1.243% | −2.072% | −0.080 |

→ 取 **`flat`（低位横盘多时）** 作次键：它**有原话依据**（2026-04-07「低位横盘多时的就是好 超跌都没有低位走平多时的好」）、
5 日口径两段都不劣、且不依赖代码序。`ts` 那列只作"上界参照"（说明结果不是次键挑出来的）。

## 3 第一版定义（已写进模块默认值）

```
候选域 = 候选池（组内前2 ∩ 主题容量分位 50%）∩ pos ∈ {LOW, MID}
         ——去掉 r20 ≥ 0 与 rs ≥ 0 两个闸（实测净负）
排序   = LOW 优先（+1.0）；并列次键 = flat_low_days 降序（低位横盘多时）
         ——不加动量的任何分量（r60/涨停/成交额/曾领涨实测一致为负）
取前 2（与现行 tier1 对齐）
仍保留的门（未动）：L2 过滤（ST/流动性/数据完整）、位置闸 dist_prevlow ≤ 5%（实测正贡献）、风向标硬拦
```

## 4 仍未达标的部分（下一轮要解决）

1. **20 日口径劣于现行**（Δ −0.48pp，不显著）→ 入场质量改善但**持有期不占优**；要么把持有期与兑现层绑定验收，要么承认这套只服务"短持有"。
2. **绝对超额仍为负**（−0.198%）→ 尚未产生 alpha；离"低位埋伏不亏"还有距离。
3. **分层/阶段依赖还没真正落地**：B1 原话里最系统的一组（强主题→二供/二线、弱主题→最抗跌、低位→横盘）中，
   本轮只有"低位"与"横盘"进了实现；V3 的"强主题跳第 1 名 / 弱主题偏好抗跌"在实测里**没有增益**（V3 < V1），
   说明我那两条条件化的**代理口径**（主题 r5 阈值 ±0.5%、rank_in_theme ≥0.98）不对，需要换口径重测。
4. **未做**：把"错杀要有业绩有订单没有雷 + 资金接盘"(2025-03-25) 落成可算条件（缺结构化事件源）。

## 5 接生产的前置条件（未做，等拍板）

- [ ] 把 `wolf_pick_rank_v3.pick_top` 接进 `wolf_confirm_pick.pick_v2`，**开关 `WOLF_PICK_RANK_V3`（默认 0）**，
      域变更同样放在开关内（`WOLF_PICK_DROP_RS_GATE` 之类），保证回退 = 一键。
- [ ] 定向单测：默认配置下 LOW 优先、flat 次键、域不含 r20/rs 闸；开关关闭时**逐字节回退**现状。
- [ ] 影子模式：开关开启但"只记录不布腿"跑 5–10 个交易日，比对 v3 选票与现行选票的差异与当日表现。
- [ ] 20 日口径的处理需要拍板：接受"只服务短持有"，还是先修持有期/兑现再上。


---

# 第二版（2026-09-15 同日迭代）：加入「阶段 + 二供」与执行域对齐

## 6 v2 定义（= v1 + 四项改动）

```
域  = 候选池（组内前2 ∩ 容量分位50%）∩ pos ∈ {LOW,MID}          ← v1：去掉 r20≥0 / rs≥0 两闸
      ∩ 执行板块过滤（剔除 300/301/688/北交所 = WOLF_PICK_BOARD_EXCLUDE）   ← v2 新增（对齐可执行域）
排序 = LOW 优先；并列次键 = flat_low_days（低位横盘多时）        ← 同 v1
阶段 = 主题 r5 的**跨主题分位** ≥ 0.5 → 强；≤ 0.5 → 弱            ← v2 新增（v1 的 ±0.5% 绝对阈值已废）
      强主题 → **二供**：剔除该主题候选里 r20 最高的一只          ← 2026-04-13「已经涨起来的板块的龙头不做 做他的二供」
取 1 只（limit=1）                                              ← v2 新增（v1 取 2）
```

## 7 v2 结果（2,005 个有选票的 theme-day / 164 交易日）

| 配置 | hold | v2 超额 | 现行 leader | Δ vs 现行 | 配对块状 t | H1（v2/现行） | H2（v2/现行） |
|---|---|---|---|---|---|---|---|
| **e1 = v2（qtile 0.5/0.5）** | 5 | **+0.213%** | −0.982% | **+1.120pp** | **2.29** | **+0.113%** / −0.133% | **+0.306%** / −1.879% |
| e2（qtile 0.66/0.33） | 5 | +0.157% | −0.982% | +1.097pp | 2.27 | +0.056% / −0.133% | +0.252% / −1.879% |
| f2（次键改 ts＝代码序，仅稳健性对照） | 5 | +0.165% | −0.982% | +0.935pp | 1.86 | −0.030% / −0.133% | +0.347% / −1.879% |
| **f3 = e1 配置** | 10 | **−0.282%** | −2.374% | **+1.798pp** | **2.36** | **−0.179%** / −1.158% | **−0.382%** / −3.705% |
| f1 = e1 配置 | 20 | −1.529% | −1.201% | −0.408pp | −0.04 | −1.913% / −0.334% | −1.157% / −2.192% |

- ✅ **hold=5 与 hold=10 两个口径都达标**：超额由负转正（5 日 +0.213%）/ 大幅改善（10 日 +1.80pp），
  且 **H1、H2 两段都优于现行 leader**，配对块状 t 2.29 / 2.36。
- ⚠️ **hold=20 仍未达标**（Δ −0.408pp，t −0.04 不显著；H1 明显更差、H2 更好）。
  → 与 v1 同一现象：这套改善的是**入场质量（5–10 日）**，长持有要靠兑现层。
- ⚠️ 次键仍以 `flat`（有原话依据）为准；`ts` 仅作对照（+0.165%，说明结论不依赖某一具体次键，但代码序不是可接受的生产规则）。

## 8 v2 的消融（每一步都单独量过；hold=5，n=2,005 theme-day）

| 步骤 | 配置 | 超额 | Δ vs 现行 | 块状 t | H1 | H2 |
|---|---|---|---|---|---|---|
| 基线 | 现行 leader | −0.982% | — | −1.42 | −0.133% | −1.879% |
| ① 域：去 r20≥0 / rs≥0 | cand∩LOW/MID，排序=LOW优先(次键 flat)，取 2 | −0.198% | +0.719 | 2.07 | +0.044% | −0.431% |
| ② ①+执行板块过滤 | 同上 + 剔 300/301/688 | −0.286% | +0.647 | 1.58 | −0.138% | −0.425% |
| ③ ②+阶段(二供) 取 2 | + qtile 0.66/0.33 + 跳第 1 名 | −0.186% | +0.770 | 1.77 | +0.014% | −0.373% |
| ④ ③+取 1 只 | limit=1 | **+0.157%** | +1.097 | 2.27 | +0.056% | +0.252% |
| ⑤ ④+阈值放宽到 0.5/0.5 | = **v2** | **+0.213%** | +1.120 | **2.29** | +0.113% | +0.306% |
| ✗ 反向对照：②+只取 1（无阶段） | limit=1, stage=none | −0.011% | +0.889 | 1.84 | **−0.235%** | +0.199% |
| ✗ 硬过滤 `mf5pos`（近5日资金非净流出） | ②+硬过滤 | −1.232% | +0.163 | 0.04 | −0.895% | −1.540% |
| ✗ 硬过滤 `noout5`（当日净流入） | ②+硬过滤 | −1.036% | +0.267 | 0.62 | −0.707% | −1.334% |
| ✗ 硬过滤 `flatmed`（横盘天数≥中位） | ②+硬过滤 | −0.298% | +0.625 | 1.52 | −0.180% | −0.409% |

**读法**：
1. **阶段（二供）是 H1 达标的关键**：去掉它（✗对照）H1 由 +0.113% 掉到 −0.235%（劣于现行的 −0.133%）。
2. **`limit=1` 是超额转正的关键**：③ −0.186% → ④ +0.157%。
3. **资金类硬过滤（`mf5pos`/`noout5`）再次证实有害**（与 ② 的结论一致：他在个股级上的资金条件在我们已选池内是负向）——
   这也是本轮第三次验证"他的**板块级**判据不能当**个股级**闸"。
4. `flatmed` 硬过滤无增益 → 「低位横盘多时」作为**排序次键**有用，作为**硬门槛**没用。

## 9 v2 仍未解决

1. **20 日口径**（H1 段 −1.913% vs 现行 −0.334%）：需与兑现层联合验收，或明确"本层只服务 5–10 日持有"。
2. **绝对超额仍小**（5 日 +0.213%）：刚过盈亏平衡线，扣费后是否为正需按生产口径复核（本轮未计费用）。
3. **阶段口径是"跨主题 r5 分位"**（我们的代理）：他原话只说"强势/弱势板块"，没有给分位口径；
   生产里更自然的口径是**方向层池**，但用池当强主题的实测更差（c5：−0.009%，H1 −0.145%）→ 保留分位口径并标注为代理。
4. **未做**：错杀票的"有业绩有订单没有雷"（缺结构化事件源）、龙虎榜席位结构（数据薄）。


---

# 接生产接线（2026-09-15，round 4）：开关 + 影子

## 10 接线方式

`apps/main_line/wolf_confirm_pick.pick_v2()` 末尾新增分支（**默认关**）：

```
WOLF_PICK_RANK_V3=1         → 生效：返回 v3 选票（pick_source='v3'，只取 1 只）
WOLF_PICK_RANK_V3_SHADOW=1  → 影子：**只记录不生效**（返回仍是原 leader 的票）——**默认就是 1**
                               （与 P1 门影子一致：只写 data/rank_v3_<as_of>.json，不改决策，天天自动攒对账数据）
WOLF_PICK_RANK_V3=1         → 生效（默认 0，等拍板）
SHADOW=0 且 RANK_V3=0        → 完全不碰（连影子文件都不写），行为与接线前一致
```

v3 分支用的就是**已验收配置**：域 = 候选池 ∩ LOW/MID（去掉 `r20≥0` 与 `rs≥0` 两个实测净负的闸）、
排序 = LOW 优先 + `flat_low_days` 次键、阶段 = 主题 r5 **跨主题分位** ≥0.5 → 强 → 二供（跳过组内 r20 最高一只）、
只取 1 只；`flat_low_days` 由 pick_v2 现成的日线算出（`fetch_daily` 新增 high 字段）。
影子/生效都会写 `data/rank_v3_<as_of>.json`（含 v3 选票、leader 选票、域大小、主题分位）。

`theme_r5_quantile(theme, as_of)` 在 `wolf_pick_rank_v3.py` 里实现（PG 聚合 + 按日缓存 `data/theme_r5_<as_of>.json`），
**已用本地独立复算校验**：2026-09-11 各主题分位逐一对上（农业 0.9167 / 军工 1.0 / 医药 0.0833 / 半导体 1.0；
本地未做 1.0 截断故最大值为 1.0833，生产已 `min(1.0, …)`）。

## 11 生产影子实测（2026-09-15，容器内只读 + `WOLF_PICK_RANK_V3_SHADOW=1`）

```
RANK_V3 SHADOW theme=半导体/芯片 domain=10 q=1.0 v3=['SH603986'] | leader=[]
status=ok picks=[]        ← 返回的仍是 leader 的票（空），行为未变
data/rank_v3_20260911.json: mode=shadow, q=1.0, domain_n=10
  v3 会选: [{"symbol": "SH603986", "v3_score": 1.0, "why": "低位(LOW)"}]
```

→ 这一天 **现行 leader 位置闸命中 0 只（不布腿）**，而 **v3 会布 SH603986（低位）** —— 正是"对齐后会有什么不同"的实例。

## 12 单测

`backend/tests/test_rank_v3_wiring.py` 3 项：默认关（选票不变、无影子文件）／影子（返回 leader 票 + 写影子文件）／
开启（返回 1 只 `pick_source='v3'` + `mode=on`）。


## 13 逐日对账脚本（`jobs/shadow_reconcile.py`）

把三类影子产物汇总成一张"对齐后会怎样"的表（只读）：
`theme_volfund_shadow_*`（P1 门会拦谁）/ `rank_v3_*`（v3 会选什么 vs leader 实际选什么）/ `dip_tol_shadow_*`（C1 会少成交哪些票）。

生产实跑样例（2026-09-15）：

```
【P1】检查过的主题=1  会拦=1  → ✋ 农业 资金连续5日净流出 → 不新开腿
【v3】半导体/芯片  mode=shadow 主题分位=1.0 域=10  v3=SH603986  leader=—   **不同**
【C1】（无影子文件：需部署含影子的 t_monitor）
```
