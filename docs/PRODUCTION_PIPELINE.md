# Marcus 生产全链路（狼大流程落地版 · 2026-09-10 梳理）

> 目的：把「生产实际怎么跑」与「狼大原话/规则怎么落」对齐成一份可核对的主文档。
> 来源：生产代码（/opt/marcus-platform ↔ 本仓同级路径）+ 调度配置 config/tasks.yaml + 数据库 t_* 表 + 线上产物。
> 本文只描述**已实现并在线**的链路；未实现/未接入的写在 §11。

---

## 0. 全景

```
[数据层]  gzcloud(Tushare代理) / brze(分钟) / 腾讯·新浪(实时报价) / news.db / 研报(catalyst)
   │
   ├─(收盘后 18:45) 主线确认每日更新 mainline_gate_daily
   │      concept_long → etf_flow → inst_flow → trend_confirm(结构GATE) → heat_v2(资金热度) → mainline_gate → 注入 main_line_state
   │      产物: concept_long.json / trend_confirm_<date>_long.json / heat_v2_<date>.json / mainline_gate_<date>.json / mainline_confirm_history.json
   │
   ├─(次日 08:00) 主线判定 main_line_judge  → 以最新 gate 为准覆写 main_line/candidates, 写 main_line_state.json
   ├─(08:10) 波浪判定 wave_agent → wave_state.json（level/sub_level/operation, defense/exit 硬拦不建仓）
   ├─(08:15) 板块洗盘收敛期 G3 judge
   ├─(08:20) 成分股确认 stock_confirm_judge → stock_confirm_result.json（低吸池的成分/阶段）
   │
   ├─(09:20) 主线内切换布腿器 rotation_switch_arm → 写 t_conditions(account=stock, publisher=switch)
   │      卖腿: 持仓落在拥挤/出货链 → quote.vwap_break
   │      买腿: ①当日 confirmed 主题池(pick_v2) ②rotation 链候选(再按 confirmed 主题过滤) → 每腿挂 253 + 254 两条买条件
   │
   ├─(全天, 30s/轮) TMonitor 触发 → 写 t_triggers
   │      254 = quote.dip_prev_low(当日5min最低≤前一日5min最低×1.005) ∧ vol_ratio≤0.9
   │      253 = index.m5_dump≥0.4(指数单根5min急杀) ∧ average>0 ∧ current>0
   │            【2026-09-10 更正】原写的"∧ 时段09:45-14:40 ∧ 非跌停"**不在表达式内**：
   │            09:45 下限**任何地方都没有**；14:45 禁新开在通用护栏；非跌停/跌停禁买在网关层。
   │      护栏: regime GATE / 14:45 后禁新开 / armed / 5min 冷却 / 板块权限
   │
   ├─(触发后) 执行层 t_gateway.gateway_execute → validate_order(硬闸门+账本+建议层) → 执行器成交 → paper_orders/paper_positions
   │
   └─(卖出/做T) wolf_t_rules(正T/倒T/确认制T出) + 止损扫描 + auto_exit/manual_guard 腿
   │
   └─(复盘/评估) 每日复盘 / 周五周度反思 / P2 Gate 报告 / 买点对齐评测 / 沙箱大样本回测
```

---

## 1. 运行时与调度

| 组件 | 作用 | 备注 |
|---|---|---|
| marcus-worker | APScheduler 读 `config/tasks.yaml`；**TMonitor 常驻线程**（30s/轮） | 脚本类任务在容器内以子进程运行 → **改 jobs/*.py 即时生效** |
| marcus-backend | FastAPI（:8000）+ 进程内服务模块（t_monitor / trade_graph / p2 等） | **改这些模块需重启 backend + worker** |
| marcus-postgres | 交易/条件/触发/持仓/回测表（t_conditions, t_triggers, paper_* 等） | |
| marcus-dsh | 内部 LLM 通道（:3001 /chat） | 供 agent 决策与链路抽取 |

### 1.1 关键时间表（生产实跑）

| 时间(工作日) | 任务 | 脚本 | 主要产物 |
|---|---|---|---|
| 08:00 | 主线判定 | apps/main_line/main_line_judge.py | main_line_state.json（以最新 gate 覆写） |
| 08:10 | 波浪判定 | apps/main_line/wave_agent.py | wave_state.json |
| 08:15 | 板块洗盘收敛期(G3) | jobs/sector_g3_judge.py | — |
| 08:20 | 成分股确认刷新 | apps/main_line/stock_confirm_judge.py | stock_confirm_result.json |
| 08:40 | 业绩披露日历刷新 | apps/main_line/build_earnings_calendar.py | earnings_calendar.json |
| 08:50 | 黄金坑盘前报告 | — | — |
| 09:00 / 09:10 | 盘前扫描 / 盘前诊断 | jobs/pre_market_scan.py / morning_diagnosis.py | — |
| **09:20** | **主线内切换布腿器** | jobs/rotation_switch_arm.py | t_conditions（253/254 买腿 + vwap 卖腿） |
| 09:35 / 09:53 / 10:35 / 13:35 / 14:30 | 自动交易 agent 档 | — | 建仓/减仓/只卖不买 |
| 盘中 20/31/50 分 | 盘中扫描 | jobs/market_scan.py | — |
| 每 30 分钟 | 新闻采集 | apps/news/news_collector.py | news.db |
| 15:01 / 15:05 / 15:06 / 15:20 / 15:25 / 15:30 | 净值快照 / 系统性风险 / 宏观机构状态 / P2 Gate 报告 / 今日计划 / 黄金坑快照 | — | — |
| 16:00 | 每日复盘 | jobs/daily_review_enhanced.py | — |
| 16:30 | 指数日线刷新 | jobs/refresh_index_daily.py | 指数CSV + wave_pivots |
| **18:15** | 产业链图每日增量 | apps/main_line/chain_map.py | chain_map_<date>.json |
| **18:45** | **主线确认每日更新** | apps/main_line/mainline_gate_daily.py | 见 §3 |
| 18:45 | 域外主线方向监控 | apps/main_line/scan_theme_gaps.py | — |
| 19:30 / 周五15:30 / 周日08:00 / 周六10:00 | 新闻晚报 / 周度反思 / 股票池更新 / chain_map 全量强核 | — | — |

> **关键时序（务必记住）**：主线门在**收盘后 18:45** 产出（用当日收盘数据）；因此**次日 09:20 布腿**读到的 gate 是「昨天收盘产出的那一份」。布腿选股用的日线数据也就截至**昨天**。

---

## 2. 数据层

| 源 | 用途 | 入口 |
|---|---|---|
| gzcloud（ts.gyzcloud.top，Tushare 兼容代理） | 日线 daily / daily_basic / moneyflow_dc / top_inst / margin / fund_share | wolf_confirm_pick.gz()、各 build_*.py |
| brze（tu.brze.top） | **历史分钟** stk_mins(5min/1min，可回溯 2024+)、实时 rt_k/rt_min | app/services/t_data_sources.fetch_brze_stk_mins() |
| 腾讯 qt | 实时报价（做T/触发取价） | fetch_tencent_quote() |
| 新浪 | 5min（近 ~25 交易日）、指数快照 | fetch_sina_minline() |
| 本地/DB | news.db（新闻+概念+影响级别）、stock_pool.db（成分/概念映射/市值）、concept_long.json（概念等权指数）、wave_state.json | — |

约束（实测）：brze 单次最多 8000 根（5min≈167 交易日）→ 长区间需分块；**上证指数分钟在 brze 不可用**，指数相关（253）需用 510300 等 ETF 代理。

---

## 3. 主线判定链（gate）

**脚本**：`apps/main_line/mainline_gate_daily.py`（18:45）依次执行
1. `build_concept_long.py` — 概念等权指数（20250101 起，pct 环比规避复权）
2. `build_etf_flow.py` — 主题 ETF 份额流（佐证因子）
3. `build_inst_flow.py` — 龙虎榜机构/游资净买
4. `trend_confirm.py --as-of <date>` — **结构 GATE**：逐主题判断 track_b（确认链）比例 → `gate = ratio >= 0.35`
5. `heat_v2.py --date <date>` — **资金热度**：四因子（mf5 主力5日净额 / rel 概念等权20日 / mf_accel 加速 / etf 份额）→ 排名
6. `mainline_gate.py --date <date> --fusion-json heat_v2_<date>.json` — 组合：

| verdict | 条件 | 含义（狼大语义） |
|---|---|---|
| confirmed_candidate | 热度排名 ≤ TOPN(=2) **且** 结构GATE PASS | 资金主导 + 结构确认 = 主线候选（可布腿） |
| watch | 排名 ≤ TOPN 但 GATE FAIL | 资金在、结构未确认 → 观察 |
| reserve | GATE PASS 但排名 > TOPN | 结构健康等资金点火 → 预备 |
| none | 其余 | — |

7. `mainline_state_inject.py` — 注入 main_line_state.json（Pi/agent 可见）

**波浪只作风险提示**：`wave_env`（level/sub_level/operation/c_kill）写入 gate 文件但**不否决主线资格**（狼大：调整浪内也做主线；破位位由执行层保险丝处理）。

**确认历史**：`mainline_confirm_state.ensure_history` 维护 `mainline_confirm_history.json`（"曾确认"窗口，供布腿器回退/参考）。

---

## 4. 环境/波浪链

- `wave_agent.py`（08:10，周一为主）→ `wave_state.json`：level(d1..down) / sub_level / operation(build/t_only/side/defense/exit) / confidence / reasons。
- `wave_alloc.read_wave_alloc()`：把 operation 映射成 `invest` 仓位系数；布腿器按 invest 裁剪买腿数量（build/t_only/side=1 不裁剪）。
- `trade_graph.node_check_safety_gates`：**operation ∈ (defense, exit) → 不建仓（硬拦）**；可用 `SAFETY_GATE_BYPASS` 旁路（默认关）。
  ⚠️ **范围更正（2026-09-10 核验）**：该硬拦**只覆盖 AI-agent 决策路径**（`run_trade_decision`，即 09:35/13:35 等 `auto_trade_*` 档）。
  **253/254 表达式腿路径**（`rotation_switch_arm`(09:20) → TMonitor → `t_gateway.gateway_execute`）**全程不读 `wave_state`**
  （`t_gateway.py` 只引用 `t_regime`），故不受此门约束。无底仓建仓另有一条 wave 门：`wolf_253_build.choose_intent()` 对 defense/exit 返回 `None`。
  **2026-09-10 变更**：原同处的「总回撤≥5% 禁买」「连续亏损≥3 笔当日熔断」两条账户级熔断**已删除**（审计 §5.2 S5）；
  `drawdown` / `consecutive` 仍计算并写入 state，但仅作观测，不再拦截。
- `t_regime.compute_regime()`：日内环境门（沪深300 跌 >2% → HALT；市场诊断 extreme/bear → HALT 等），产出 `gate_low_buy` / `gate_high_sell`（ALLOWED / MANUAL_ONLY / BLOCKED）。

---

## 5. 标的确认链（低吸池）

- `stock_confirm_judge.py`（08:20）→ `stock_confirm_result.json`：{子概念 → 成分股与确认阶段}。`wolf_confirm_pick.confirm_universe(theme)` 即读它。
- `chain_map.py`（18:15 增量 / 周六 全量）→ 产业链图（主题→环节→代表股），供 agent 语义核对。
- `position_judge.py`（周一）→ 概念高低位；`crowding_blacklist.json` 由基金持仓拥挤度刷新（`refresh_fund_crowding.py`）。

---

## 6. 布腿链（rotation_switch_arm，09:20）

只布腿、不下单；执行交给 TMonitor。

1. **过期**：`expire_old`（把 trade_date < today 的 switch 腿置 expired）
2. **卖侧**：当前持仓落在 `crowded_top` 或 `holdT_top`（rotation universe）→ 布 `quote.vwap_break` 卖腿（SELL_EXPR）
3. **买侧（两条路径）**
   - **路径 A**：`room + holdT` 链候选 → 该链 `pick_buy(chain, limit=3)`（按关键词匹配成分、剔 ST/拥挤黑名单、位置 LOW/MID）→ 用「当日 confirmed 主题」过滤（`theme_of_chain`）
   - **路径 B（主线确认池）**：`gate_confirmed_today(today)` 取最近 gate 文件的 confirmed_candidate 主题（排除银行）→ 逐主题 `confirm_pick(th, limit=pool_legs-got)`（内部 `pick_v2`：主题内 leader 榜 → 位置闸（距前日低 ≤5%）→ tier1 严格前 2 + tier2 接近档补位，跨主题合计 ≤ `ROT_POOL_LEGS`=4）
4. **仓位系数**：`wave_alloc.invest` < 1 时按比例裁剪买腿（保留前序=主线优先）
5. **板块权限**：`WOLF_PICK_BOARD_EXCLUDE`（默认 cyb,bj,kcb）→ 创业板/科创板/北交所腿直接剔除（账户无权限）
6. **写入**：每条买腿写两条条件 `custom_m5dump`(253) + `custom_prevlow`(254)；每条卖腿写 `custom`(vwap_break)。同时写入 **换手基准** `benchmark_turnover_profile`（近 5 完成交易日均换手）供 vol_ratio 使用。
7. `SWITCH_ARM_DRY=1` 只输出决策 JSON 不写库。

---

## 7. 触发链（TMonitor，30s/轮）

- 轮询 `t_conditions`（account=stock、当日子弹）→ 并发取价（腾讯/新浪）→ 构建字段快照 → **表达式求值**（t_expr）→ 通过后仍须过通用护栏。
- **字段与语义**
  - `quote.dip_prev_low`：当日 5min 最低 ≤ 前一交易日 5min 最低 ×1.005（狼大「挂前一天低点」）
  - `vol_ratio` = [当日累计换手% × (240/已开盘连续分钟)] ÷ base，base = 近 5 个已完成交易日**日换手均值**（`benchmark_turnover_profile.same_minute_avg`，缺省兜底 0.5%）
  - `index.m5_dump`：上证（当前以 ETF 代理）单根 5min 跌幅%，急杀阈值 0.4
  - `quote.vwap_break`：跌破日内均价线（黄线）→ 卖
- **通用护栏**：regime GATE(低吸/高卖分别) → **14:45 后禁新开仓** → `armed=1` → **同条件 5 分钟冷却** → 板块权限（`_board_tradable`）
- **状态机**：狼大表达式腿是**非消费式持续腿**（命中后保持 active，靠冷却防刷；底仓不动、T仓反复做）；其他做T腿触发即消费；manual_guard 一次性。
- **跨日结转**：`_roll_wolf_legs` 把昨日仍 active 的狼大腿复制到今日（不复活已停用/已消费的腿）。
- **其它每轮动作**：止损扫描（现价 ≤ stop_loss_price）、做T规则（正T/倒T/确认制T出）、尾盘 de-T、防御减T、board_half 等。

---

## 8. 执行链与风控（t_gateway）

触发写入 `t_triggers(pending)` 后立即走 `gateway_execute()`（唯一放行者）：

| 层 | 内容 |
|---|---|
| 账户白名单 | 仅 `stock`（狼大做T账户）可执行 |
| **硬闸门** | 裸空/无券卖、无底仓禁裸买（除非条件单建仓路径）、跌停禁买/涨停禁卖、STOP_ALL、触发事件状态异常、**wolf 回补当日上限 `WOLF_REFILL_MAX_PER_DAY`（默认 2，2026-09-10 由硬编码 1 放宽，S7）**、当日有撤销卖单需人工确认、低吸加仓次数上限（MAX_DAILY_BUY_LEGS）<br>【2026-09-10 更正】原列于此的「基础回撤 guard」**不在 `t_gateway`**（该文件对 drawdown 零引用）——回撤检查原在 `trade_graph`（agent 路径），且已随 S5 删除 |
| 账本层 | 可卖底仓断言、买腿 ≤ 可卖底仓、日亏损熔断（止损卖单豁免）。**注：原「当日回转额 ≤3×净值」上限已于 2026-09-10 删除（审计 §5.2 S5）** |
| 建议层 | **单笔 ≤ 净值 5%**、价差成本比、冷却、频次（仅告警） |
| 执行 | MarcusVNPyExecutor + PaperTradingEngine → paper_orders/paper_positions；失败/被拒 → t_triggers 置 blocked + 审计 |

---

## 9. 做T / 卖出链

- `wolf_t_rules`：正T买（`zheng_t_buy_quote`）/ 倒T卖（`dao_t_sell_quote`）/ 确认制T出（停量 + 二次不过前高）/ 日高与周期 PnL 记录；周五减 T 仓、不强制日结。
- `SELL_EXPR = quote.vwap_break`（黄线破位）用于切换链卖腿；`custom_level_sell`/`custom_vwap_sell`（manual_guard）一次性。
- **止损（2026-09-10 对齐狼大「止损六层」）**：TMonitor 每轮扫描，`止损线 → 收盘确认守卫 → 时点门 → 卖量分级`
  四道串联；止损单豁免日亏损熔断（止血优先）。
  | 层 | 狼大原话 | 系统落地 |
  |---|---|---|
  | ① 个股波段逻辑止损 | 2026-03-05「13日内跌破波段低点的-3%没有收回 直接止损」+「是**自己逻辑**的有效跌破 除非是意外事件，黑天鹅那种…如果是**无利空**…就必须止损」 | `wolf_early_stop.resolve_stop`：**建仓初期(≤13 交易日)** 止损线 = **建仓时点锁定的波段低点 ×(1-3%)**；**「无利空」前提**= 有意外事件/黑天鹅（`data/wolf_negative_events.json`）则**不套结构线**、退回 `stop_loss_price`。`WOLF_EARLY_STOP=0` / `WOLF_NEG_EVENT=0` 关 |
| ①「无利空」数据源 | 2026-03-05 同句「**除非是意外事件，黑天鹅那种**。如果是**无利空**13日内下跌那新低后-3%就是逻辑问题」 | **公告判据**（2026-09-11 落地）：`ak.stock_notice_report` 当日全市场公告 → 过滤持仓 → 类型白名单＋高精度标题短语 → 写 `data/wolf_negative_events.json`；`jobs/wolf_neg_event_scan.py` 每 30 分钟（8-16 时工作日）。⚠️ **只对个股有效（ETF 无个股公告）**；日粒度非实时；属**合理外推**（狼大是盘中看公告知情） |
| ① 后半句：逻辑与时间离场 | 2026-03-05 同句「**13日内需要碰新高或者新高。否则这个票呆的意义就不大，证明自己的买入逻辑和时间有问题**」 | `wolf_early_stop.logic_time_stop` + `t_monitor._check_logic_time_stop`：建仓后 13 交易日窗口走完仍**从未碰前高** → 离场（碰即算、盘中最高计入、窗口未走完不动作、数据不足不动作）。`WOLF_LOGIC_TIME_STOP=0` 关 |
  | ② 成趋势后→趋势线法 | 2026-03-06「成为趋势后…这个就没意义了…用**趋势线**的方法…不是一个策略用到底的」 | ❌ **未落地 → 用自研振幅 `stop_loss_price` 顶着**。<br>**⚠️ 2026-09-11 更正**：此前本行写"语料**无任何参数**"是**错的**——狼大本人给了成套参数：2021-01-28「我主要做波段…**用13日 34日做强弱分类，60日是我的底线**，甚至**没站稳34日带量下穿的我都会砍掉**」、2016-03-30「趋势线用**13和34天**来标」、2016-08-05「除非**跌破趋势线**或者大盘崩」、2025-12-08「站稳下跌趋势线就加仓」、2026-01-12「收黑K**跌破5日线**…减仓避一下」。<br>→ 用户 2026-09-10「先用 stop_loss_price、回测后再定」的决策**建立在我这条错误陈述上**，应按原意**重新评估**（见 §11.2）。⚠️ 另：该口径对 253/254 持仓**非无条件可用**（switch 腿布腿不带止损价，须触发过一次重建后才有）—— 见 §12.11(a) |
  | ③ 指数大级别止损 | 2026-08-27 549楼「**只看指数大级别**如果不走大5浪而转为下跌1浪就止损」 | `wolf_context.index_level_stop` + `t_monitor._check_index_level_stop`（只取 `wave_state.level=='down'`，带过期护栏；`WOLF_INDEX_LEVEL_STOP=0` 关） |
  | ④ 止损时点 | 2026-03-23「每天的止损**绝对不应该是下午1点到2点半**…要么早上卖要么尾盘卖」 | `t_monitor._stop_time_ok`：禁止 [13:00,14:30) 执行（该时段仅预警） |
  | ⑤ 止损预设 | 2026-08-19「按计划做 然后**设定好止损**就行了」 | 波段低点**以建仓日为锚**重算（等价建仓时锁定、不随行情滚动）；「没有收回」一半由收盘确认守卫承担 |
  | ⑥ 组合层（用仓位而非止损） | 2026-02-02「仓位一定要控制」+ 2026-04-15「70% 毫无压力」 | P2-5 `position_cap` 总仓位上限 70%（见 §8） |
  其余配套：`stop_loss_price` 由 `t_build.build_t_conditions` 写入（振幅口径，自研；非狼大口径）；
  `WOLF_STOP_CLOSE_CONFIRM`（收盘确认/假跌破）、`WOLF_BASE_EXIT_CLOSE`（收盘清仓含底仓/盘中减半）见 P2-4。
- auto_exit：自动离场腿（持续监控）。

---

## 10. 复盘 / 评估链

- 生产：每日复盘（16:00）、周度反思（周五 15:30）、P2 Gate 每日观察报告（15:20）。
- 评测：`judge_wolf_event_alignment.py`（买点与狼大事件对齐率，v3 ±5 日 92.9%）。
- **沙箱大样本回测（本轮新增，不在生产调度内）**：`/app/data/_bt_pit/`，脚本 `/app/jobs/_bt_*.py`（gate 逐日回放 → 布腿复刻 → brze 5min 触发 → 分层统计 → REPORT.md）。

---

## 11. 与狼大流程的对照（2026-09-11 用**穷举法**重建）

### 11.0 覆盖声明（结论只在此范围内成立）

- **语料**：NGA 楼层 818 楼（**2026-07-31 → 09-07**）+ XLS 汇总 25,807 条（**2016-01-02 → 2026-08-14**）；
  已剥除 `[quote]` 与 NGA 的 `> *用户*` 引用段，只留他本人的话。
- **方法**：**数字锚定穷举**（动作词 × 阈值数字/时段），**全量计数、不带 limit**。
  ❌ 不再使用"关键词 + limit"—— 旧版本表就是这么做的，而它与"只在 NGA 上检索"叠加后造成了**系统性漏检**。
- **为什么重建**：旧 §11 用错方法 → 一轮穷举就**推翻了 2 条旧结论**（见 11.2）。
  根因复盘：①两语料覆盖不重叠（NGA 无 2026-08 之前）；②高频词 + limit 截断，且他给规则时**常常不出现该主题的名词**
  （例：讲仓位分档的答案是「主升趋势就75%以上…调整就50%」，**一个"仓位"都没有**）；③先框定问题再搜印证词。
- **本轮局限（不得越界引用本表）**：
  ① 只穷举了**含数字/时段**的规则，**纯定性规则**（如"不追高"）覆盖较弱；
  ② XLS 止于 2026-08-14，其后只有 NGA；
  ③ 下表"生产落点"是**逐行 grep 核对**得到的，**未做端到端行为验证**（除已单独验证的仓位分档）；
  ④ 表中标 ❓ 的行表示**本轮未复核**，沿用旧结论，需按新方法重做。

### 11.1 规则族矩阵

| # | 狼大规则族 | 语料证据（原话 + 日期） | 语料覆盖 | 生产落点 | 状态 |
|---|---|---|---|---|---|
| 1 | **按市况分档给仓位** | 2026-01-17「**主升趋势就75%以上** 然后盘中满仓滚动啊 **调整就50%** **有风险就30%** **下跌趋势就不做**」 | 仓位/资金族 287 条 | `wolf_discipline.position_cap.tier_targets`（DB 单行 JSONB）+ P3 现金底线同源 | ✅ 已落（映射属外推） |
| 2 | **仓位下限** | 2025-08-11「这个位置 **仓位低于55%** 日内分时低于80%都是不太合适」 | 同上 | `tier_floor.build=55`（其余档 0） | ✅ 已落 |
| 3 | **条件式仓位目标** | 2026-02-05「对啊 **只要当天收盘没有跌破前一天低点 都是70%仓位** 没走弱不用减仓」；2026-09-03「我这里**仓位不会低于65%收盘**，日内做T仓位20%」；2026-01-27「**收盘60%-70%**」 | 同上 | ❌ **未落**（判据"收盘 vs 前日低点"我们已在算，可直接接线） | ❌ 缺口（本轮新发现） |
| 4 | **趋势线法（层②）** | 2021-01-28「我主要做波段…**用13日 34日做强弱分类，60日是我的底线**，甚至**没站稳34日带量下穿的我都会砍掉**」；2016-03-30「**趋势线用13和34天来标**」；2016-08-05「除非**跌破趋势线**或者大盘崩」 | 13和34：5+6 条；55日线 2；破趋势线 1+3；破5日 8；跌破5日线 3 | ❌ **未落**：仍在用自研振幅口径 `stop_loss_price` 顶着 | ❌ 缺口（**推翻旧结论**，见 11.2） |
| 5 | 止损：13日内破波段低点−3%未收回 | 2026-03-05（同句还含"13日内需要碰新高"） | 止损族 70 条 | `wolf_early_stop.resolve_stop`（建仓初期档） | ✅ 已落 |
| 6 | 止损：13日内未碰前高→离场 | 2026-03-05 同句后半 | 同上 | `wolf_early_stop.logic_time_stop` + `t_monitor._check_logic_time_stop` | ✅ 已落 |
| 7 | 止损：指数大级别转下跌 | 2026-08-27 楼549「**只看指数大级别**如果不走大5浪而转为下跌1浪就止损」 | 浪型族 219 条 | `wolf_context.index_level_stop` + `t_monitor._check_index_level_stop` | ✅ 已落 |
| 8 | 止损时点（禁 13:00–14:30） | 2026-03-23「每天的止损**绝对不应该是下午1点到2点半**…要么早上卖要么尾盘卖」 | 时间/时段族 214 条 | `t_monitor._stop_time_ok` | ✅ 已落 |
| 9 | 周末降仓 / 板上减半 | 2026-09-01 楼678「**吃一口减一半**」「**板上减了**」 | 减仓/清仓族 246 条 | `wolf_discipline.weekend_de_risk` / `board_half` | ✅ 已落 |
| 10 | 兑现：T 目标 3–5 个点 | 2026-08-13 楼275/280「至少能有吃 **3-5个点** 的幅度吧」；2026-09-02 楼728「**3个点**就远远超过这个量」 | 做T族 82 条 | `roundtrip_sell.ROUNDTRIP_SELL_UP=0.03`（下沿） | ✅ 已落 |
| 11 | 小赚就兑现（浮动止盈） | 2026-09-01「吃一口减一半 安全第一」；回测 47%→53%（+rs 闸 56%） | 同上 | `wolf_discipline.profit_take`（**enabled=false**） | ⚠️ 已实现未开 |
| 12 | 低吸：挂前一天低点、缩量才接 | 2025-03-06「挂前一天的低点 能买进去就做正T」 | 建仓/加仓族（大户） | 254 腿 `dip_prev_low ∧ 0<vol_ratio≤0.9` | ✅ 已落 |
| 13 | 龙头优先、绝不后排 | 2026-01-16「后排反倒不能去 要看好龙头那些」 | 选股/龙头族 91 条 | `pick_v2` 组内前2 + `pick_buy` leader 榜 + U9 分位 | ✅ 已落（阈值无出处） |
| 14 | 风向标死了就不做 | 2026-01-12「龙头风向标死了就不能做了」 | 同上 | `WOLF_PICK_WIND_HARD=1` + 卖侧 + 禁回补 | ✅ 已落 |
| 15 | 大盘/板块没危险才低吸 | 2025-03-06「你首先得判断现在大盘行情没有危险 板块没有危险 那就可以做」 | 浪型族 219 条 | `systemic_block` + P2-2 `theme_buyable` | ✅ 已落 |
| 16 | 缓跌不买 / 急杀可买 | 2025-06-05「**急杀可以买，缓跌不买**」 | 建仓/加仓族 | P2-3 `slow_decline` | ✅ 已落 |
| 17 | 收盘跌破才出 | 2026-01-29「我说了 **收盘跌破我才出**」；2026-01-12「**等收盘确认破位出清**」 | 止损族 70 条 | P2-4 `_stop_close_confirm` + 卖量分级 | ✅ 已落 |
| 18 | 去弱留强 | 2026-04-23「**反弹的时候卖弱的 留强的 不要搞反了**」 | 选股族 91 条 | P1-6 `position_discipline` | ✅ 已落（**今天才修好静默失效**） |
| 19 | 底仓不动、T仓反复做 | 2026-08-04「你们始终分不清 **做T仓位和底仓的区别**」 | 仓位/做T族 | 100 股底仓保护 + 非消费式腿 | ✅ 已落 |
| 20 | 「无利空」前提（意外事件才豁免） | 2026-03-05「是自己逻辑的有效跌破 **除非是意外事件，黑天鹅那种**」 | 止损族 | `wolf_neg_event` 公告判据（**只对个股有效**，ETF 无公告） | ✅ 已落（数据源属外推） |
| 21 | 只做主线 | 语料高频（"主线"为核心词）；gate → 09:20 布腿 | 选股族 | `mainline_gate` + 布腿器 | ⚠️ **非唯一权威**：`rotation_switch_arm` 有 defensive_resource 非主线分支；`auto_trade_*` 5 档走 LLM agent 不经 gate |
| 22 | 急杀抢反弹（253）的时段约束 | 表达式仅 `m5_dump≥0.4 ∧ average>0 ∧ current>0` | — | ❓ 是否有"09:45 下限"语料未复核 | ❓ 待复核 |
| 23 | 「调整浪内也做主线」 | 与 P1-4 相悖，故 P1-4 不做 | — | — | 🔶 有意不做 |

### 11.2 本轮**推翻**的旧结论（2 条，均为"语料里没有"型误判）

1. **仓位分档**：旧结论「P3 三仓档位是自造术语、狼大只有底仓/T仓/现金」——
   **错**。他 2026-01-17 明确给了四档目标（75/50/30/不做）。骨架有据，自造的只是**命名与阈值**。
2. **止损层②趋势线法**：旧结论「语料**无任何参数**，唯一"破5日/破趋势线"是 2022-04-26 一位用户自述、狼大未背书」——
   **错得非常彻底**。他本人 2021-01-28 给了成套参数（**13日/34日强弱分类、60日底线、没站稳34日带量下穿就砍**），
   2016-03-30 说「趋势线用13和34天来标」，2016-08-05 说「除非跌破趋势线或者大盘崩」，2025-12-08 说「站稳下跌趋势线就加仓」，
   2026-01-12 说「收黑K跌破5日线…减仓避一下」。
   ⚠️ **这直接影响一个既有决策**：用户 2026-09-10 拍板"趋势中段先用 `stop_loss_price` 顶着、回测后再定"，
   其**依据是我给的错误陈述**（"语料没给参数"）。现在参数存在 → 该决策应**按原意重新评估**，而不是继续当作"没有更优口径"。

### 11.3 以**他的成文流程**为准的对齐矩阵（2026-09-11，见 `wolf-playbook.md` §15）

**为什么单列这张表**：§11.1 是按"规则族"组织的，而 §15 收录的**三篇他自己写下的流程**（2025-04-15 买卖做T方法、
2025-04-21 复盘流程、2026-01-27 调整期四步）是**原文级的操作说明**，比二手归纳可靠。
下表把它**逐条**对照到系统（**每条都 grep 核过代码**，不是文档声称）。

#### A. 2025-04-15「我的买卖做T方法」

| 要素 | 他的要求 | 系统落点 | 状态 |
|---|---|---|---|
| A1 前一日**流入前5/流出前3** | 看板块资金流排名，**避开"三日都是流出前3"的板块** | 只有主题级资金危险门（`theme_fund_danger`，连续流出即禁新开）；**无"排名前5/后3"与"连续三日"口径** | ⚠️ 部分（口径不同） |
| A2 **融资盘**增减 | 大量减少肯定不对 | `p2_entry_gate` 的 `margin_burst`（两融净卖→禁新开）+ trade_graph 上下文带两融余额/20d/净买 | ✅ 部分实现 |
| A3 **A50/沪深300/科创50 期货多空** | 用来**分辨次日开盘黄线还是白线在上** | ❌ 无任何期货持仓数据 | ❌ 缺口 |
| A4 **黄线上/白线上**判日内强弱 | 黄>白 → 主线强、**做T成功率高**；白>黄 → **减少做T** | ✅ **已落（2026-09-11, commit 37647db + 修正）**：`wolf_index_breadth` —— 白线=上证涨跌幅、黄线=**中证1000 涨跌幅（代理）**，spread=黄−白；`index.huang_bai_spread`/`index.bai_on_top` 已注册进 t_expr、进 t_monitor 快照、进纪律上下文。⚠️ **是代理不是那条线**（软件里的黄线=全市场等权；新浪全市场等权源**实测被限流返回 HTML**，故不用它做默认，若开启会自动回落）。`WOLF_HUANG_BAI=0` 关 | ✅ 已落（代理口径） |
| A5 **时间窗 9:45–10:00 / 14:00–14:30** | **只做这两个时段**（消息刺激股例外） | ✅ **已落（2026-09-11, commit 5b57f23）**：`wolf_trade_window` —— 在 `t_monitor._round` 对**日内做T买腿**（`low_buy`/`custom_prevlow`）强制；**253 建仓腿/卖腿/止损不适用**（按他自己"大级别买卖是另一个方法"的范围声明）。「消息刺激例外」**默认不实现**（无可靠"刺激"判据）。`WOLF_TRADE_WINDOW=0` 关 | ✅ 已落（**明显收紧**：日内T腿只剩 45 分钟/天） |
| A6 量能两形态（低开缩量黄线上不破支撑 / 高开放量黄线上不破跳空缺口） | 这两种情况**加仓进攻板块成功率高** | 有 `vol_ratio`、`quote.average`、`index.m5_dump`；**无"跳空缺口"字段**、**无低开/高开与黄白线的组合判据** | ❌ 缺口 |
| A7 缩量转放量进场（条件5） | 分时接近下方支撑、缩量转放量时进场 | `minute.m5.t1_shrink_expand`（T1缩转放=正T买点），已接入做T字段 | ✅ 已实现 |
| A8 想追高的改在 14:00–14:30 回补（条件6） | 要求上涨放量/回调缩量，补进攻板块里涨得不多的 | 无对应腿 | ❌ 缺口 |
| A9 **不可加仓的 4 种情形** | 黄白线交叉三情形 + 接近大指数级别压力位 | 依赖 A4（缺失）；"大指数级别压力位"也无字段 | ❌ 缺口 |
| A10 **当日卖出条件清单**（6 条） | 消息刺激短时上涨／碰自身均线或 **BOLL 上轨**／指数碰大级别压力位／分时被大量抛售／黑天鹅资金出逃 | 有 `quote.vwap_break`（黄线）、`minute.m5.t_sell`（确认制T出）、`board_half`（板上减半）；**BOLL 只存在于 industry_leaderboard（行业因子），未用于个股卖出** | ⚠️ 部分 |
| A11 七要素之一：**不抗有明确利空风险的板块及个股** | — | P2-2 `theme_buyable`（结构∧资金）+ 层①公告判据 `wolf_neg_event` | ✅ 已实现（两处） |
| A12 七要素之七：**不要把做 T 的仓位变加仓**；"三日内没达预期=自己预期有问题" | — | `roundtrip_sell`（等量换手）+ `WOLF_REFILL_MAX_PER_DAY=2` + 层①两条（13 交易日）；**没有"三日内未达预期"这个时间判据** | ⚠️ 部分 |
| A13 七要素之三/四：不因小波动激情来回交易、不板块快速轮动期切换 | — | 做T腿有 **5 分钟冷却**（防刷）；无"小波动/轮动期"的抑制逻辑 | ⚠️ 部分 |

#### B. 2025-04-21「每日复盘流程」

| 要素 | 他的要求 | 系统落点 | 状态 |
|---|---|---|---|
| B1 **前 8 项打分、两倍差判多空** → 预判次日指数前 2 小时方向 | 表格化打分法 | 有 `mainline_gate`（主题确认链）与 `heat_v2`（热度），但**不是这套多因子打分表**，也没有"次日前 2 小时方向"的输出 | ❌ 缺口（**可落地**：我们有大部分底层数据） |
| B2 看第 5/6 项（避开流出、关注流入） | — | 同 A1 | ⚠️ 部分 |
| B3 **看龙虎榜判资金结构** | — | 只有 P2 的 `lhb_foreign_sell`（外资净卖方向） | ⚠️ 部分 |
| B4 **看涨停板方向 / 梯队结构 / 毕业照 / 上板失败** → 判板块强弱 | — | **`连板`/`涨停梯队`/`梯队` 全库 0 命中** | ❌ 缺口（**明确**） |
| B5 扫外围（各 0.1 分） | — | `p2_entry_gate` 有部分宏观开关（yield_spike / gjd_withdraw 等） | ⚠️ 部分 |
| B6 做好计划、次日执行时微调 | — | `t_conditions` 表达式腿 + 计划触发（`_check_plan_triggers`） | ✅ 机制在 |

#### C. 2026-01-27「调整期四步」

| 要素 | 他的要求 | 系统落点 | 状态 |
|---|---|---|---|
| C1 ①**先减仓位**（利润垫："赚的取一半留一半"） | 调整期降仓，且**利润垫**决定敢留多少 | 仓位分档（市况档位）已落；**"利润垫"这个量没有建模**（他明确说它决定能留多少仓） | ⚠️ 部分 |
| C2 ②找「**跌得少、弹得早**」的方向 | — | 只有**持仓层**去弱留强（P1-6）；**方向层**没有这个判据 | ❌ 缺口 |
| C3 ③不要在**重回上涨趋势前**不理智加回 | — | P2-2（结构未确认不新开）+ P0-4（买不到就等） | ✅ 部分实现 |
| C4 ④趋势核心底仓 + 灵活仓打野做超额 | **"做超额是第四，先做好 123"** | 底仓/T 仓二分 + 100 股底仓保护 | ✅ 机制在（但**没有"123 未完成则禁止做超额"的优先级门**） |

### 11.4 由本矩阵导出的**优先缺口清单**（按"他的权重 × 我们的可实现性"排）

| 优先级 | 缺口 | 为什么优先 | 可落地性 |
|---|---|---|---|
| ~~P1~~ | ~~A4 黄白线~~ | — | ✅ **已完成 2026-09-11**（口径改为**中证1000−上证**的指数对代理：原设计想用"指数均价=累计额/累计量"，实测**不可行**——指数成交额/成交量是"元/股"量纲，与指数点位不可比；软件里的黄线是**等权线**；新浪全市场等权源生产实测被限流） |
| ~~P1~~ | ~~A5 正向时间窗~~ | — | ✅ **已完成 2026-09-11** |
| **P2** | **B4 涨停梯队 / 连板结构** | 他复盘流程里判"板块强弱"的主要依据；全库 0 命中 | 中 —— 需涨停/连板数据源 |
| **P2** | **A6/A8 跳空缺口 + 高开低开组合判据** | 他"加仓做进攻板块"的两个形态之一 | 中 —— `quote.open/pre_close` 已有，缺缺口基准 |
| **P3** | **B1 复盘打分表 → 次日前 2 小时方向** | 他的日常流程主干 | 中 —— 底层数据大半已有，缺"两倍差"这套打分与输出 |
| **P3** | **C2 方向层"跌得少弹得早"** | 调整期核心动作 | 中 |
| **P4** | **A3 期货多空（A50/沪深300/科创50）** | 他用来预判黄白线 | 低 —— 需期货数据源 |
| **P4** | **C1 利润垫建模** | 决定调整期敢留多少仓 | 中 |
| — | A10 BOLL 上轨用于卖出 | 他当日卖出条件之一 | 中 |

### 11.5 尚未复核（明确留档，勿当成已完成）

- 上表 ❓ 行（253 时段约束等）沿用旧结论，**未按新方法重做**。
- 旧审计 §5.2 的 **S 表"自造机制"判定**：其中"语料未见"型结论多来自关键词检索，
  已证与 P3 同类的误判风险 → 需逐条用"全量计数 + 同义词扩展"复核（P3 那条就是全量计数，可信）。
- 纯定性规则（不含量化阈值）本轮覆盖弱，例如"不追高""买在确定"等。

## 12. 已知偏差与未落地（重要）

1. **回测只覆盖布腿路径 B**（当日 confirmed 主题池）；路径 A（rotation 链候选）未建模（其输入 rotation_universe_result.json 无历史快照）。
2. **回测的布腿时序需修正**：本轮回放按「gate 当日布腿、选股截至前一日」建样本；而生产是「gate 收盘后 18:45 产出 → **次日 09:20 布腿**、选股截至 gate 当日」。两者差一天，需重跑样本（已记录）。
3. **253 的指数源澄清（2026-09-10 更正）**：本条原表述（"253 的指数用 ETF 代理"）易被误读为"生产也用 ETF 代理"。
   实际——**生产用真实上证指数**：`t_monitor._index_m5_dump()` 与 `_index_intraday_dd()` 均走腾讯
   `fetch_tencent_mkline("sh000001", freq="m5")`。**ETF 代理只是沙箱回测侧的限制**（回测走 brze，而上证指数分钟在 brze 不可用，
   故用 510300 等代理，与上证 5min 收益相关系数 0.89）。
   → 结论：253 的**生产语义与回测语义存在数据源差异**，回测结论外推时需考虑该 0.89 的相关性损失。
4. **主题成分非严格 PIT**：农业/金融/稳增长基建 用生产 stock_confirm_result；其余主题用 THEME_CONCEPTS × 概念映射近似（按市值取前 120）。
5. **成交假设**：回测按「一腿一次、每笔满额可成交」；未建模资金闸分批、涨跌停不可成交、滑点。
6. WOLF_TASKS_OVERVIEW.md §0 已列出的未做项：个股级两融/杠杆检查、板块与个股级机构行为、持仓纪律与完整复盘闭环。
7. **路径 A 的日线窗口被硬编码冻结**（2026-09-10 新发现，**待修**）：`rotation_switch_arm.py:110`（`pick_buy`）硬编码
   `end_date="20260901"`、`:193`（legacy `confirm_pick` 回退分支）硬编码 `"20260908"`。与已修的 `mainline_gate_daily.py`
   硬编码日期属同类缺陷 → 这两条路径的 `position_features` / LOW|MID 位置分类长期使用**过期收盘序列**。
8. **"曾确认 40 日窗"资格闸未接入**（2026-09-10 核实）：`mainline_confirm_state.chain_qualified` / `ts_qualified`
   有定义但**无任何调用点**；买侧实际使用 `gate_confirmed_today`（取最近一份 `mainline_gate_*.json`）。
   详见 `docs/wolf-dip-entry-rule.md`（已同步更正）。
9. **2026-09-10 删除的自造机制**（依审计 §5.2，用户拍板）：S1 ETF 兜底腿、S2 破位禁低吸④门、S3 trail_break 移动止损、
   S4① 60分MA 缺数据硬禁建仓、S5 账户级熔断三件套（总回撤/连亏/回转额）、S6 底仓浮亏守卫调用点；
   S7 回补口径已统一为 `WOLF_REFILL_MAX_PER_DAY`（默认 2）。对应 commit：566a410 / b26878a / 82a486f / ebde502。
   **保留**的同类机制：S8 板上减半（经复核**有狼大原话支撑**：2026-09-01 楼678「吃一口减一半」「板上减了」）、
   S4② 日内分位硬禁（机制=不追高，与狼大一致）、S5 之外的网关层硬闸门、S10 P2 宏观开关。
10. **253/254 建仓链的 P0-1 缺陷已修**（2026-09-10）：`wolf_253_build.build_253` 成功分支原引用未定义的 `snapshot`
    → 成交后抛 NameError 被 except 吞掉、上报 blocked、`mark_base_254` 永不调用 → 254「3 日内 ≤2 次回补」链整条失效。
    已修（commit b1b5c20）。**此前任何基于 `t_triggers.status` 的"建仓成功率"统计均不可信**。
11. **止损六层落地的口径与已知未定**（2026-09-10 立，2026-09-11 补 (c)(d)）：
    (a) **层②趋势线法没有口径** —— ❌ **本条已于 2026-09-11 更正**：原写"语料没给任何参数"是**误判**
        （来自"关键词+limit + 只在 NGA 检索"的漏检）。狼大本人给过成套参数，见 §11.1 第 4 行与 §11.2 第 2 条：
        2021-01-28「用 **13日 34日做强弱分类，60日是我的底线**，没站稳34日带量下穿的我都会砍掉」、
        2016-03-30「趋势线用 **13和34天** 来标」、2016-08-05「除非 **跌破趋势线** 或者大盘崩」、
        2025-12-08「站稳下跌趋势线就加仓」、2026-01-12「收黑K **跌破5日线**…减仓避一下」。
        → 结论：层② **有口径、只是没落地**；用户 2026-09-10「先用 stop_loss_price 顶着、回测后再定」的决策
        **建立在我给的错误前提上**，需按原意重新评估。
        用户决策：**趋势中段暂用既有 `stop_loss_price`**（振幅口径，`avg×(1-max(3%, 振幅×0.55))`），
        等回测（`docs/backtest-plan.md`）出结果再定趋势线口径。落地位置 = `wolf_early_stop.resolve_stop` 的
        非建仓初期分支（`held > 13` → 原样返回条件表里的 `stop_loss_price`，不新造机制）。
        **「确认可用」的核查结论（2026-09-11，逐行核对）**：
          · 写入点**只有一处**：`t_build.auto_gen_conditions_for_build`（`t_build.py:1345/1350`，
            `rule_stop = avg×(1-max(3%, amp_med×0.55))`，并钳制 `> avg×0.99` 时回落规则值）；
            规则公式本体在 `t_pool.build_t_conditions`（`stop = cost×(1-max(3%, amp×0.40))`）。
          · 读取点：`t_monitor._check_stop_loss`。
          · **可用性边界（重要）**：`rotation_switch_arm.arm()` 布的 253/254 腿（`account_id='stock'`,
            `publisher='switch'`）**布腿时不带 `stop_loss_price`**；只有当该腿触发过一次、走
            「消费式条件自动重建」（`t_monitor.py:1696-1712`，**同账户重建**）之后才会被写入。
          ⇒ 所以趋势中段"用 stop_loss_price 顶着"对 253/254 持仓**不是无条件可用**。
            建仓初期（≤13 交易日）由 ① 的结构线兜住；**超过 13 交易日而条件表仍无止损价 → 该持仓无任何止损**
            —— 这是层②缺口径留下的**残留风险敞口**，也是回测要优先量化的一项（见 backtest-plan §1.5 止损②行）。
    (b) **层①的"波段低点"窗口语料未给** —— 狼大只说"波段低点"，未给回看窗口。
        本仓取 `WOLF_SWING_LOW_WIN=13`（与"13日内"同数），**可回测调参**；`WOLF_EARLY_STOP_PCT=3` 为原话数字。
        注：该止损线在多数情形下与既有振幅止损**量级接近**（253/249 属回踩低吸，建仓日常在近 13 日低点附近），
        但方向上**替换**而非叠加 —— 狼大「不是一个策略用到底的」，把两条线做 min/max 复合属自造。
    (c) 层①的前提「无利空」**已落代码**（2026-09-11）。原话（2026-03-05，答「-3%再卖是因为破了-3%就是有效跌破了吧」）:
        「是**自己逻辑**的有效跌破 **除非是意外事件，黑天鹅那种**。如果是**无利空**13日内下跌那新低后-3%
          就是逻辑问题 要控制损失就必须止损。后面涨是别的逻辑」
        → 语义**与直觉相反**：**无利空**才套用 ① 的结构线；**有利空/意外事件/黑天鹅时不套**
          （跌下去是"别的逻辑"，不是自己买入逻辑被证伪）→ 退回 `stop_loss_price` 兜底，不放大风险。
        数据来源：本仓**拿不到可靠的个股利空/公告面数据** → 做成**显式输入**
          `data/wolf_negative_events.json`（`{"SH600000": {"date":"20260910","note":"…"}}`），
          由人工/上游 AI 标记；只认**持有期内**（事件日 ≥ 建仓日）且 `WOLF_NEG_EVENT_DAYS`（默认 13）内有效；
          读文件失败 → **fail-open 到"无利空"**（照常按 ① 止损，不能因为读不到就不止损）。
        开关 `WOLF_NEG_EVENT=0` 忽略该前提。落地位置 `wolf_early_stop.negative_event` / `resolve_stop(neg_event=/symbol=)`。
    (d) **层①原话的后半句「13日内需要碰新高」已落地**（2026-09-11，用户拍板补上）:
        同一条原话：「**13日内需要碰新高或者新高。否则这个票呆的意义就不大，证明自己的买入逻辑和时间有问题**」
        → 建仓后 13 个交易日是**观察窗**：窗口走完仍**从未碰过前高** → **离场**（不是等它慢慢跌到止损线）。
        落地 = `wolf_early_stop.swing_high_asof / made_new_high / logic_time_stop` +
        `t_monitor._check_logic_time_stop`（接入周期块，紧跟指数级止损之后）。
        · 前高 = 建仓日（含）之前 `WOLF_SWING_HIGH_WIN`（默认 13）根日K 的最高价（与波段低点同窗同源）；
        · 「碰新高或者新高」按原话取 **>= 前高**（碰即算，不要求严格突破）; **盘中最高计入**
          （腾讯 `quote.high`；否则窗口最后一天的盘中触碰会漏判、白多拿一天）;
        · **窗口未走完（持有 < 13 交易日）一律不动作**（不能买三天没新高就卖）;
        · 数据不足（前高算不出）→ **不动作**（fail-open，不凭缺失数据卖票）;
        · 复用 `_stop_exit_volume`（收盘窗口→清仓 / 盘中→减半）+ ④ 时点门 + `is_stop_loss=True`;
        · 「无利空」前提**沿用**：黑天鹅导致的不创新高属"别的逻辑"（这是**同窗逻辑的外推**，
          原话的前提只针对 -3% 那条；`WOLF_NEG_EVENT=0` 会把两条一起关掉）。
        开关 `WOLF_LOGIC_TIME_STOP=0`、窗口 `WOLF_LOGIC_TIME_STOP_DAYS=13`。
        **数据源前置（2026-09-11 生产实测，重要）**：本机制依赖 `t_monitor._daily_dated`，
        而它的本地缓存源 `data/stock_5m_bt`（37 个文件，最新 mtime 2026-09-03 14:25）与
        `data/recent_sync`（14 个文件，2026-09-03 21:21）都是**一次性导出、没有常驻同步任务** ——
        当日实测在册 13 个标的里 **0 个新鲜**（3 个停在 09-03、10 个无文件）。
        → 已给 `_daily_dated` 加**实时源兜底**（本地最新日期早于"今天-2 自然日"时改用实时源；
        结果按 symbol+日缓存；`WOLF_DATED_LIVE_FALLBACK=0` 关闭）。兜底是**两级**：
        `t_build._fetch_daily_bars`（Tushare 主源 → 东财降级）→ 失败则 `_fetch_daily_tencent_dated`
        （腾讯前复权日线，实测更稳且含当日）→ 再失败才退回本地缓存并打印原因。
        （加第二级的原因：生产实测 Tushare/东财会短暂失败："东财日线失败: Remote end closed connection"。）**注意**：`_prev_daily`（P1-6 去弱留强、m5dump 的 prev_days、
        收盘确认守卫的 prev_daily）**仍只读那两个冻结目录**，未改 —— 属既存的数据源缺口，单独留档待定。
        **未做**：`t_conditions.time_stop_open` / `time_stop_close` 两列仍是**被写入但从不被读**
        （`t_pool.py:290` 写 `"14:45"`，语义上其实是**日内时段**而非日期）→"机制静默失效"第 6 例**仍存在**；
        本机制没有复用那两列（语义不符），故该死字段问题单独留档。
12. **【事故·已修】止损重复执行：盘中"减半仓"被同轮执行两次 → 变成全清（2026-09-11 09:36, SH588170）**：
    (a) **现象**：`paper_trades` 两条间隔 4 秒的卖出，各 17000 股（0.897），持 34000 → **清零**；
        reason 均为「止损离场（stop_loss, 盘中减半仓）」。触发源 = 当日刚上线的 ① 建仓初期结构止损
        （SH588170 结构止损 0.898，现价 0.897 击穿）。
    (b) **根因**：`_round` 是 `for cond in conditions:` **逐条件**调用 `_check_stop_loss`，而该函数原先的
        "当日已止损过则跳过"查的是 `t_triggers WHERE event_type='stop_loss'` —— **这条路径从来不写这种行**
        （成功时只 `update_condition_state(armed=0)`）→ 查询恒为空 → **去抖形同虚设**（死守卫，第 7 例）。
        同一标的有 2 个条件 → 同一轮执行两次"减半仓" → 17000×2 = 全清，**违反狼大「底仓不卖」**。
    (c) **修复**：①新增内存当日去抖 `TMonitor._stop_done_day`（同轮/同日即时生效，不依赖 DB 时序，
        在 `gateway_execute` 返回 success 后立即打标记；被拒则不打，保证当天真跌破还能处理）；
        ②成功后在 `t_triggers` 落一行 `event_type='stop_loss'`、**status='executed'（终态）** 作为审计
        与跨重启去抖依据 —— **不能用 pending**：`claim_pending_trigger` 是**按 status='pending' 无差别认领**
        （只按 id 顺序取第一条，不筛 event_type），审计行留 pending 会被 t_bridge 当成待办**再卖一次**。
        为此给 `t_db.insert_trigger` 加了可选 `status` 参数（默认 pending，既有调用方行为不变）。
    (d) **回归测试**：`backend/tests/test_stop_dedup.py` 5 例（两条件同标的只执行一次 / 审计行为终态 /
        库中有当日行则拦下 / 网关拒绝不打标记 / 收盘档位仍是清仓）。
    (e) **教训**：新增一个"能真的卖出"的机制时，必须同时检查**它经过的每一道去抖/限流是否真的生效**
        （本例里守卫写着、也能查到代码，但查询条件指向一张永远不落该 event_type 的表）。
13. **层①「无利空」前提的数据源 = 公告判据（2026-09-11 落地）**：
    (a) **为什么不是新闻情绪**：生产 news.db 有 62,463 条新闻且已带 `sentiment`/`impact_level`，但
        `sentiment='negative'` 占约 19%（**常态高发**，而黑天鹅的定义是罕见），且会把「中报净利润同比下降 1.95%」
        这类**常规财报**算成利空 → **错误豁免 → 该止不止**，方向危险。`impact_level` 衡量的是市场影响力
        （S 级利好也是 S 级）→ 同样不能用。**已明确否决**。
    (b) **改用公告**：`ak.stock_notice_report(symbol='全部', date=YYYYMMDD)`（生产实测 1,144 条 / 2.1s）
        带**结构化 `公告类型`**（风险提示/退市/停牌/处罚/立案/诉讼/仲裁/违规/调查/会计差错/非标…），
        「意外事件/黑天鹅」这一档可由类型直接识别。
        注：个股接口 `stock_individual_notice_report` 在生产 akshare 1.18.81 抛 `KeyError: '代码'`（坏的）
        → 走"拉全市场清单再按持仓过滤"。项目此前**未使用**任何公告接口。
    (c) **判据调参（真实数据）**：首版白名单在 2026-09-11 当日 1,144 条公告上命中 **38 条**，其中约 25 条是
        **常规质押/解质押**（类型 `股份质押、冻结` 含"冻结"二字被误纳入类型白名单）＋实控人“变更”（方向不定）
        ＋转股价下修（偏利好）＋换股吸收合并导致的“终止上市”（重组，通常利好）。
        收紧后（冻结改为**标题**判据；质押全家族/换股吸收合并/转股价修正进排除表；实控人只认 被/失联/留置）
        命中 **16 条 = 1.40%**，且逐条人工核对均为真风险事件。**回归测试钉死**在
        `backend/tests/test_wolf_neg_event.py::TestProdTuningRegression`。
    (d) **风险不对称（判据纪律）**：误报 = 该止损没止损（危险）；漏报 = 少豁免一次、仍按 ① 止损（安全）
        → **宁可漏不可滥**，不追求召回。拉取失败 → **不动既有标记文件**（fail-safe）。
    (e) **适用范围（勿越界）**：**只对个股有意义** —— ETF 没有个股公告（实测当日持仓 588170/512480/600519
        各 0 条，当前 stock 账户只剩 SH512480 一只 ETF，故首跑命中 0）；公告只有 `公告日期`、**无发布时间**
        → 日粒度非 tick；与狼大"盘中看公告知情"仍有差距 → **属合理外推，不等于等价**。
    (f) 开关：`WOLF_NEG_EVENT=0` 忽略该前提；`WOLF_NEG_EVENT_SCAN=0` 停采集任务；
        账户 `WOLF_NEG_EVENT_ACCOUNT`（默认 stock）。

14. **U9 主题容量约束 + S9 兑现幅度（2026-09-11 落地，阈值待回测校准）**：
    (a) **U9**：狼大 2026-09-02 楼729「小票就太多了 不好判断」+ 楼733「农业拉10个点带动的资金量不过100E」。
        此前 `capacity_amt20_yi` **只输出提示、不拦截**。用户决策用**相对分位而非绝对名额**
        → `wolf_confirm_pick.theme_quantile_keep`：主题内 `leader` 分位前 `WOLF_THEME_QUANTILE_PCT`%（默认 50）才可买，
        路径A（`rotation_switch_arm.pick_buy`）与路径B（`pick_v2`）**共用同一函数同一 env**（不再分两套口径）。
        **阈值语料没给数**（只有"小票太多了"这个定性说法）→ 水平由回测校准（`docs/backtest-plan.md` A16），`=0` 关闭。
        过滤点在 `theme_r20`（主题均值）算完之后 —— 否则会污染 rs 闸的基准，等于连带改掉另一条已验证的门。
        边界口径：取 `ceil(n×pct%)` 名，**与第 k 名并列的一并保留**（不按序位切并列）。
    (b) **S9**：`roundtrip_sell.ROUNDTRIP_SELL_UP` 由 **0.008 → 0.03**。
        狼大 2026-08-13 楼275「至少能有吃 **3-5个点** 的幅度吧 哪怕是ETF」、楼280「等下一个 3-5个点 的机会」；
        2026-09-02 楼728「半导体只要 3个点 就远远超过这个量了」。原 +0.8% 属**自设的小止盈**，
        连狼大说的"波动连手续费都不够"那档都不到。可 `WOLF_ROUNDTRIP_SELL_UP` 覆盖（0.05 取上沿）。

---

## 13. 运维要点

- **改脚本类任务（jobs/*.py、apps/*）**：即时生效（子进程执行）。
- **改 backend 进程内模块（t_monitor/trade_graph/p2 等）**：必须重启 backend + worker。
- **改调度**：config/tasks.yaml（worker 需 reload/重启）。
- **回放纪律**：任何 DATA_DIR 沙箱回放，先把「本次会写的文件」列出并 COPY 到沙箱（严禁软链可写文件）；分片并行上限 2（4 分片会被 OOM 静默杀掉）。
