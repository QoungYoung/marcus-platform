# 对齐清单（2026-09-12）

> 目的：一页看清**已经建成什么 / 发现哪些不对齐（带证据）/ 正在做什么 / 还欠什么**。
> 每条都尽量带可核对的数字或原话；不确定的标"未验证"，不做成完成事实。

---

## A. 已建成并验证

| # | 项 | 证据 / 数字 |
|---|---|---|
| A1 | **语料逐条精读**（非正则） | NGA 818 楼 + XLS 25,807 条全覆盖；5 份逐日记录（`docs/wolf-daily-log-*.md`，11,710 行）；蓝图 §1–§15 |
| A2 | **G1 每日决策对象** | `data/decision/<date>.json`；**PG 主存 + 文件镜像**；六层 L1–L6 各带 `basis`；`revision` 递增可追溯 |
| A3 | **G2 每日存档** | 文件快照 + **PG 双写** `daily_artifacts`；含当日覆盖型文件（concept_long / theme_inst_flow / etf_share_flow / main_line_state / stock_confirm_result） |
| A4 | **准入闸真正生效** | `WOLF_DECISION_GATE=1`；**只拦买入**（卖出/止损永不拦）；无对象→拒开新腿（`entry_allowed('20270101')` 实测 False）；60s 缓存；陈旧回退 ≤4 天 |
| A5 | **G9 周末避险 / G10 量能门槛** | 已上线（`WOLF_WEEKEND_HEDGE=1` / `WOLF_VOLUME_GATE=1`）；G10 指令带 `as_of` 日期；实测 09-11 判"破位"（非诱多） |
| A6 | **行情底座** | `mkt_bars_daily`：**169 天 / 929,573 行 / 5,582 只 / 2026-01-05→09-11**（tushare） |
| A7 | **gate 链结论全年无缺口** | `mainline_gate` + `heat_v2` 各 **179 天**（H1 回放 105 天 + 6–9 月 74 天 + 12 天原生产物，来源可区分） |
| A8 | **研报标题库** | `research_reports`：**163 天 / 6,380 条**（0 失败；promax 支持按日查全市场且有历史） |
| A9 | 已关闭/清理 | A5 时间窗关、C2 方向层关、A9 谨慎门关；G13 清掉 4 条测试条件 + 2 个死列 |

---

## B. 已发现的不对齐（带证据，按重要性）

| # | 不对齐 | 证据 | 影响 |
|---|---|---|---|
| **B1** | **gate 不是"选方向"的闸门，只是粗筛**，且**确认集合月月不变** | ① 判据 = `B_only >= 0.35`（`trend_gate_params.json` 标定，F1 0.902）＝主题内成分股**技术结构达标比例**过线；② **生产链必传 `--fusion-json`（heat_v2）→ catalyst/研报只在"不传 fusion 的兜底分支"才被读，即 gate 根本不看研报**；③ 各主题 ratio 基线差异巨大（机器人 0.636/过线率 87%、电力 0.667/82%、新能源 0.714/80%、汽车 0.462/80% ↔ 资源 0.286/50%、金融 0.300/46%、军工 0.222/38%、传媒 0.167/3%）→ **强者恒强形成静态名单**；④ 阈值敏感性：0.20→确认 12 个、**0.35→10 个**、0.50→6 个、0.65→4 个、0.80→1 个（0.35 落在宽区间）；⑤ 每个主题只有 **6–21 只**成分股参与判定，噪声大 | **只作资格闸，不能作主线判定**；主线判定必须改用"**相对变化/边际改善**"（ratio 环比、研报密度变化、资金流变化），阈值要按"选方向"重标，样本过小的问题要单独处理 |
| **B2** | **heat 排序与他的方向基本无关** | 他 top-1 落在 heat 前 3 仅 **8.3%**，平均 heat 排名 **8.4 / 13** | 热度因子（资金流/ETF份额/相对强度）不能代表他的方向判断 |
| **B3** | **gate 与他"真正在做"的方向显著反向**（v2，dsh 直读口径） | 他 `doing` 主题的 gate 确认率 **0.6164** vs **同日基线 0.6987** → **超额 −0.0822（配对 t = −2.16）**，只有 50.5% 的日子为正 | 我们的 gate 不只是"没区分度"，而是**系统性不确认他在做的方向** |
| **B3b** | **研报（catalyst）从未进入生产判据** | 生产 `mainline_gate_daily.py:54` 必传 `--fusion-json heat_v2_<date>.json`；catalyst 仅在兜底分支（未传 fusion）被读 → 研报每月变化对 gate 无影响 | 若要用（IC 仅 0.056）必须按"**变化 + 位置**"使用并标 `title_only` |
| **B4** | **热度排序与他做的方向强反向**（最刺眼的一条） | 他 `doing` 主题的平均 heat 排名 **10.42 / 13**（随机期望 7.0）→ **−3.42，t = −11.89，仅 12.8% 的日子进入前半** | heat_v2 的假设（追资金流入）与他的方法（缩量低吸）**方向相反** |
| **B5** | 标题密度能"认出他在做什么"但不能预测涨 | 他 `doing` 主题的标题分超额 **+32.2（t=6.79，75.9% 天为正）**；但 IC 仅 **0.056 / 0.064** | 可作"注意力/覆盖度"代理（低权重、标 `title_only`），**不能**进 L1 主判据 |
| **B6** | **触发条件不提升胜率** | 首次触发 n=428：胜率 47% / 均值 +0.29%，同池同日随机买入基线 **49% / +0.61%** | 执行层条件（何时按按钮）没有 alpha |
| **B7** | **回测尺子与生产实际不符** | 用"持 T+5"量做 T 腿得 47%，而生产**已实现卖出腿**是 **63.6%**（口径：`direction='卖出' and profit<>0`） | 持续误导所有机制评估（A5/C2 曾被带偏） |
| **B8** | 机制静默失效家族（已修） | `realized_pnl` 从未被写、`risk_breaker` 死守卫、模块不查 `enabled()`（假开关）、外围联动返回假值、`build_etf_flow` 硬编码窗口 | 已逐个修，并加测试钉死 |
| **B9** | 语料映射陷阱（v2 已改用 dsh） | 关键词会把"提到/评论/明确不做"算成关注（他说过「我不做红利」「不做海外链」「我不去那边」） | 已改用 **dsh 直读** 判 `doing` / `not_doing` |

---

## C. 正在做（本轮三步）

| # | 步骤 | 状态 |
|---|---|---|
| C1 | **用 dsh 直读判定"他真正在做的方向"** | ✅ **完成**：151 天全部处理，产出 `wolf_actual_mainline`（`doing[]` 含 direction/action/原话证据、`not_doing[]` 含 why、`notes` 说明无法归因）；有效方向日 **117 天**（与 gate 交集 109） |
| C2 | **加基线对照**（超额命中率 + 配对 t） | ✅ 完成：见 B3/B4/B5（gate 超额 −0.0822 t=−2.16；heat 相对随机 −3.42 t=−11.89；标题超额 +32.2 t=6.79） |
| C3 | **查 gate 为何过松** | ✅ 完成：每日 14 候选 → 中位确认 **10 个**（确认率 60.5%），见 B1 |
| C4 | doing 自由文本 → 13 主题的映射（也交给 LLM） | ✅ 完成：220 个唯一方向串 → **124 个归入 13 主题 / 96 个"非主题"**（仓位管理/指数/泛指/游戏等），落 `wolf_dir_theme_map` |

**C 的总结论（v2，`/app/data/wolf_mainline_consistency2.json`）**：两种口径（LLM `doing` vs 关键词 `mentions`）**结论一致**，
但 `doing` 更清晰 → **我们的 gate 与 heat 都与他"真正在做"的方向显著反向**。
最可能的机制解释：**heat 奖励"已在放量/资金流入"的方向，而他做的是"缩量、还没被资金注意"的方向**（他原话：趁小跳买、量能缩转放）
→ 这是**结构性错配**，不是参数没调好。

---

## D. 待办清单（未做，按优先级）

| 优先 | 事项 | 为什么 / 备注 |
|---|---|---|
| **D1** | **L1 方向层重建**（依据 C 的结论） | 这是当前最大缺口：gate/heat 都无区分度，方向层等于空缺 |
| **D2** | **阶段 1：G3 尺子**（分类型度量 + 回测离场口径改生产实际） | 底座已具备（`mkt_bars_daily` + `daily_artifacts` 179 天）；不做它后面每个结论都不可信 |
| D3 | H1 波浪（`wave_state` 仅 6–9 月 73 天） | 需 `wave_replay.py`（LLM，有成本） |
| D4 | catalyst 打分（若采用 → 标 `title_only=true` + ic 值；**PIT 严格 T-1**） | 等 C 的结论决定权重；H1 gate 结论在补齐前须标 `catalyst_missing` |
| D5 | 阶段 2：§9 硬参数闸门（买点只在下跌里/高开不追 等） | G9/G10 已上；其余仍只是提示 |
| D6 | 阶段 3：腿型收敛（11 种 trigger_kind）+ **G11 买腿是否含基本面** | 他要求"纯资金指标、不含一丝基本面"的机械式入场 |
| D7 | 阶段 4：反事实验收（NGA 30 天带时刻逐日对照） | 最硬的验收，但要在 D1/D2 之后 |
| D8 | 安全：`main_line_judge.py` **硬编码 promax key**（已进 git 历史）→ 改读 `PROMAX_API_KEY` 并轮换 | 实测 env 里那把 key 有效 |
| D9 | 3 个 pending + 3 个 empty 研报日 | 影响 catalyst 覆盖率 |
| D10 | `t_conditions` 里 `custom`/`custom_tech` 等语义不清的条件归类 | 腿型收敛的前置 |

| D11 | **D1 排除型数据（已验收→否决）** | 2026-09-12 完：复用现成 `crowding_pit/stock_crowd_*.json`(20 期 PIT 公募持仓) + `crowding_blacklist.json` 自带规则(n_funds≥4,float≥1%) + 成交占比占据分位。**结论=不接**：主题层 IC≈0(float_avg +0.008 t=0.35)、个股层 IC 亦正(n_funds +0.019 t=2.08)；加排除后基线 **+0.683%(t=3.04) → −0.152%/−0.009%**，他方向命中 23%→12~13%。机制：黑名单 subs 就是「芯片/半导体、材料、AI终端」= **他的第一主线**，排除=与主线对着干（半导体 40→**0** 天、AI 11→**0** 天）。见 `docs/wolf-vs-system-mainline.md` §九 |
| D12 | ~~D1 偏差修正：防御方向过滤~~ **已办结（假设被否定，不改 D1）** | ① **语料取证**（dsh 逐月精读，`jobs/wolf_d12_evidence.py` → `/app/data/wolf_d12_evidence.json`）：他明确回避低波动方向——「选方向看波动幅度，只做波动大的方向，**不碰日均波动仅 2 个点的银行**」（2026-08-03）、「选大科技因单日套利空间（3-10 个点）远大于红利」（08-04）、「**绝对不让自己陷入低波动的陷阱里**」（08-04）、「银行那种波动根本不在我的视野里」（08-05）；量能——「量能由缩转放是介入信号」（08-12）、「缩量不参与」（08-04）。② **验收**（`jobs/eval_d12_vol.py`）：门槛在**主题层面几乎不触发**（所有主题 amp20 3.50%–5.59%，他的"2 个点"是**个股/风格**口径）→ W2(剔除<2%) 从不生效；W1(剔除低波动 1/4) +0.852%(t=3.41)、W3(剔除缩量 1/4) +0.837%(t=3.56)、W4 +0.869%(t=3.47) vs 基线 **+0.781%(t=3.30)**，增量 0.06–0.09pp **在噪声内**；他方向 top1 命中 23%→26%（W1/W4）。③ **关键否定**："防御方向拖累收益"不成立——**农业被选中时 +1.354%(t=2.48)**（第二好），农业+医药合计 +0.659%(t=1.60) ≈ 其余 +0.833%(t=2.88)；真正拖累的是电力/公用 −0.510%、机器人 −0.542%、AI/算力 −0.341%。他从不做的金融/消费/稳增长（各 0–1 天）恰好是 amp20 最低的三个（3.50/3.78/3.86%）→ **他的"不做低波动"在方向层已被自动满足**（D1 选中金融 2 天、消费 0 天、稳增长 1 天）。④ **处置**：**不接门槛**（不做无依据调参）；为对齐他而砍农业反而砍掉真实收益（t=2.48）。见 `docs/wolf-vs-system-mainline.md` §十一 |
| D14 | **D1 的真拖累方向**（D12 副产物） | V0 按主题分解：电力/公用(19 天,**−0.510%**)、机器人(11 天,−0.542%)、AI/算力(11 天,−0.341%) 合计 41 天是亏的，而它们都是**高波动**方向 → 问题不在波动率。要查的是：这三者被选中时的**共性**（r5 冲高但无后续？主题/个股层相对强度倒挂？）——仍须语料支撑 |
| D13 | IC 的**块状 t 检验**（低优先） | 现在 IC 的日度 t 被重叠样本膨胀（20 期快照 × 重叠 5 日前瞻 → 有效独立样本≈20 块），要按快照分期算块状 t。不影响 D11 的操作结论 |
| D15 | **crowding_blacklist 三处错位（待定夺）** | ①缺「无量/被迫卖」维度——他避的核心是「公募+机构+外资同步被套、没量出不来」；②范围错——他避的是**海外链**里那几个大公募持仓个股，9/3 那版却拦了中芯国际/北方华创/兆易创新/中微等**国产链核心**（他 08-11 原话：「反倒是海外链就不怕 因为里面信仰资金太多了」）→ 方向恰好相反；③v2 自 2026-09-03 上线至今**无事件研究**。建议形态：`公募拥挤 ∧ 位置高 ∧ 无量(成交额萎缩/流动性枯竭)` 且限定海外映射链，先做被拦样本事件研究。见 `docs/wolf-overseas-chain-policy.md` |

---

## E. 环境与红线（长期适用）

- **取数分工**：行情走 gzcloud 镜像（批量回填会打限流，需限速+断点续跑）；研报/行业指数走 promax（**服务抖动 502/503/504 常态化，必须重试并显式记录 failed**，不能把 failed 当"当天没有"）。
- **传数据进容器禁止覆盖生产文件（2026-09-13 事故）**：为验收送拥挤度数据时用 `docker cp /tmp/x/. marcus-backend:/app/data/`，把容器里真实的 `crowding_blacklist.json`（9/11 版：AI应用 3 只）覆盖成宿主上的 9/3 旧副本（芯片/半导体等 **17 只**）→ `indicator.py` 会错误硬拦 17 只半导体核心票。**规则**：只往**新建子目录**（如 `/app/data/crowding_pit/`、`/app/data/corpus/`）里传；`/app/data/` 根目录下已存在的文件名在覆盖前必须先备份 + 比对 `ts`；发现覆盖立即按原任务参数重建并留证。
- **重任务内存纪律（2026-09-12 事故）**：容器 cgroup 512MB，把 94 万行行情拉进 pandas 做全市场 pivot/merge 会被 OOM 杀掉，**并连带杀掉 marcus-backend 的 uvicorn 使生产容器重启**（实测 `restarts=1`、dmesg 有 `Killed process ... uvicorn`）。**规则**：重任务一律把聚合下推 SQL（临时成分表 + group by，出库只留 ~170×13 行），pandas 侧只做小宽表；跑之前先看 `free -m` / `docker stats`，一次只跑一个重任务。
- **传文件进容器用 `docker cp`**（宿主顶层目录不映射到 `/app`）。
- **红线**：不做未经语料支撑的自造机制；我们的代理必须标"我们的代理"；重建产物标 `_rebuilt`/`_source`；语料绝不手打转写。
- **PIT 纪律**：回测用 T-1 及更早的研报；gate 结论是盘后产出，供次日使用。
