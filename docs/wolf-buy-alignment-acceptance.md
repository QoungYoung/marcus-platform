# 买入层对齐 · 验收总页（2026-09-15）

> **这一页解决一件事**：买入层该与狼大对齐的、能对齐的，都已实现完毕（**全部默认关 / 只记录**），
> 每一项的证据、样本量、显著性、副作用都在这里；请您按这一页逐项拍板。
> 明细与推导在 `docs/wolf-buy-parameter-ledger.md`（§1–§17）、落差清单在 `docs/wolf-buy-gap-audit.md`、
> 排序验收在 `docs/wolf-pick-rank-v3-eval.md`、选择层验收在 `docs/wolf-pick-selection-eval.md`。
>
> **当前生产状态（未拍板前）**：交易决策与对齐前**完全一致**；只多了 **8 类影子文件**（全部只记录、零行为变化）：
> `rank_v3_*`（v3 会选谁）/ `board_prefilter_shadow_*`（白丢了哪条腿）/ `theme_volfund_shadow_*`（他的板块门会拦谁）/
> `ma144_shadow_*`（144 状态 + 当日其它门状态）/ `ma_line_shadow_*`（均线挂单会挂在哪）/ `line_regime_shadow_*`（白线在上∧缩量）/
> `step_refill_shadow_*`（回补条件：缩量）/ `market_vol_shadow_*`（两市成交额与他的 1WE/1.5WE/2WE 档位），
> 另有一页影子日报 `data/shadow_daily_<date>.json`（`jobs/shadow_daily.py`）。
>
> **2026-09-15 round 24 刷新**：开关从 7 个增至 **10 个**（新增 §1a 的 3 个）；新增 §2.3「xlsx 未覆盖部分的精读结果」
> 与 §5「自检与复现」。

> ⚠️ **结论的边界（2026-09-15 round 14 体检，重要）**：本页与参数总账的所有判定，都建立在
> **派生 md 语料**（`docs/wolf-daily-log-*.md` = 容器 `/app/data/corpus/`）之上；实测该 md 只覆盖
> **原始 xlsx（`狼大回复汇总 20260814-1457&往期.xlsx`）里他本人交易相关发言的 38.1%**（3,773 条 → 1,436 条，总账 §22）。
> 也就是说：**「已对齐 / 自设 / 未知」只在 md 这 38% 范围内成立**；缺的 62% 里若还有买入判据，我们尚未盘过。
> 反向的好消息：§21 的原话真实性校验显示 87/106 条能对回语料、无编造。建议下一步把抽取底座换成 **xlsx 原表**再对一遍账。

---

## 1 十个开关（每项都：一个开关 + 定向单测 + 独立 commit）

### 1.1 早期七项

| # | 开关 | 改什么 | 证据（口径 / 样本 / 显著性） | 建议 |
|---|---|---|---|---|
| 1 | `WOLF_PICK_RANK_V3=1` | 路径 B 选股：域去掉两个实测为负的闸（r20≥0 / rs≥0）+ 执行板块过滤 + LOW 优先(次键 `flat_low_days`) + 阶段(二供) + **只取 1 只** | 同尺子（同主题等权超额、次日收盘入场、5 日、ISO 周块状 t）；**2,005 个 theme-day**：超额 **+0.213%**（现行 **−0.982%**），配对 **+1.120pp，块状 t 2.29**；含费（往返 0.1292%）后 **+0.083% vs −1.112%**；hold=10 含费 **−0.412% vs −2.504%**（配对 +1.798pp，t 2.36）；H1/H2 两段都优于现行 | ✅ **建议开** |
| 2 | `WOLF_PICK_BOARD_PREFILTER=1` | 路径 A **选股时**就剔除无权限板块（创业板/科创板/北交所）→ 用次优票补位（现行是**选了再砍、不补位**） | 整包回放（`jobs/eval_aligned_package.py`）：主链样本 293 个 (日,链)，现行能布的腿 253 **+0.361%** → F2 **+0.567%**（Δ **+0.245pp**）；254 +0.096% → +0.463%（Δ +0.312pp）；全链 3,943 个 (日,链) Δ +0.089pp / +0.080pp（均不显著）。**真正的收益是腿数**：现行选 3 条丢 1~2 条（86.8% 的 (日,链) 都有腿被丢）→ 能布 **1.3(全链)/1.8(主链)** 条，F2 后 **2.8** 条 | ✅ **建议开**（先看影子 2–3 天；注意会真实增加暴露） |
| 3 | `WOLF_DIP_PREVLOW_TOL=0.0` | 254 挂单价 = 前一天最低（他的话），现行是 ×1.005（历史自设） | 每条**已挂腿的期望**（成交率 × 成交收益，未成交=资金空着）：主链 h5 **−0.027% vs −0.032%**；h10 **+0.153% vs +0.064%**；全链 h5 **−0.373% vs −0.293%** → 期望差在 **−0.088 ~ +0.080pp** 间来回、符号随窗口变。机制：tol=0 单价低 0.5%/腿（机制性），但少成交 5~7pp 的腿，那批「浅坑腿」很赚（+2.75%~+4.50%） | ⚪ **可开可不开（等价改写，非改进）** |
| 4 | `WOLF_THEME_VOLFUND_GATE=1` | 主题层加他的「**选板块第一要素**」：量能活跃(10日量能 2/3 天以上) ∧ 资金不连续 5 日净流出 | `jobs/eval_theme_vol_fund.py`（164 天 × 13 主题）：我们的池 ∧ 他的门 **+0.718%**（**n=177**，胜率 65.5%，块状 t 1.73）vs 我们的池 ∧ 不过他的门 **−0.080%**（**n=732**）。副作用：他的门 104/164 天非空 → 开启后约 60 天不布新腿 | ⚠️ **谨慎**（方向对、样本偏小；开=收益质量↑、机会↓） |
| 5 | `P3_TIER_PROFILE=corpus` + `WOLF_TOTAL_CAP_CORPUS=1` | 三仓 50/30/20（底仓/T/现金）+ 总仓位分档 75/50/30/0 | `jobs/eval_align_c3c4.py`：stock 账户 12 个交易日逐日仓位 **最大 48.03%**、均值 18.41%、**超 50% 上限 0 天**、超 30% 仅 2 天 → 历史上不会被拦；约束的是未来「主升敢上 75% / 风险期不超 30%」 | ✅ 建议开（低风险） |
| 6 | `WOLF_ETF_VOL_GATE=1` | ETF 买入要求近 20 日日均振幅 ≥3%（他的话：选半导体就因为波动大） | 真实买过的 **15 只 ETF 里 13 只达标**；不达标的 2 只都是宽基（SZ159632 1.114% / SH510300 2.119%）且**已实现盈亏都是 0.0** → 早就在只会拦 2/15，不损失盈利 | ✅ 建议开（低风险） |
| 7 | `WOLF_MA144_GATE=1` | 144 线资格：MA144 走平/向上才做波段（他的话） | **证据不足以开闸**：斜率口径被拦日**全在一个连续时段**（k=20 只 2026-08-03→09-11、k=40 只 08-18→09-11）= **伪重复**（单段样本 t=−9.69 不可信）；跨 4 段的「收盘 vs MA144」口径放行 +0.116% vs 被拦 −0.615%（t −1.9）/ 254 −0.04% vs −1.36%（t −3.22），且**未与现有大盘门做增量对照** | ⛔ **暂不开**（先影子攒跨时段样本） |

### 1.2 round 16–22 新增的三项（都已在生产跑影子）

| # | 开关 | 改什么 | 证据（口径 / 样本 / 显著性） | 建议 |
|---|---|---|---|---|
| 8 | `WOLF_MA_LINE_ENTRY=1` | 用**均线挂单**（13/34/60/144 线上挂单买）替换现行 `254 前低挂单` | 同批 5,065 条腿：成交率 **25.9%**（254 是 52%）；**每条已挂腿期望 −0.151% @h5 / −0.181% @h10**，优于 254 的 −0.293% / −0.210%，但**不如 253** 的 −0.045% / +0.398% → **同类替换**（让 254 那档少亏一点）。**语料多年份一致**（2016-02-19「压回13天线附近，击不击穿都是买点」/2016-07-07 挂单在 13 天线/2021-01-28「34线也接回」/2021-02-01「13线上一点挂单补的」/2025-07-15「找自己票挂线上」） | ⚪ **可开可不开**（先看影子 2–3 周） |
| 9 | `WOLF_LINE_REGIME_GATE=1` | **白线在上 ∧ 缩量 → 当日没有买点**（不加仓） | 253 腿在他说"没买点"的日子 **−1.556%**（n=608，t **−3.60**，周块状 t −1.35）vs 放行 **+0.157%**；254 −1.226% vs −0.450%。**19 个连续段**（非单段伪重复）但 **ISO 周配对 16/24 周为负、周级 t −1.19（不显著）** | ⚠️ **先影子**（与 P1 同级别证据；只拦新买腿、不动卖出） |
| 10 | `WOLF_STEP_REFILL=1` | **分步回补/加仓的附加条件**：缩量（时间窗默认**不叠加**，见下） | 语料：2025-04-15 条件6 + 2025-06-10「下去了缩量再买回来」+ 2025-06-27「低开再买回来」+ 2026-06-18「分批进场」。**未单独做收益验收**（离线缺分钟级挂单数据）→ 属"按他的原话实现、先影子看机会成本" | ⚠️ **先影子**｜⚠️ 时间窗（14:00–14:30）**默认不叠加**，因为 A5 窗口已被我们自己的证据否掉（总账 §23.3：窗内更差约 2pp）且用户已关 `WOLF_TRADE_WINDOW` |

---

## 2 已判定的边界（**勿当缺口重做**）

> **2026-09-15 round 18 自检**：`jobs/verify_acceptance_claims.py` 已把"文档漂移"自动化 ——
> 抽出本页与总账里提到的全部开关名，逐个到代码里找 `os.getenv(NAME, default)`，并检查每个 `*_shadow_*` 产物是否真有模块在写。
> 当前结果：**文档提到的开关 63 个 → 代码里找不到的 0 个**；**9 个影子产物全部有模块在写**；显式默认值比对 5 个 → **不一致 0 个**。
> 另外：这份文档里写 `NAME=1` 表示**置 1 才生效**，不代表默认值是 1（默认值一律看 §1 的「默认关」描述）。

| 项 | 结论 | 依据 |
|---|---|---|
| **B3 龙头风向标**「死了就不做」 | **不搬到路径 A** | 720 chain-day：破位日布腿 5 日 **+1.556%** vs 未破位 **+1.102%**（破位反而略好）；10 日 +1.63% vs +2.02%；阈值 −0.5~−3% 扫描结论不变、**全部不显著**（总账 §15） |
| **C2 个股级资金确认** | **不能当"叠加闸"**（池内反向），只可当**选股因子** | 离线：跌∧净流入 +0.045%(5d)/+0.173%(20d)，但**池内反向**（`docs/wolf-buy-moneyflow-check.md`） |
| **D15 避开海外链/公募重仓** | **代码已真删**（用户指令） | 事件研究证"整体拦反"：被拦组后 5 日 +3.689% vs +0.250% |
| **#7 集中持仓**「一天最多做一次T」 | **对我们的现状不适用** | 语料：集中持仓 = 把一个方向合并到 **1 只**；反义是"平铺"，而我们就是多链多腿平铺（总账 §5 #7） |
| **#2 换手 7%** | **不实现** | 口径=庄股识别（涨停日+次日换手均 <7%），但他对庄股两种态度（2022-06-15「我玩点庄股」vs 2016-05-05「短线抢题材的就不要做这种」）→ 无确定动作 |
| **`flat_low_days`（v3 次键）** | **语义偏离，不得宣称"与语料一致"** | 他的话是**方向级 × 2 天**，我们落成**个股级 × 20 日**（总账 §6 第 3 条）；离线它有正贡献，但口径不同 |
| **「0.382 回撤」买点** | **不落地** | 他的公式**只有系数、没写区间来源**（"前两根红K"那句是**持有**口径）；按字面口径实测是**最差一档**（每条已挂腿期望 −0.373% @h5），换 20/60 日区间才变好但那区间是我们自设 → 按红线不拍值（总账 §18） |
| **容器路径审计（round 28–29）** | **已闭环，勿当买入层缺口重做** | 25 处「按仓库布局上溯」的路径在生产容器里解析错（`/data`、`/core`、`/.env` 都不存在）；逐条定性后 **只有 1 处影响实盘交易链**（`position_tier_monitor` 档位状态，§36 已修），其余是兜底链第一分支就命中 / 死代码 / parquet 数据湖早就不存在 / 回测沙箱写临时层（总账 §37）；顺带修掉根因级兜底 `config._detect_workspace()`（生产行为零变化） |
| **「全部可调参数」的扫描面（round 32 补齐）** | **四类载体都扫过了；但"生效值"另有一层** | ① env 键 + ② 模块级标量大写常量（§19，304 条未收录，其中策略阈值 92 条已由 §40 判定/备案）；③ **函数字面量**（§41，128 条 → 真正影响买入的 3 条，2 条工程/护栏、1 条已覆盖）；④ **字典型默认值的内部键**（§42：`t_build.BUILD_PARAMS_DEFAULT` 50 键、`trend_confirm.TREND_CFG` 10、`position_class.CONFIG` 11 等 10 个容器，此前**完全未覆盖**）。<br>另：参数**优先级链 = DB 配置表 → 配置文件 → 代码默认**，实测 **2 处真实覆盖**（`profit_take.enabled` DB=true/代码=false；`no_rebuild_symbols` DB 2 只/代码 1 只）→ **任何"与语料一致"的断言必须核 DB 生效值**（复现：`jobs/audit_config_overrides.py`） |

### 2.1 待拍板的**新规则**（已验收、未实现）

| 规则 | 验收结论 | 建议 |
|---|---|---|
| **均线挂单买**（跌到 13/34/60/144 线上挂单） | 口径明确（线集合来自原话）。成交率 **25.9%**（现行 254 是 52%）；**每条已挂腿期望 −0.151% @h5 / −0.181% @h10**，优于现行 `254` 的 −0.293% / −0.210%，但**不如 `253` 次日收盘**（−0.045% / +0.398%）→ 属**同类替换**（更深的挂单、成交更少、让 254 那档少亏一点），不是新增收益来源。**2026-09-15 round 15 补：语料依据从「少量」升级为「多年份一致」**——xlsx 缺口扫描（总账 §23）找到 2016-02-19「任何时间**压回到13天线附近，击不击穿都是买点**」、2016-07-07「挂在大富科技**13天线29.65**这里」、2016-07-08「回踩13天线附近都可以做一把波段建仓」、2025-07-15「找自己票**挂线上** 等万一突然杀到就有机会进去」 | 先**影子**记录（「如果挂在最近均线上会怎样」），再拍板是否替换 254（总账 §18/§23） |

> ⚠️ 这两条的逐腿配对差**全部是机制性的**（同一条腿只有入场价不同 → 更低挂单价必然量出更高收益）：实测配对差与机制性入场价差几乎相等（如 MA −0.659 vs −0.688）。判据只看**每条已挂腿的期望**。

### 2.2 round 16 新增：从 xlsx 原表（md 未覆盖的 62%）定向抽取出的 3 条规则

| 规则 | 语料（xlsx 全表，带日期） | 验收/建议 |
|---|---|---|
| **13/34 线买点** | 2016-02-19「任何时间**压回到13天线附近，击不击穿都是买点**」、2016-07-07「**挂在大富科技13天线29.65**」、2021-01-25「之前在**13线补的**」、2021-01-28「**34线也接回**汇川」、2021-02-01「**13线上一点挂单补的**通威…**挂线有没有用**」、2025-07-15「**找自己票挂线上**」（**2016/2021/2025 三个年份一致**） | 同 2.1 的均线挂单：**建议先影子**（`ma_line_shadow_*` 已在跑），再拍板是否替换 254 |
| **白线在上 / 缩量 → 不加仓、无买点、仓位≤50–60%** | 2025-07-15「**缩量 白线在上**的局**千万别没事加仓**…**60%仓位内**」、2026-03-03「**缩量**…**白线在上**…尽量保持 **70-75%仓位**」、2026-03-25「**缩量 白线在上** 我会**减回50%内**」、2026-04-09「**今天白线在上 肯定没有买点**…**减回50%-55%仓位**」（**多时点一致**） | ✅ **已实现为第 9 个开关 `WOLF_LINE_REGIME_GATE`（默认关）+ 影子**（round 17：模块 `apps/main_line/wolf_line_regime.py`、接线臂、单测 7 项、生产已验证）。离线（总账 §26）：他说"没买点"的日子 253 **−1.556%**（n=608，t −3.60，周块状 t −1.35）vs 放行 **+0.157%**；但 **ISO 周配对 16/24 周为负、周级 t −1.19（不显著）** → 先影子攒样本 |
| **BOLL 中轨（板块回中轨加 / 跌破跑）** | 2025-06-16「**板块突破中轨**就重新进去」、2025-07-09「**板块指数回BOLL中轨再加**」、2025-07-10「**回中轨企稳缩量再买**」、2025-07-14「**到日K BOLL中轨就补一点**…**到上轨就卖一点**」、2025-05-07「**跌破中轨就跑路**」、2026-04-23「**大盘BOLL中轨我减仓了**」 | ⛔ **买入侧离线验收未通过**（总账 §25）：事件方向对（跌破中轨日 253 **−2.37%**，n=169，t −3.07，跨 **8 段**）但**逐段极不稳定（仅 9/15 段为负）** → **不做买入资格线**。⚠️ **卖出侧早已实现**（`wolf_boll_levels.py`，A10，语料 2025-04-15/2026-04-29/2026-01-13/2025-05-13，`WOLF_BOLL_SELL` 默认 1 在跑）——两者不冲突，见总账 §27.2 |
| 日内 −2% 触发买入 | 仅 2016-03-02「**只要当天低过-2%我就进去**」一条（另 2 条是讲散户行为） | ⛔ **不落地**（单条、十年前、风格年代差异大） |

---

### 2.3 round 19–23 新增：xlsx 未覆盖部分挖出的候选（**待拍板，未实现**）

| 候选 | 语料（xlsx 全表，带日期） | 现状 |
|---|---|---|
| **方向级事件回避** | 2025-04-03「买的要**避开关税方向**，这东西绝对没完，还有反制一波」 | 我们只有**个股级**负事件（`wolf_early_exit.negative_event`），无方向级 → **可离线验收**（事件窗口内该方向的腿收益），**待拍板** |
| **业绩门槛** | 2025-07-04「现在炒的都是**业绩方向**，CPO PCB 游戏这些都是**业绩比较确认**的」＋同日「AI大数据 机器人这些目前**没有业绩支持就算了**」 | 我们只有「纯量价 + 排除暴雷」，catalyst 判据 2026-09-13 已停用 → **没有正向业绩准入**；需先确认可算代理与数据可得性，**待拍板** |
| **间隔式加仓（跌 5% 补一次）** | 2016-05-06「跌下来 **5 个点**补一次，补到满仓」 | 年代久远（2016，当时还有 120% 总仓位）→ **备查**，不实现 |
| **集中持仓形态（板块内市值前 5）** | 2025-12-06「判断板块核心标…1 他是**板块内总市值前 5** 的股」 | 已验收（总账 §31.2）：**不劣但腿数减半** → 不动生产，备查 |
| **抗跌影子（低位 ∧ 调整段少跌，基准=同期大盘）** | 2025-10-23「防御类品种，**就是那种跌了也就最多跌 2 个点的**」＋2021-01-27「**什么叫防御型，就是大盘跌的时候他少跌一点**」＋2026-04-07「**低位横盘多时的就是好 超跌都没有低位走平多时的好**」 | 见总账 §39：**「≤2 个点」是特征描述、不是阈值**，且"调整段"未定义 → **不落地为闸门**；若要量化，建议做成**影子**（记录：调整段内个股跌幅 vs 同期指数跌幅），窗口须拍板 |

### 2.4 round 31 新增候选：执行/成交侧（来自总账 §40，**待拍板，未实现**）

| 候选 | 语料（跨期） | 现状 | 说明 |
|---|---|---|---|
| **(a) 主题内腿数 4 → 2–3** | 2016-01-26「**同板块尽量找 2-3 支做**，龙头找一支…」＋2025-07-24「**光刻机持有 2 只** 做T **光伏 2 只**做T」 | `WOLF_PICK_MAX_LEGS=4` | 跨 9 年两期一致指向 2–3 只；但 §12 已验收的「腿数 1.3–1.8 → 2.8/(日,链)」是我们主动做多腿数的结论 → 两者要一起看 |
| **(b) 可T价差门槛 0.5% → ~1 个点** | 2016-01-21「**1 个点感觉做起来不值**」＋2016-01-19「不够 1 块钱，是我贪心了」＋2021-01-29「汇川的T**没到一个点**」 | `MIN_T_SPREAD=0.5`（%）、`MIN_T_SPREAD_FILTER=0.002` | 三期一致；**我们比他的口径松** → 建议先离线验收（历史 T 记录），再谈开关 |
| **(c) 加仓档距形态（价格档）** | 2016-02-18「**每跌 5% 补一次，分三批进场**」＋2016-05-06「跌下来 5 个点补一次」 | C6 分步回补 = 缩量 + 2 日窗 | 他的"档"是**价格**、我们的是**量能+时间**；落地是给 `wolf_step_refill` 加一层价格档条件，不是改现有条件 |
| **(d) 回踩门槛偏松（1% vs 他的 3–5%）** | 2016-08-16「只要**回踩 3 个点左右就进**…大踩大补，小踩小补」＋2016-04-14「**5 个点以内**的拉回算什么」＋2021-01-22「回调幅度不超过 **3 个点** 我做T的空间都没有」 | `t_build.drawdown_min_pct=1.0`（距当日高点回撤 ≥1% 才算回踩；仅做T账户**自动建仓**的入场前置） | 方向一致（回踩才买）但**我们口径更松**；三条都是 2016/2021 年代、且他说的是**波段进场**，层级不完全相同 → **先离线验收再谈**，不直接拍值（总账 §43.2） |


## 3 仍在等您定的（**1 条参数口径 + 1 个批次决定 + 1 条验收口径**）

| # | 内容 | 需要您定什么 |
|---|---|---|
| ~~1~~ | ~~「低位最多跌 2 个点」的相对基准~~ | **已结案（round 30，总账 §39）**：全表复核后确认那句是**对"防御类品种/持仓特征"的描述**（"跌了也就最多跌 2 个点"），不是选股阈值；他明确给过的比较基准只有「大盘跌的时候他少跌一点」→ **不需要您定数值**，也不落地为闸门（若要量化，改走 §2.3 的"抗跌影子"候选） |
| 2 | 影子里「资金门」读取失败时的 **fail-open**（放行）| 可否接受？（现状：取不到资金数据就放行，不因读数失败停买） |

| 3 | **买入/执行侧阈值** —— round 31 已判定 20 条（总账 §40）| **收窄后只剩一个批次决定**：92 条里真正影响成交/风险敞口的 20 条已判完 —— ✅ 有语料支撑 4 条（量能 2WE/3WE 直接对齐、回补不追高、回补窗口、高抛低吸幅度量级一致）；⚪ 其余 16 条按**系统护栏备案**（成本/风控/池比例/筛选/数据窗口，其中"每日亏损与冷却"全表 **0 命中**）；顺手订正一条假阳性（`_TOTAL_MIN=240` 是**交易时段分钟数**不是阈值）。→ 剩下 ~72 条**建议直接按护栏备案，不再逐条找语料**（复现：`jobs/scan_exec_thresholds.py`） |
| 4 | **`rank_v3` 影子的对照口径**（总账 §38，round 30 订正后）| 它的配对是「v3 会选谁 vs 现行 leader **实际**选谁」，而 leader 选票**只在路径 B 产生** → 走路径 A 的日子没有对照、无法记录（09-15 正是 `legs_by_source={'pathA': 3}`）。要让 v3 影子天天有记录，需先定路径 A 的日子拿什么当对照：**同日同主题下不排序的池内等权**？还是**路径 A 实际挂的腿**？<br>（round 29 曾提的「影子积累节奏 A/B」**已撤回**：三段状态影子其实每次 arm run 都写，速率 1/交易日，不需要改代码） |

> 另：**#9 删票 TTL = 13 交易日**已用 xlsx 全表检索确认「他的话里 0 命中」（总账 §31），属**确认自设**；
> 自设参数 11 条、代理参数 14 条已逐条列在 `docs/wolf-buy-parameter-ledger.md` §3/§4（代码里也逐条标了 `⛔自设`）。

> **「全部可调参数」的完整画面（2026-09-15 round 13 覆盖率体检）**：总账 §1–§8 是**人工清单（44 条）**；AST 扫 293 个模块后，另有 **304 个未入账旋钮**（其中**策略阈值 92 个**，多为执行侧护栏）——见总账 §19；其中方向/买点相关的 **23 个模块其实早已按语料实现**（docstring 带原话），已登记进 §20。复现：`.venv/bin/python jobs/build_param_ledger.py --coverage`。→ 待您定：这 92 个执行侧阈值**要不要逐条给语料判定**（建议只做影响成交/风险敞口的少数几条：`MAX_DAILY_BUY_LEGS`、`STOP_LOSS_PCT`、`MIN_T_SPREAD_FILTER`、`COOLDOWN_AFTER_LOSS_MIN`、`WOLF_REFILL_*`）。

---

## 4 复现命令（全部离线、可重跑）

```bash
# ① v3/v2 排序验收（含费率、配对、H1/H2）
.venv/bin/python jobs/eval_pick_rank_v3.py --domain cand_low --components pos --tiebreak flat \
  --stage qtile --diergong --qtile-hi 0.5 --qtile-lo 0.5 --limit 1 --board-filter \
  --fee-pct 0.1292 --hold 5
# ② 整包回放（F2 + C1 vs 现行，路径 A）
.venv/bin/python jobs/eval_aligned_package.py --hold 5
# ③ 他的板块门
.venv/bin/python jobs/eval_theme_vol_fund.py
# ④ C3 仓位档 / C4 ETF 波动（历史追溯）
.venv/bin/python jobs/eval_align_c3c4.py
# ⑤ B3 风向标（否证）
.venv/bin/python jobs/eval_wind_path_a.py --hold 5 --start 20250210
# ⑥ 144 线资格（含分段检查）
.venv/bin/python jobs/eval_ma144_regime.py --index 000001.SH --hold 5
# ⑦ 影子期对照（灰度期累积后跑；读 data/ 下影子文件）
.venv/bin/python jobs/eval_shadow_v3.py --shadow-dir data
# ⑧ 一页影子日报（生产容器内）
docker exec marcus-worker python -u /app/jobs/shadow_daily.py --data /app/data
```

---

## 5 自检与复现

**改完文档/开关后先跑自检**（round 18 新增）：

```bash
.venv/bin/python jobs/verify_acceptance_claims.py     # 文档里的开关名 ↔ 代码、影子产物是否真有模块在写、默认值是否一致
.venv/bin/python jobs/build_param_ledger.py --coverage # 代码旋钮 ↔ 参数总账（漂移/漏记）
```

当前结果：文档提到 63 个开关 → **代码里找不到 0 个**；9 个影子产物 → **全部有模块在写**；显式默认值比对 → **不一致 0 个**。

## 6 读数时请一并注意（本项目反复踩过的口径陷阱）

1. **基准必须是"生产实际会发生的事"**：整包回放第一版把"含无权限票的**理论收益**"当现行基准，
   得出"开 F2 反而差 0.79pp"的**反号结论**。生产是**砍掉不补位（资金空着=0）**，修正后 Δ 才是 +0.245pp。
2. **挂单价类参数必须看"每条已挂腿的期望"**：只看"成交后平均收益"会把机制性价差（tol=0 便宜 0.5%/腿）
   误当 alpha；把成交率算进去后，C1 是**等价改写**。
3. **择时类规则先按时间分段**：144 单段样本 t=−9.69 是伪重复；分段后（≥3~4 段且方向一致）才看 t。
4. **断言必须基于「生效值」**：参数优先级是 **DB 配置表 → 配置文件 → 代码默认**（§42）。实测两处真实覆盖：`profit_take.enabled`（DB true / 代码默认 false）、`no_rebuild_symbols`（DB 2 只 / 代码 1 只）→ 只看代码默认值可能对着**没生效**的数字下结论；自查：`jobs/audit_config_overrides.py --only-changed`。
