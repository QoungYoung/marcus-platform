# 计划：退出/兑现层对齐（G3 尺子 → 兑现口径）

> **给执行 agent 的完整交接**：目标是让系统的**卖出/兑现行为**与狼大一致。本文自包含：背景、现状、狼大原语料、开发计划、验收标准、环境红线、以及**明确不要做**的事。
> 写于 2026-09-13。作者上一轮已完成"方向层（主线判定）"的对齐，本文是**下一步**。
> 术语：**腿** = 一次买卖动作；**t 账户** = 做 T 专用模拟账户；**PIT** = 只用当日及以前的信息。

---

## 1. 背景

**项目**：`marcus-platform`（A 股交易辅助系统，Python + FastAPI + APScheduler + PostgreSQL）。
**目标**：复刻一位交易者（下称"狼大"）的做法——不是复刻收益数字，而是复刻他的**判据与动作结构**，并用他本人的语料原话作为设计依据。

**已完成的上一阶段（方向层/主线判定）**：用"**池 = 主题近 5 日成交额占比 topK ∩ 近 5 日相对强度>0 → 池内 r5 top1**"取代了旧的 `mainline_gate`。
- 与他"股票主线层"口径对齐：**recall 75%**、他方向落我们 top1 **54%**（随机 7.7%）、top3 **85%**（随机 23%）；
- 收益：2026 后 5 日超额 **+1.03%（t=3.37）**、2025 **+0.34%（t=3.01）**；相对"全主题对照"的两年超额一致（+0.35pp / +0.30pp）；
- 生产开关：`WOLF_MAINLINE_SELECT=1`、`WOLF_MS_POOL_K=3`、`WOLF_MS_USE_GATE=0`；每日 18:55 产出，次日 08:00/08:25 消费。
- 详见 `docs/wolf-structural-pool.md`、`docs/wolf-behavior-blueprint.md` §16。

**为什么下一步是"退出/兑现层"**（三条证据，全部为既有实测）：
1. **买点被证明是"中性"的**：254/253 触发条件的后 5 日胜率 **47% ≈ 同池同日随机买入基线 49%**（n=428 vs 23,512）——**买点不是胜率来源**；
2. **兑现方式才是**：全部样本 T+5 收盘胜率 47% → 加 **+3% 小止盈 → 53%**；加 **−3% 止损 → 39%**（+5%/−3% 组合 36%）；
3. **我们现在的尺子是错的**：评估做 T 腿时用"买入后持有 T+5"，量出 **47%**；而生产**已实现卖出腿**（`paper_trades` 中 `direction='卖出' and profit<>0`）是 **63.6%、+2,828.5**。
   → **在错的尺子上下任何结论都无效**，所以本计划的**第一阶段就是换尺子**。

---

## 2. 现状（开工前必读）

### 2.1 生产链路（2026-09-13 现状）

```
18:45  daily_inputs_chain（apps/main_line/daily_inputs_chain.py）
         concept_long → build_etf_flow → build_inst_flow → trend_confirm(结构, 供方向层浪型) → inject_state
18:55  wolf_mainline_select（jobs/wolf_mainline_select.py）→ 池判定 + 直写 main_line_state.json
次日 08:25  daily_decision（backend/app/services/daily_decision.py）→ L1 用方向层
次日 09:20  rotation_switch_arm（jobs/rotation_switch_arm.py）→ 轮动换仓 arming（读池）
盘中       t_monitor（30s）→ t_gateway.gateway_execute 下单；TMonitor 执行卖出腿
```

### 2.2 四条腿的现状（`docs/wolf-behavior-blueprint.md` §6）

| 腿型 | 现状 | 备注 |
|---|---|---|
| **做 T 低吸** | ✅ 有腿；**回测口径错配**（用"持 T+5"量出 47%，生产实际 63.6%） | **本计划主战场** |
| **低位埋伏** | ⚠️ 有腿、**在亏**（stock 账户 **27.3%（3/11）/ −2,538.6**） | 另一个紧急项（G4），本计划**不含**，可另起 |
| **确认加仓** | ⚠️ 有腿、**未验证** | — |
| **逃顶/减仓** | ✅ 已落（A10，带前提） | — |

### 2.3 生产开关现状（实测 `.env`）

```
WOLF_BOLL_MID_EXIT=1     WOLF_CUSHION_CAP=1      WOLF_VOLUME_GATE=1
WOLF_WEEKEND_HEDGE=1     WOLF_DAILY_DECISION=1   WOLF_DAILY_ARCHIVE=1
WOLF_DECISION_GATE=1     WOLF_MAINLINE_SELECT=1  WOLF_MAINLINE_GATE_STEP=0
```
⚠️ **「小赚兑现」(P0-3) 在生产 `.env` 中不存在该变量 → 走代码默认 False（关闭）**。

### 2.4 可复用的既有设施

| 设施 | 位置 | 说明 |
|---|---|---|
| 纪律规则（可配置） | `backend/app/services/wolf_discipline.py` | 已有 `weekend_de_risk()`（周末降仓）、`board_half()`（板上减半）、`profit_take()`（小赚兑现）；**配置走 `_cfg()`/`save_cfg()`，可从 API 改** |
| 卖出腿实现 | `backend/app/services/t_monitor.py`、`t_db.py`、`wolf_boll_levels.py` | `board_half` / `high_sell` / `custom_vwap_sell`（破黄线）/ `custom_support_sell`（破支撑）/ `wolf_defensive_t_reduce` |
| 触发与订单 | `t_triggers`（触发记录）、`t_gateway.gateway_execute`（下单白名单 + `_decision_gate()`） | 决策对象准入在 `gateway_execute` 内 |
| 决策对象（L1–L6） | `backend/app/services/daily_decision.py` + `daily_artifacts.artifact_key='decision'` | L1 方向 / L2 档位 / L3 仓位 / L4 票 / L5 准入 / L6 退出 |

### 2.5 数据底座

| 数据 | 位置 | 覆盖 |
|---|---|---|
| 行情（回测用） | PG `mkt_bars_daily` | **455 个交易日 / 247 万行 / 2024-11-01 → 2026-09-11**（字段：OHLC、pre_close、pct_chg、vol、amount、total_mv、turnover_rate） |
| **成交流水（关键）** | PG **`paper_trades`** | 含 `direction`、`profit` —— 已实现卖出腿胜率 **63.6%** 的口径是 `direction='卖出' and profit<>0`。另有 `backtest_trades`（回测用） |
| 每日产物 | PG `daily_artifacts` | `mainline_select`、`wave_state`、`decision`；`mainline_gate`/`heat_v2` 为**历史**（机制已删，见 §7） |
| 本地镜像 | `data/mkt_bars_local.db`（SQLite）、`data/stock_pool_local.db` | 本地分析用，避免生产容器 OOM |
| 语料 | `docs/wolf-daily-log-*.md`（NGA 30 天带时刻 / XLS 各期逐日）、`/app/data/wolf_*evidence*.json` | 设计依据来源 |

### 2.6 既有验证脚本（可直接复用/改写）

| 脚本 | 用途 |
|---|---|
| `jobs/eval_d12_vol.py` | **退出规则变体**（+3%/+5% 止盈、−3% 止损）在 2025/2026 上的胜率对比 —— **本计划的主要起点** |
| `jobs/eval_structural_pool_local.py` | 本地（SQLite+pandas）跑池判定，含 H1/H2 分段 |
| `jobs/eval_gate_vs_pool.py`、`jobs/eval_pool_alignment.py`、`jobs/eval_pool_k.py` | 对齐度/命中率/池宽扫描（口径可借鉴） |
| `jobs/backfill_market_bars_local.py` | 本地回填（promax → SQLite，多 worker 分块） |

---

## 3. 狼大原语料（设计依据，带日期）

> 规则：**每条判据都要能指回下面的原话**；找不到原话的机制不要做（用户红线）。

### 3.1 仓位两分法（底仓 vs 做 T 仓）——所有兑现规则的前提

- 「所以我这里**仓位不会低于 65% 收盘，日内做 T 仓位 20%**」（2026-09-03）
- 「所以我一直说 **做 T 套利的仓位是做 T 套利的 底仓是底仓**。」（2026-08-05 10:38）
- 「**分不清做 T 仓位和底仓的区别就是『贪』**。」（2026-08-05 13:30）

### 3.2 做 T 的目标幅度与出场（本计划核心）

- 「**2 点到了 力度不够**…今天**目标 3%-4% 实际 2% 结束今天半导体做 T 操作**。」（2026-09-02）→ 达到目标幅度即兑现；**力度不够也收工**
- 「做大科技方向**最低套利 3 个点、最多 10 个点**，红利做不到这个数字。」（2026-08-05 09:58）
- 「**这半小时内日均线那条黄线绝对不能破，突发跌破直接走**；没跌破就找这半小时的高点。」（2026-08-05 10:48）→ **破黄线离场 + 不破则找分时高点出**
- 「**做 T 的前提是自己判断能执行：做错会撤、涨上去舍得卖**。」（2026-08-04）
- 「光和半导体都只是 B 反预期，**不做 T 死拿是回不了本的**。」（2026-08-06 15:33）
- 「**我今天没抄底 没有资格 T。**」（2026-08-25）→ **做 T 必须先有低吸仓位**

### 3.3 高位做 T / 低位不动（分类型分工）

- 「**高位方向是要 T 的，低位方向（他下半年最看好的方向）是不动的**。」（2026-08-05 10:03）
- 2026-09-04 15:07 长帖（分类型总纲）：**高位**＝**卖强留弱、拉升后都走**；**低位**＝找**辨识度最高的老龙头**埋伏、**留强丢弱**；超短＝按链条走、要快。

### 3.4 退出判据（"什么时候算走完")

- 「（为什么不做卫星）因为**十字星后第二天没有突破，上面卖单一点没撤 还加大了**…然后**板块没有带动效应**。」（2026-02-12）→ 三个条件：不突破 + 卖单加大 + 无带动
- 「**诱空一般最多三次。之后很容易短期买不回来。**」（2026-09-07）→ 影响卖出节奏（不要被诱空骗出）
- 「大光拔估值 3 天后 → 二线光 2-3 天 → 光纤 3 天 → 光上游 3 天 → 光边缘 2 天。然后结束。继续等下一波」（2026-05-29）→ 链条节奏（兑现时点的自上而下顺序）

### 3.5 周末/事件前避险（已上线，需按新尺子复验）

- 「**2 点半 如果还是缩量 还是不拉升 我会先把这两天 T 进去的仓位出来一半**…**65% 仓位过周末**。」（2026-08-21）

### 3.6 尾段规则（**当前系统没有"尾段"状态**）

- 「就等吃最后一段 不管吃不吃得到我都不会加仓 **顶多做 T 然后最后一段撤掉**」（2026-09-04）

### 3.7 止损口径（与我们的六层止损不同）

- 「**顶多就是指数破位后的止损**…哪怕止损就是我的技术策略抗不住这个波动而已。」（2026-08-25）
  → **他的止损以指数破位为准，个股不止损**；我们的六层止损里"个股级止损"需按此复核（缺口 G8）。

### 3.8 其他相关硬参数

- 关键整数位：「在指数**第一次接近 4000** 的时候我第一次把**底仓**给减了」（2026-08-26）／「4000 收费站」（2026-09-01）
- 黄金（事件驱动兑现）：「沃十讲话 解读偏鹰 黄金跌 — 数据出来，不及预期 黄金涨…」（2026-08-31）／「黄金股要**舍得卖**」（2026-09-03）

---

## 4. 缺口与要实现的规则（每条挂语料）

| # | 规则 | 语料 | 我们有吗 |
|---|---|---|---|
| R1 | 做 T 腿**目标幅度 +3%~+4%**，达到即兑现（减半/锁定）；**力度不够也要收工** | §3.2（09-02、08-05） | ⚠️ 有兑现腿，**无目标幅度** |
| R2 | **破分时黄线立即离场**；不破则等分时高点出 | §3.2（08-05 10:48） | ⚠️ 有 `custom_vwap_sell`，需确认与"半小时均线"口径一致 |
| R3 | **做 T 前置**：无低吸仓位不做 T | §3.2（08-25） | ⚠️ 部分（254 低吸腿） |
| R4 | **高位方向要 T、低位方向不动**（分类型分工） | §3.3 | ⚠️ 只有统一"去弱留强" |
| R5 | 高位**卖强留弱、拉升后都走** | §3.3（09-04 长帖） | ⚠️ 口径不同 |
| R6 | 周末避险：**14:30 仍缩量不拉升 → 出一半、65% 过周末** | §3.5（08-21） | ✅ 已上线（G9），**待按新尺子复验** |
| R7 | **尾段状态**：不加仓、顶多做 T、然后撤 | §3.6（09-04） | ⛔ 无"尾段"状态 |
| R8 | **止损只看指数破位**（个股不止损） | §3.7（08-25） | ⚠️ 六层止损，需按 G8 复核 |
| R9 | 退出判据三条件（不突破+卖单加大+无带动） | §3.4（02-12） | ⛔ 无 |
| R10 | 诱空最多三次（不要被骗出） | §3.4（09-07） | ⛔ 无 |
| R11 | 底仓/做 T 仓**分账**（≥65% / 20%） | §3.1 | ⚠️ 有 tier 档位，无两分法 |

---

## 5. 开发计划

### 阶段 0（**必做第一步**）：换尺子 —— 分类型度量 + 生产实际离场口径

**为什么**：现有评估用"持 T+5"量做 T 腿（47%），与生产实际（63.6%）口径不一致 → 后续任何结论都不可信（checklist B7 原文）。

**交付物**：
1. `jobs/eval_leg_metrics.py`（新）：从 **`paper_trades`**（`direction='卖出' and profit<>0`）与买入侧成交流水重建**每条腿**（做 T / 低位埋伏 / 确认加仓 / 逃顶），逐腿计算：
   - 做 T 腿：**成功率 + 盈亏比 + 持有分钟/天数分布 + 兑现幅度分布**；
   - 低位埋伏腿：**区间收益 + 最大回撤 + 持有天数**；
   - 确认加仓腿：**延续率 + 加仓点超额**；
   - 逃顶腿：**避开的跌幅**（对照未减仓反事实）。
   - 每组都要有**同池同日基线对照**，并给绝对与超额两个口径。
2. 一页**度量规范**（写进 `docs/`）：腿的定义（如何从流水配对出腿）、口径、样本期、最小样本量（n<100 只作探索）、以及**重叠样本的处理**（同一时点的多腿高度相关 → 关键结论要做**块状/分批 t 检验**，不要只报日度 t）。
3. 用新尺子**复算**尚未验证的机制：G9 周末避险（`WOLF_WEEKEND_HEDGE=1`）、`WOLF_BOLL_MID_EXIT=1`、`WOLF_CUSHION_CAP=1`。

**验收**：①同一批腿用"持 T+5"与新口径分别量 → 复现"47% vs 63.6%"的口径差（证明尺子修对了）；②给出四类腿的基线指标表；③把三个既有开关的增量收益/胜率影响量化出来。

### 阶段 1：兑现口径对齐（R1–R5）

**做法**：**先离线验收、后接线**。
1. 在 `jobs/eval_d12_vol.py` 基础上扩展为 `jobs/eval_exit_rules.py`：在同一批腿（阶段 0 重建）上做**规则变体对照**：
   - 基线：现状（今日生产规则）
   - V1：+**3% 目标幅度**兑现（R1）
   - V2：+**破分时黄线**离场（R2）
   - V3：V1+V2
   - V4：**做 T 前置**过滤（无低吸仓不做 T，R3）
   - V5：高位/低位分类型（R4/R5）
   输出：每变体的胜率、盈亏比、期望、最大回撤、以及**分档**（高/低位）表现。
2. **只有**当某变体在**两年样本**（2025/2026）与**分段（H1/H2）**都优于基线，才进入接线。
3. 接线方式：改动集中在 `wolf_discipline.py`（`profit_take()`/`board_half()`）与 `t_monitor` 的卖出腿参数；**一律带开关**（默认关），并在 `daily_decision` 的 L6 退出层登记。

**验收**：做 T 腿胜率**不低于**现状 63.6%（不劣化），且"胜率/盈亏比/最大回撤"至少一项显著改善；给出前后对照表与样本量。

### 阶段 2：退出判据与尾段（R7、R9、R10）—— 视阶段 1 结果决定是否做

- R9（不突破+卖单加大+无带动）与 R10（诱空三次）需要**分时/盘口数据**；先确认数据可得性（`t_monitor` 快照里有什么），**数据不足就不做**（不要用日线近似硬凑）。
- R7（尾段状态）需要定义"尾段"的**可识别条件**；若只能用事后信息识别 → **不做**（前视）。

### 阶段 3：止损口径复核（R8 / 缺口 G8）

- 对照实验：现状六层止损 vs「**只在指数破位止损**」（个股不止损）；
- 指标：**最大回撤、亏损分布尾部、被止损后 N 日的反事实收益**（判断"割在最低点"是否成立）。
- 若证据支持他的口径 → 把个股级止损**降级为提示**（不是直接删，需与用户确认）。

---

## 6. 环境与红线（**必读**）

1. **重任务一律本地跑**：`marcus-backend` 容器 cgroup 只有 **512MB**，在里面跑大 pandas/多进程会把 uvicorn 一起 OOM 掉、**导致生产容器重启**（本会话发生过两次）。
   - 本地已有：全套依赖（requests/psycopg2/sqlalchemy/pandas/numpy/paramiko）、promax 直连；
   - 需要读生产 PG 时用**进程内 SSH 隧道**：`.dsh-tmp/wolfbt/local_pg.py`（`ensure_tunnel()` + `DSN`）；**不要**依赖后台常驻隧道进程（会被 shell 重置杀掉）；
   - 大数据先落**本地 SQLite**（`data/mkt_bars_local.db`），再一次性 `COPY` 进 PG。
2. **改 `.env` 必须重建容器**：`cd /opt/marcus-platform/docker && docker compose up -d --force-recreate --no-deps backend worker`（`docker restart` 不会重新读 `.env`）。
3. **传文件进容器**：`docker cp`（宿主顶层目录不映射到 `/app`）；**只往新建子目录传数据**，覆盖 `/app/data` 根目录同名文件前必须备份+比对（曾覆盖过生产黑名单）。
4. **部署后先做语法自检**（`python -c "import ast;ast.parse(open(...).read())"`）——生产曾跑着一份带 git 合并冲突标记的文件（`build_concept_matrix.py`）导致 rc=256。
5. **每项改动**：可回退开关（默认关或保持现状）+ 定向单测 + **独立 commit**；**生产开关的开启需用户批准**。
6. **语料工作用 dsh**（`http://marcus-dsh:3001/chat`，仅在容器网络内可达；本地跑不了），**不要用正则/关键词提取**做语料结构化。
7. **PIT 纪律**：回测只用 T-1 及更早；盘后结论供次日使用。**重建产物标 `_rebuilt`/`_source`**。

---

## 7. 明确不要做（已删除的负向机制，勿重新引入）

| 机制 | 为什么不做 |
|---|---|
| **`heat_v2`**（主力资金热度） | 他 `doing` 主题平均 heat 排名 **10.42/13**（随机 7.0）→ −3.42、**t=−11.89**；假设与做法**方向相反**。代码已删（`apps/main_line/heat_v2.py`） |
| **`mainline_gate`**（结构资格闸） | 单独命中他方向 ≈ 随机（top1 **28%**/precision **12%**），作约束净负。代码已删 |
| **研报 catalyst + fusion（`main_line_judge`）** | ①研报催化从未进生产判据、生产实测 9 主题**全 0.0**；②IC 仅 0.056（弱）；③fusion 的 `conc` 有**方向缺陷**（实测第一名与池判定不符）。代码与任务已删 |
| **拥挤度/公募重仓"排除型"数据** | 主题层 IC≈0、个股层 IC 为正（+0.019）；加排除后基线 **+0.683% → −0.152%**；对生产 `crowding_blacklist` 的事件研究显示**被拦组反而更好**（+3.689% vs +0.250%） |
| **静态方向白名单** | 他的方向池**随时代轮换**（2021 白酒/医药 → 2025 机器人 → 2026 半导体/AI）；静态池在 2026 实测 **−0.201%(t=−1.42)** |
| **链内联动 / 龙头带动 / 波动率门槛（主题层）** | 实测均无效或有害（联动在 2026 几乎恒 ≥2、不触发；龙头带动三种实现都降低命中与收益；他的"日均波动 2 个点"是**个股**口径，主题层 amp20 全在 3.5–5.6%） |

**注**：`main_line_state.json` 里的 `gate`/`catalyst`/`fusion` 字段**已被移除**；`docs/wolf-alignment-checklist.md` **§F** 记录了每项的删除证据与回退 commit——**若确需回退，从 git 历史取回，不要重新发明**。

---

## 8. 相关文档索引

| 文档 | 内容 |
|---|---|
| `docs/wolf-behavior-blueprint.md` | **狼大行为蓝图**（§1 他的一天 / §2 动作全集 / §6 分类型度量表 / §9 硬参数 16 条 / §16 新发现与勘误） |
| `docs/wolf-alignment-checklist.md` | A 已建成 / B 不对齐 / C 三步 / D 待办（D1–D18）/ **§F 已删除的负向机制** |
| `docs/wolf-structural-pool.md` | 方向层（池判定）的完整验收、上线与迁移记录 |
| `docs/wolf-daily-log-nga.md` | NGA 30 天**带时刻**的逐日记录（做 T 的时序证据主要来源） |
| `docs/wolf-daily-log-xls2026.md` 等 | XLS 各期逐日精读（原话+日期） |
| `docs/wolf-d15-crowd-blacklist-event-study.md` | 拥挤黑名单事件研究（否决依据） |
| `docs/wolf-vs-system-mainline.md` | 他的主线 vs 我们主线的逐月对照 |

**开工第一步建议**：先只做**阶段 0**（换尺子）并把基线表发出来确认——在尺子修好之前，不要动任何兑现规则。

---

## 9. 狼大语料资产、取证方法与教训（**开工前必读**）

> 本计划的所有判据都必须能指回**狼大的原话+日期**。这一节告诉你：语料在哪、怎么查、以及**别再犯的错**。

### 9.1 语料资产清单

**原始语料**

| 文件 | 内容 | 覆盖 |
|---|---|---|
| `狼大回复汇总 20260814-1457&往期.xlsx`（仓库根目录） | 原始 XLS 汇总，**25,807 条** | 2016-01-02 → 2026-08-14 |
| NGA 帖子 `tid=47288722` | 楼层原文（**含精确时刻**） | 2026-07-31 → 2026-09-07 |

**已整理的逐日/逐期文档（人可读，含原话+日期；**设计判据时优先读这些**）**

| 文档 | 体量 | 内容与用途 |
|---|---|---|
| `docs/wolf-daily-log-nga.md` | 182KB | NGA **30 个交易日**逐条精读：**263 条动作 / 137 条策略陈述 / 85 条（前提-执行-退出）归纳 / 26 条口径冲突**，**每条带时刻**——**做 T / 兑现的时序证据主要来源** |
| `docs/wolf-daily-log-xls2026.md` | 393KB | XLS 2026 段（4,136 条）按**日历日**重建：**151 个日历日 / 700 条动作 / 533 条策略陈述**；**无精确时刻** |
| `docs/wolf-daily-log-xls2025.md` | 472KB | 2025-01-23 → 2026-08-14（**按动作分类**组织，日期在行内） |
| `docs/wolf-daily-log-xls2122.md` | 572KB | 2021–2022（同上，按动作分类） |
| `docs/wolf-daily-log-xls2016.md`、`-xls-misc.md` | 396KB / 4.5KB | 2016 段与杂项 |
| `docs/wolf-behavior-blueprint.md` | — | **主蓝图**：§1 他的一天 / §2 动作全集 / §6 分类型度量表 / §9 硬参数 16 条（每条带原话）/ §16 新发现与勘误 |
| `docs/wolf-alignment-checklist.md` | — | 对齐清单（A 已建成 / B 不对齐 / C 三步 / D1–D18 待办 / **§F 已删除的负向机制**） |

**已用 dsh 抽取的结构化证据（机器可读，带 `date`+`quote`）**

| 产物（容器 `/app/data/`；部分有本地副本） | 条数 | 内容 | 产出脚本 |
|---|---|---|---|
| `wolf_d12_evidence.json` | **340** | A 有无行情 / B 回避 / C 主线支线 三类判据 | `jobs/wolf_d12_evidence.py` |
| `wolf_overseas_evidence.json` | **130** | "海外链"态度演变（K1 参与 / K2 回避+理由 / K3 条件与重返；带 `scope=long_term\|conditional`） | `jobs/wolf_overseas_evidence.py` |
| `wolf_why_res_def.json` | **202** | 军工/资源的**角色定位**（主线/支线/低位埋伏/超短/避险/不参与）+ 理由 | `jobs/wolf_why_res_def.py` |
| `wolf_period_directions.json` | 12 期 | **跨期方向池**（2021Q1→2025Q4，按季度） | `jobs/wolf_period_directions.py` |
| `wolf_dir_kind.json`（本地 `data/`） | 232 | **方向标签分类**（股票方向 132 / 非方向 68 / 模式词 20 / 期货商品 12） | `jobs/classify_dir_labels.py` |

### 9.2 取证方法（**要新增判据时按这个流程**）

**通道**：dsh 网关 `POST http://marcus-dsh:3001/chat`，body `{"message": "...", "session_id": "..."}` → `{"reply": "..."}`
- ⚠️ **该地址只在容器网络内可解析**（本地跑不了）→ 脚本要在 `marcus-backend` 容器里执行；
- 现成调用封装：`jobs/wolf_d12_evidence.py` 的 `call_dsh()` / `_extract_json()` / `slices_for()` —— **直接复用，不要重写**。

**标准流程**：
1. **按时间切片**：把逐日/逐期文档切成"月"或"季度"片（单片 ≤ ~40KB）。`slices_for()` 已支持两种版式：
   - 版式 A（`### YYYY-MM-DD` 日块，如 `xls2026`、NGA）；
   - 版式 B（**动作分类表**，日期在行尾**全角括号**里，如 `xls2122`/`xls2025`）→ 按**行内日期重组**到季度。
2. **写 prompt（要点）**：
   - 明确**唯一任务**与输出 JSON schema（例：`{"items":[{"date":"","kind":"","quote":"","criterion":""}]}`）；
   - **强制要求逐字原话**，并写"**找不到就不输出，绝不编造或用你自己的话冒充**"；
   - 要求**日期**；按需加 `scope`（`long_term`/`conditional`）、`role`（主线/支线/超短…）等字段；
   - **禁止泛泛总结**（"不要输出泛泛的交易道理"）。
3. **跑 + 落盘**：每片一次调用，结果按片累积写入 JSON（**保留 raw reply 便于复核**），断点续跑（跳过已完成片）。
4. **人工复核**：抽读 10–20 条，确认 `quote` 是**原文**（不是改写）、`date` 正确。
5. **写进文档时**：每条结论**附原话与日期**；若同一主题有冲突表述，**按时间列出来**，不要只挑一条。

**发现新语料源时**：先更新 §9.1 的表，再开始抽取（避免多份互相矛盾的口径）。

### 9.3 教训（**这些坑都踩过，别再踩**）

| # | 教训 | 具体 |
|---|---|---|
| **L1** | **绝不手打/改写语料** | 曾为绕过子代理失败而手打文本冒充语料 → 混入自造句子 → 污染产物（已全部删除并用真实原文重做）。**语料只能来自 XLS/NGA 原文** |
| **L2** | **不做关键词/正则提取** | 语料结构化一律走 dsh 语义精读；正则只用于**切片与版式处理**，不用于判断语义 |
| **L3** | **"他这么说" ≠ "这么做有效"** | 凡可检验的句子都要标注**已检验/仅原话**。反例：§9 硬参数 #14「基金持仓多的就上不去」——原话很硬，但四项检验**全部不成立**（个股层 IC +0.019；加排除后基线 +0.683%→−0.152%；对生产黑名单的事件研究显示被拦组反而 +3.689%） |
| **L4** | **比对时必须用"股票主线层"口径** | 他的 doing 里混着**期货商品仓**（"油（多油）""黄金（黄金期货多单）"）、**操作模式词**（低位方向/打野/做T）、**非方向**（整体仓位/ETF）。剔除后（用 `wolf_dir_kind.json`）recall 从 55% → **75%** |
| **L5** | **dsh 会抖动，必须重试** | 实测 `RemoteDisconnected` / 连接被拒 / 容器重启；`call_dsh` 退避重试 **4–6 次**、切片间隔 **≥5s**；失败要记入 `failed` 字段，**不能把"取数失败"当成"当天没有"** |
| **L6** | **切片版式不统一** | `xls2026`/NGA 是日块；`xls2122`/`xls2025` 是**动作分类表**且日期在**全角括号**中（`（2021-01-12）`）→ 正则要同时认全角/半角，且先剥掉行尾的 `|` |
| **L7** | **子代理并发上限 ≈3** | 9 个并发只活 1 个；尾块（`*_04.md`/`*_07.md`）反复失败 → 串行重试或主代理自己精读 |
| **L8** | **样本量与统计纪律** | n<100 只作探索；**重叠样本会把日度 t 虚高**（例：11 个快照×重叠前瞻 → 日度 t=3.66 但**块状 t=1.39**）→ 关键结论必须做**块状/分批检验**，并给"同池同日基线对照" |
| **L9** | **跨期比较要声明覆盖** | 语料只有 **2016 / 2021–22 / 2025–26** 三个可观测期（2017–2020、2023–24 几乎为 0）→ **不得断言"他改变了"**，只能说"这几期分别如何" |
| **L10** | **他剩下的两条判据我们实现过且无效** | "链内联动"（2026 科技链几乎恒 ≥2 个子方向在动 → 加"≥2"完全不触发）与"龙头带动"（三种实现都**降低**命中与收益）——**不要以为没实现，是实现了但被数据否决** |
| **L11** | **他的方向池随时代轮换** | 2021 白酒/医药/面板 → 2025 **机器人** → 2026 半导体/AI/资源。用 ≤2025 语料定的**静态池在 2026 实测 −0.201%(t=−1.42)** → 复刻的是**框架**（量能集中度 × 有没有在动），不是方向清单 |
| **L12** | **语料与数据的边界要写清** | 例：他在**期货/商品**（油/金）与**超短题材**（军工打短）上的动作**不属于**我们股票方向层的对照集；一致性指标只在他"做股票主线的日子"上成立 |

### 9.4 一条可复用的"取证 → 判据 → 验收"闭环

```
① 取证（dsh 切片精读）        → 得到 带日期+原话+一句话判据 的 JSON
② 可算化                     → 把判据翻译成 PIT 可算量（例："光+半导体占市场50%成交量" → 主题近5日成交额占比）
③ 离线验收（本地跑，不碰生产） → 同池同日基线对照 + 分档 + 分段(H1/H2) + 块状 t
④ 决策                       → 优于基线才接线；不优/有害就写进 §F 并删除（不要留"关掉的开关"）
⑤ 上线                       → 开关默认关 → 影子模式（只记录）→ 经用户批准才开启
```
