# 批次 2 回测设计（2026-09-11 §11.4 落地批）

> 批次 1 的计划见 `docs/backtest-plan.md`（那份覆盖 P0/P1/S 项的布腿·选股层改动）。
> 本文是**批次 2**（§11.4 清单 11 项 + 2026-09-11 两个开关）的专用设计，口径与批次 1 不冲突，
> 但**不能照搬**——原因见 §1。

---

## 1. 为什么不能照搬批次 1 的"逐项 A/B → 净值对比"

批次 1 的新增项几乎都在**布腿/选股层**（rs 闸、空窗等待、位置闸、去弱留强），改变的是"**买什么**"，
所以"同一区间开关对照比净值"是成立的。

批次 2 的 11 项里，只有三类会改变成交，且性质完全不同：

| 类 | 项 | 作用点 | 能不能用净值对比 |
|---|---|---|---|
| **A 准入/拦截** | A5 时间窗、A9 谨慎4条 | 拦掉部分**日内做T买腿** | ❌ 净值会被"少做T"整体影响，看不出单个判据好坏 → 要看**被拦的那批腿本身好不好** |
| **B 卖出** | A10 上轨减半、A10 中轨全止盈、C1 上限放宽 | 改持仓时长与规模 | ⚠️ 可以，但需先知道"触发点之后的走势"，否则分不清是机制好还是行情好 |
| **C 纯提示/信号** | B4 涨停梯队、B1 打分、C2 方向、A3 期指、A4 黄白线 | **不改变任何成交**（只进纪律上下文） | ❌ 净值对比恒等 → 只能测**判据的预测力** |

**结论**：必须拆成三层，且**顺序是 L1 → L2 → L3**（先淘汰无效判据，再验拦截/卖出，最后才组合跑净值）。

---

## 2. 三层结构

| 层 | 回答的问题 | 观测单位 | 数据 | 成本 | 结论形式 |
|---|---|---|---|---|---|
| **L1 信号层** | 这个判据指向的方向/标的，**之后是不是真的更强**？ | (信号日, 标的/主题) | 全市场日线 by date + 指数日线 + 期指持仓/两融/龙虎榜/资金流（**已全部实测可取**） | 1 天 | 命中率 / 分层收益 + **n 与区间** |
| **L2 执行层** | ①**被拦掉的腿**是不是真的更差？②卖出规则比"持有到 T+5 收盘"更好吗？ | 逐腿事件 | 个股 m5（253/254 触发样本）+ 日线 | 2~3 天 | 被拦 vs 放行的 T+1/T+5 分布、错过成本 |
| **L3 组合层** | 合起来净值/回撤/换手是否改善 | 账户 | 需 gate 逐日回放（批次 1 Stage 0 前置） | 3~5 天 | 净值曲线、回撤、换手 |

**赔率**：L1 最便宜且当天能出结论，**L1 判无效的项直接不进 L2/L3**（这是本设计最大的省钱点）。

---

## 3. L1 逐项设计（信号层）

| 项 | 信号（PIT 口径） | 结果变量 | 数据现状 | 现在能跑吗 |
|---|---|---|---|---|
| **C2** 跌得少弹得早 | D 日收盘：主题窗口累计 = 主题等权 − 全市场中位 | D+1..D+5 **主题等权 − 全市场中位** | 全市场日线 by date（1 请求/日）、主题成分（生产有 `stock_confirm_result.json`） | ✅ **能，长窗** |
| **A3** 期指 → 次日黄白线 | D 日 `fut_holding` 多空**增减**偏多度（口径已按他 2026-01-23 纠正） | D+1 中证1000 − 上证 | 已验证 | ✅ **已跑：158 天 51.3%**（当日下跌日 54.4%、上涨日 48.9%）→ **样板** |
| **A10** BOLL 上轨 | D 日 high ≥ **D-1 收盘算的** BOLL(20,2) 上轨 | D+1..D+5 个股收益 vs 同池同日基线 | 个股日线 | ✅ 能，长窗 |
| **A10** BOLL 中轨 | D 日**收盘** < D-1 算的中轨 ∧ 放量（量≥前日×阈值） | D+1..D+5 收益（**看是否避开了下跌**） | 个股日线（有 vol） | ✅ 能，长窗 |
| **A4** 黄白线 | D 日收盘 spread = 中证1000 − 上证（**代理**） | D+1 小盘−大盘；再做一次 D 当日 spread → 当日做T腿收益 | 指数日线 | ⚠️ 只能日终口径（**盘中黄白线无历史分钟数据**） |
| **B1** 复盘打分 | D 日 8 项打分 → 多空判定 | D+1 **前 2 小时**方向 | ⚠️ 需要**指数分钟历史**：腾讯 m5 单次 ≤500 根（≈10 交易日）、brze 指数分钟不可用（§12.3）、东财被拒 | ⛔ **受阻** → 先跑"D+1 全天方向"长窗版兜底 |
| **B1** 分项 | 各计分项 → 次日方向 | 同上 | 期指/两融/龙虎榜/资金流均 ✅ | ✅ 可以，**且能顺便看哪几项真的有预测力** |
| **B4** 涨停梯队 | D 日 主题/行业 梯队标签（完整/毕业照/上板失败） | D+1 该方向涨停家数、D+1..D+3 超额 | `limit_list_d` 是 EOD（当日盘中返回 0 行） | ⚠️ 先探**历史可回溯多久**，再定窗口 |
| **A5** 时间窗 | 腿发生时刻是否在 09:45–10:00 / 14:00–14:30 | → 属于 L2（不是信号层） | m5 有 | L2 |
| **A6** 缺口/量能 | "量能比"可算；"黄线在上"不可算（同 A4 限制） | 分组收益 | m5 + 日线 | ⚠️ 只能测**缺口＋量能**部分 |
| **A9** 谨慎4条 | "黄线**迅速**下穿/交织"需要**分钟级**黄白线序列 | — | **无** | ⛔ **不可回测**（与批次 1 §8 同类，报告里必须显式声明；只做日级代理的定性验证，**不当验收**） |
| **C1** 利润垫 | 账户层已实现盈亏 | — | 由模拟账户提供 | L3 才有意义 |

---

## 4. L2 执行层设计

**样本**：沿用既有 254/253 **首次触发**样本（n=428，T+5 收盘 胜率 47%/均值 +0.29%/中位 −0.61%）+
同池同日基线（n=23,512，胜率 49%/均值 +0.61%）。

| 机制 | 对照做法 | 核心问题 |
|---|---|---|
| **A5 时间窗** | 同一批腿，只保留落在两个窗内的 | **拦掉的那 55% 腿，T+5 是不是真的更差？**（若不是 → A5 是白收紧，应回退） |
| **A9 谨慎4条** | 用**日级代理**（当日 spread）分组 + 声明代理性 | 谨慎条件下入场的腿是否更差（**弱验证**） |
| **A10 上轨减半** | 每条持仓腿：持有到 T+5 收盘 vs（触及上轨当日）卖一半 | 减半是否提高中位、降低回撤？错过成本多少？ |
| **A10 中轨清仓** | 同上：持有到 T+5 vs 收盘破中轨即全出 | 是否避开了后续下跌？（他这句是"顶部阶段"做法 → **必须分时段看**：顶部段 vs 非顶部段） |
| **A6 缺口/量能** | 不拦，按"腿时刻的量能比/缺口状态"**分组** | 分组收益是否单调（若单调 → 才值得升级成门） |

**口径**：同一批腿、同一区间；**被拦掉的腿要单独统计"后来涨了多少"（错过成本）**。
**验收**：Δ中位 > 0 且最大单笔亏损不恶化；**n < 100 不下结论**。

---

## 5. L3 组合层

沿用批次 1 的 Stage 0 前置（gate 逐日回放、`concept_long.json`、`stock_pool.db`）与 §3 时序纪律。
**新增需写进 `backtest_env.json` 的口径**：

```
WOLF_TRADE_WINDOW / _AM / _PM / _TW_KINDS        # A5
WOLF_GAP_OPEN / WOLF_GAP_CAUTION / WOLF_GO_*     # A6 + A9 谨慎
WOLF_LIMIT_LADDER / WOLF_LL_GRAD_MIN_N / WOLF_LL_FAIL_RATE   # B4
WOLF_REVIEW_SCORE / WOLF_THEME_RESILIENCE / WOLF_INDEX_FUTURES  # B1 / C2 / A3
WOLF_CUSHION_CAP / WOLF_CUSHION_KEEP / WOLF_CUSHION_PRINCIPAL   # C1
WOLF_BOLL_SELL / WOLF_BOLL_MID_EXIT / WOLF_BOLL_N / WOLF_BOLL_K / WOLF_BOLL_MID_VOL  # A10
WOLF_CLOSE_BREAK_HM                              # A10 中轨的"收盘确认"时刻
```

**L3 组合基线 = 当前生产口径**（含 2026-09-11 用户已打开的 `WOLF_CUSHION_CAP=1`、`WOLF_BOLL_MID_EXIT=1`）。

**叠加顺序**（避免一次叠加看不出谁在起作用）：基线 → +A5 → +A9 → +A10 上轨 → +A10 中轨 → +C1。
每步单独记录边际贡献。

---

## 6. 前视与口径纪律（本批特有的坑）

1. **A6 的"当日累计量"必须渐进式计算**：t 时刻只能用 ≤t 的 bar，**绝不能用当日全天量**（否则是前视）。
2. **A10 的上轨必须用"前一日收盘"算**：用当日收盘算 BOLL 再判"当日是否触及" = 前视（本设计的 L1 用 D−1 收盘算的轨去判 D 日 high）。
3. **C2 的主题窗口截止 D 日收盘**，结果变量从 D+1 开始。
4. **A3/B1 的 EOD 数据只能用于 D+1**，不得用于 D。
5. **B4 的 `limit_list_d` 是 EOD**（实测当日盘中返回 0 行）→ 只能进 D+1。
6. **所有阈值参数**（`WOLF_LL_GRAD_MIN_N`、`WOLF_GO_*`、`WOLF_BOLL_*`、`WOLF_REFILL_*`）在报告里列明取值；
   **参数扫描不算验收**（它是校准，不是验证）。

---

## 7. 统计纪律（本批已经吃过一次亏）

1. **必须声明样本量与区间**。反例就在本批：A3 期指规则在**26 天窗口命中 65.4%**，拉到 **158 天只剩 51.3%**
   → 所以 **n < 100 一律只作探索性结论**，不得写进验收。
2. **多重比较**：本批可测判据约 15 个，5% 水平下随机会有 1 个"显著"。
   → **主指标事前固定为「T+5 中位收益 + 胜率」**，其余（命中率、均值、分层）标注为探索性。
3. **同池同日基线强制**（沿用批次 1 §3.3）：任何"更好"的结论都要与同池同日随机买入对比，否则把 beta 当 alpha。
4. **分层报告**：按 主题 / 时段（下跌段 vs 反弹段）/ 市值档 分层，避免被单一主题主导。

---

## 8. 无法回测 / 只能定性（**必须写进报告**）

| 项 | 原因 | 处理 |
|---|---|---|
| **A9 谨慎4条**（"迅速下穿/交织"） | 无**分钟级**黄白线历史：腾讯 m5 单次 ≤500 根（≈10 交易日）、brze 指数分钟不可用、东财被拒 | 只做日级代理的**定性**验证；**不进验收** |
| **B1 的"前 2 小时"** | 同上（需要指数分钟） | 先跑 D+1 全天方向的长窗版；盘中版待补数据源 |
| **A4 实时黄白线** | 同上（只有日线代理） | 用日终 spread 做代理并声明 |
| **A6 的"黄线在上"** | 同上 | 只测缺口+量能 |
| **执行层成交** | PG 不可达；未建模资金闸分批、涨跌停、滑点（同批次 1 §8） | 沿用乐观假设并显式声明 |
| **C1 利润垫** | 需要账户已实现盈亏轨迹 | 只在 L3 有意义 |

---

## 9. 产出物与验收

```
data/_bt_batch2/            # 沙箱（勿污染生产 DATA）
  l1_signals.csv            # 每个信号：日期、标的、信号值、结果变量、分层
  l1_REPORT.md              # 每项一节：n、区间、命中率/分层、结论（进/不进 L2）
  l2_legs.csv               # 逐腿：放行/被拦、机制、T+1/T+5、错过成本
  l3_equity.csv / REPORT.md
```

**验收线**
- **L1**：分层单调 或 命中率显著 > 50%（n ≥ 100）→ 进 L2；否则**判无效、不进 L2**。
- **L2**：Δ中位 > 0 且最大单笔亏损不恶化（卖出类）；被拦腿显著更差（拦截类，否则该机制应关掉）。
- **L3**：净值与最大回撤不劣于基线；换手成本单列。

---

## 10. 建议的第一批执行顺序（按"信息量/成本"排）

1. **L1-C2**（主题相对强度 → 未来 5 日超额）：数据最全、可长窗，**直接决定调整期这套要不要照做**
2. **L1-A10**（BOLL 上轨 → T+5 vs 同池基线）：决定**刚打开的减半锁利**是否有正贡献
3. **L1-B1 分项**（期指/两融/龙虎榜/资金流各自 → 次日方向）：顺便淘汰没有预测力的计分项
4. **L2-A5**（被拦腿 vs 放行腿）：A5 是本批**最收紧**的机制（日内T腿只剩 45 分钟/天），先验它
5. 再进 **L3** 组合

> 在这之前，A3 那套"L1 长窗 + 分组 + 明确披露 n"的做法已经跑通（158 天，51%/54.4%），
> **可以直接当模板复用**：脚本 `.dsh-tmp/wolfbt/bt_a3_validate.py`。
---

## 11. 首次运行记录（2026-09-11，L1-C2）

**做法**：生产容器内跑（`/app/data/_bt_batch2/l1_c2.py`，脚本副本在 `.dsh-tmp/wolfbt/bt2/`）。
窗口 110 个交易日（20260407→20260911），样本 = 调整期内的「主题 × 日」，结果变量 = D+1..D+5
「主题等权 − 全市场中位」。产出 `data/_bt_batch2/l1_c2.csv`。

**结果（n=243）**

| 分组 | n | 中位 | 胜率 |
|---|---|---|---|
| leader（跌得少∧弹得早） | 92 | +0.96% | 76.1% |
| defensive（只跌得少） | 146 | +1.17% | 69.9% |
| laggard | 5 | +3.17% | 80.0% |
| Q1 窗口超额最弱 | 60 | **+2.25%** | **85.0%** |
| Q4 窗口超额最强 | 63 | +0.55% | 61.9% |

**追加检验（`l1_c2_refine.py`）**
- ① **同日横截面（剔除大盘 beta）**：leader 中位 **−0.27%**、胜率 **46.7%**；laggard −2.47%。
- ② **尾部**：leader 标准差 2.57 / 5% 分位 −3.24%，defensive 3.06 / −3.72% → **leader 波动与尾部略小**。
- ④ **拆开两个子判据**（他原话里是两个独立的词）：只「跌得少」同日相对中位 −0.09%/胜率 49.2%（n=238）；
  只「弹得早」−0.19%/48.2%（n=199）→ **剔除 beta 后都没有正向选方向能力**。

**结论：本次运行**不能**判定 C2 无效，因为发现了实现层的两个问题——**
1. **判据几乎没有区分度**：`less_down` 命中 **238/243**、「两者都不满足」**n=0**。
   原因：基准取「**全市场中位**」，而我们的主题池（AI/算力科技、农业、稳增长）在窗口内普遍强于中位
   → "跌得少"几乎恒真 → 标签退化成常量。**必须换基准**（同批主题均值 / 全市场等权 / 上证）。
2. **有效独立样本远小于 243**：只有 **3 个主题 × ~80 个交易日**，同日 3 个观测共享大盘因子
   → 有效样本 ≈ 80 天 < 100 → 按 §9/§7 只能作**探索性**，不得进验收。

**站得住的弱证据**：leader 的**波动/尾部更小**（防守性）。这与他的语境一致——2026-01-27 那段讲的是
「**调整期怎么度过**」，本意是**防守**，不是"照着它选方向能赚更多"。
⇒ 下一轮应把结果变量换成**风险调整口径**（最大回撤 / 下行波动 / 收益回撤比），而不是原始超额收益。

**下一轮要改的三件事**：① 换基准并检查标签分布是否均衡（作为判据可用性的前置检查）；
② 扩主题池（`confirm_universe` 只覆盖确认池 → 换更宽的主题映射）以增加横截面；
③ 区间拉到 ≥1 年，并把结果口径改为风险调整。
---

## 12. L1-C2 v2 运行记录（2026-09-11，概念板块层面，n=183,942）

**改法**（对应 §11 的两个问题）：
- **改在概念板块层面测**（DB `stock_concept_map`：1068 个概念、平均 100+ 成分）——这正是他 2026-01-27
  说的「按市场**二级概念板块**」「在几百个板块里面找到唯一对的那个」；横截面 1000+/天，统计功效足够。
- 区间 **245 个交易日（20250909→20260911）**；全市场日线按日缓存（`mkt_daily.pkl.gz`，重跑不再取数）。
- 结果口径加**风险调整**：5 日累计超额 + 概念自身最大回撤 + 日超额下行波动 + 日超额命中率。
- 双基准（全市场中位 / 等权）中本次用中位。

**前置检查（区分度）—— 仍不均衡，且发现结构性偏置**
- 「跌得少」占比 **76.5%**（140,670/183,942）、「弹得早」**89.8%**；excess 分位 5/25/50/75/95 =
  −7.1 / +0.3 / **+4.4** / +9.0 / +21.1（**正偏**）。
- 原因：窗口以**基准（全市场中位）的峰值日**为锚，而多数概念在基准见顶后仍在上涨 → 从该锚点起算，
  概念的"跌幅"被系统性低估。**这是实现偏置，不是判据性质**。
  ⇒ 修法（下一轮）：窗口锚点改为**各自峰值**，或干脆只用**横截面排名**（下面 ② 就是这种口径）。

**② 按 excess 十分位（横截面，最能剔除大盘 beta）—— 信号有效但非线性**

| 分位 | n | 中位 | 胜率 | 5% 分位 |
|---|---|---|---|---|
| D1 最弱 | 18,394 | +0.83% | 61.5% | −5.67% |
| D2 | 18,394 | +0.61% | 62.2% | −3.08% |
| D3 | 18,394 | +0.55% | 62.2% | −2.73% |
| D4 | 18,394 | +0.61% | 64.1% | −2.76% |
| D5 | 18,394 | +0.66% | 64.9% | −2.73% |
| **D6** | 18,394 | +0.72% | **66.0%** | −2.71% |
| D7 | 18,394 | +0.71% | 65.9% | −2.69% |
| D8 | 18,394 | +0.74% | 65.7% | −2.95% |
| D9 | 18,394 | +0.80% | 63.4% | −3.94% |
| D10 最强 | 18,396 | **+0.95%** | **58.6%** | **−11.84%** |

→ **胜率在 D4–D8（相对强但不极端）形成 64–66% 的甜蜜区**；**极端最强 D10 胜率最低、尾部最差**
（均值 +2.39% 是被少数大赢家拉高的"博彩型"分布）。⇒ 可用的规则形态是「**相对强但不极端**」，
不是"越强越好"。

**① 他的两个词分别的贡献（横截面同源口径见下）**

| 分层 | n | 中位 | 胜率 | 5% 分位 |
|---|---|---|---|---|
| 跌得少 ∧ 弹得早（leader） | 130,445 | +0.70% | 64.0% | −3.81% |
| 只跌得少 | 10,225 | +0.80% | 63.9% | −3.50% |
| 只弹得早 | 34,801 | +0.66% | 62.3% | −3.80% |
| 都不满足 | 8,471 | +0.69% | **59.7%** | **−5.03%** |

→ **胜率贡献主要来自「跌得少」**（63.9% vs 都不满足 59.7%），「弹得早」几乎不额外加分；
「都不满足」**尾部最差** → 作为**反向排除**指标最有效。

**③ 风险调整（防守性）—— 成立**

| 指标 | leader | 都不满足 |
|---|---|---|
| 概念自身 5 日最大回撤（中位） | **−1.96%** | −2.58% |
| 日超额下行波动（中位） | **0.12** | 0.19 |
| 日超额命中率（中位） | 0.60 | 0.60 |

→ 绝对收益差别不大，但 **leader 的回撤与下行波动都更小**。这与他 2026-01-27 的语境一致
（那段讲「**调整期怎么度过**」= 防守），**不是**"照着选方向能赚更多"。

**④ 分时段（按基准窗口跌幅）**

| 基准窗口 | leader | 都不满足 |
|---|---|---|
| −99~−8%（深跌，n=93,829） | +0.72% / 63.4% | +0.81% / 62.1% |
| −8~−4%（n=68,746） | +0.69% / 64.7% | +0.84% / 60.5% |
| **−4~0%（浅调整，n=21,367）** | **+0.66% / 64.0%** | **−0.07% / 48.6%** |

→ **该判据在浅调整期最有价值**（浅调整里"都不满足"几乎无收益），**深跌期失效甚至略反**。

**结论（L1-C2 通过，带条件）**
1. **作为防守判据：通过**（回撤/下行波动/尾部更小）；
2. **作为方向选择判据：有条件通过** —— 只用「相对强但不极端」（D4–D8）这一档，**不要追极端最强**；
3. **适用条件**：浅调整期（基准窗口 0~−4%）；
4. **实现要改**：窗口锚点（各自峰值 或 纯横截面排名）—— 否则标签不均衡，结论易被偏置污染；
5. 可进 L2 的形态：**「相对强但不极端」+ 反向排除「都不满足」**。
---

## 13. L1-A10 运行记录（2026-09-11，BOLL 上轨 / 中轨）

**数据**：复用 §12 的全市场日线（245 交易日，20250909→20260911，5407 只），另按日补取 `vol`
（缓存 `mkt_daily_vol.pkl.gz`）。**PIT**：轨只用 ≤D-1 的收盘算，信号用 D 日收盘比。
（"触及上轨"他用盘中最高价；我们只有 close-to-close 缓存 → 用**收盘价**判定，比"触及"更严格，
会低估触发频率，方向性结论不受影响。）

### 13.1 上轨「减半锁利」—— **通过**

| 口径 | 结果 |
|---|---|
| 同日横截面（逐日算差再对日平均，219 天可比） | 触及/上穿上轨 **−0.250%（中位）/−0.337%（均值）**，**仅 46.1% 的天数反向** |
| 池化 | 触及 n=111,095 中位 **−1.33%**/胜率 40.8%/5%分位 **−12.64%**；未触及 −0.79%/42.5%/−8.57% |
| 按「离上轨多远」（ratio=收盘/上轨）十分位 | R1 −0.64%/44.9% → R9 −0.84%/41.5% → **R10(≥1.0) −1.29%/40.9%**，**单调递减** |

⇒ **触及上轨后未来 5 日确实更弱且尾部更差** → 他 2026-04-29「逢高卖出部分底仓、锁定利润」**有依据**，
`WOLF_BOLL_SELL=1`（减半锁利）**保留**。

### 13.2 中轨「全止盈」——**必须带上他原话的前提「顶部阶段」才成立**（本轮最重要的发现）

| 前提 | 跌破中轨 | 未跌破 | 逐日横截面差 | 差>0 天数占比 |
|---|---|---|---|---|
| **不加前提**（全样本，63 万样本） | −0.71%/42.9% | −1.01%/41.7% | **+0.159%** | 52.1% → 看似不成立 |
| 加「放量」（vol≥1.2，他原话就有"放量"） | −0.73%/42.9% | — | 放量−缩量 **−0.029%** | 48.2% → 放量几乎无增量 |
| **大盘高位**（等权近 120 日区间上 20% 分位） | **−1.52%/胜率 36.6%** | −0.90%/43.8% | **−1.994%** | **18.8%**（16 天可比） |
| 两者同时高位 | −1.14%/42.3% | **+0.24%/51.3%** | −1.685% | 30.0%（10 天） |
| 只「个股高位」（近 60 日区间上 20% 分位） | −0.31%/47.1% | −0.58%/46.6% | −0.045% | 49.1% → **无区分** |

**结论**：他把前提写在句首——「**顶部阶段**，…全止盈的位置就放在日线BOLL中轨附近，放量跌破收盘完全止盈」。
**关键是"大盘顶部"，不是"个股高位"，也不是"放量"**。拿掉前提去测会得出"规则不成立"的错误结论
（我第一轮就是这么做的）。⚠️ 可比天数只有 **16 天（独立样本 < 100）** → 按 §7 纪律**属探索性**，
待把区间拉长到 3~5 年复核。

### 13.3 据此做的代码修正（2026-09-11）

- `wolf_boll_levels.market_top()`：上证收盘在近 `WOLF_BOLL_MID_MKT_WIN`(默认 120) 日区间的分位
  ≥ `WOLF_BOLL_MID_MKT_Q`(默认 0.8) 才算「顶部阶段」；取数失败 → **不放行**（宁可不卖）。
- `mid_break_sells()` 强制先过这道门；理由文本里带上大盘位置，便于事后核对。
- 新增 3 个定向测试（非顶部不触发 / 大盘数据缺失不放行 / 顶部时触发）。
- 上轨侧不受影响（10.1 已证明它本身成立）。
---

## 14. L2-A5 运行记录（2026-09-11，时间窗：拦对了没有？）

**样本来源**：`t_triggers` 里 **A5 上线前（2026-09-11 之前）**的真实买腿触发 —— 它们都没被时间窗过滤过，
所以可按触发时刻切成"窗内/窗外"做**反事实对照**。触发日志本身只回溯到 **2026-08-25（≈13 个交易日）**。

**数据缺口（首轮踩到）**：A5 适用腿型的标的**大量是 ETF**，而 `pro.daily` 只含股票 → 首轮 310 条里
**302 条缺日线**。补 `fund_daily`（14 天，2100+ 只 ETF）后可比样本才上来。

### 14.1 全部买腿（n=306 可比）—— **窗内更好，方向支持 A5**

| 分组 | n | T+5 中位 | 胜率 | 超额中位 |
|---|---|---|---|---|
| ★ 窗内（09:45–10:00 / 14:00–14:30） | 48 | **+0.24%** | **50.0%** | **+2.18%** |
| ★ 窗外（会被 A5 拦掉） | 258 | −0.77% | 47.3% | +1.22% |

按时刻细分：**窗口2 14:00–14:30 最好**（n=32，中位 **+1.14%**、胜率 **56.2%**、超额 **+3.14%**）；
平稳时段 10:00–14:00（n=214）中位 −0.77%/47.2%；开盘前段 09:30–09:45（n=21）47.6%。

### 14.2 A5 适用腿型（low_buy + custom_prevlow，n=276 可比）

| 分组 | n | T+1 中位 | 胜率 | 超额中位 |
|---|---|---|---|---|
| ★ 窗内 | 77 | −0.11% | 35.1% | **+0.48%** |
| ★ 窗外 | 199 | −0.31% | 39.7% | −0.06% |
| 开盘前段 09:30–09:45 | 11 | −1.49% | **9.1%** | **−1.06%** |
| 尾盘 14:30–15:00 | 45 | **−1.43%** | 33.3% | +0.32% |

→ **他"尽量避免开盘直接买卖"是对的**：开盘前段 T+1 胜率仅 9.1%、超额 −1.06%；尾盘也差（中位 −1.43%）。
A5 恰好把这两段拦在外面。

### 14.3 结论与限制

- **方向支持 A5（保留）**：窗内在 T+5（全部买腿）与 T+1 超额（A5 适用腿型）上都优于窗外；
  被放行的 14:00–14:30 是全场最好时段，被拦的开盘前段/尾盘是全场最差时段。
- **⚠️ 样本太小，属探索性**：可比 48 vs 258（全部买腿 T+5）、77 vs 199（A5 适用腿型 T+1），
  **两侧任一臂 < 100**；且 **A5 适用腿型的 T+5 根本不可测**（触发集中在最近 4 天，T+5 结果尚未发生）。
- **要变成验收级证据，必须自建样本**：用 m5 在更长区间上重建 254/253 触发（含时刻），
  本轮的 `t_triggers` 日志只有 13 个交易日，样本天花板太低。
---

## 15. 本轮（L1-C2 / L1-A10 / L2-A5）对生产开关的建议

| 机制 | 开关 | 建议 | 依据 |
|---|---|---|---|
| A5 日内做T时间窗 | `WOLF_TRADE_WINDOW=1` | **建议关闭，见 §18**（两种近似口径都无正贡献；硬收紧） | L2-A5：窗内 T+5 中位 +0.24%/胜率 50.0% vs 窗外 −0.77%/47.3%；14:00–14:30 最好、开盘前段/尾盘最差（探索性，臂 n<100） |
| A10 BOLL 上轨减半锁利 | `WOLF_BOLL_SELL=1` | **保留** | L1-A10：同日横截面 −0.250%（仅 46.1% 天数反向）；离上轨距离十分位**单调递减**（n=117 万） |
| A10 BOLL 中轨全止盈 | `WOLF_BOLL_MID_EXIT=1` | **保留，但必须带「大盘顶部阶段」前提**（已实现并部署） | 不带前提：逐日差 +0.159%（看似不成立）；**带前提（大盘近 120 日区间上 20% 分位）：−1.994%、仅 18.8% 天数反向**。当前大盘分位 0.259 → 不触发（正确）。⚠️ 可比仅 16 天 |
| C2 方向层「跌得少弹得早」 | `WOLF_THEME_RESILIENCE=1` | ~~降级为"防守参考"~~ → **见 §16：连防守性也不成立 → 仅作提示、不得影响决策** | L1-C2 v2：作进攻选方向无 alpha（同日横截面中位 −0.27%/46.7%）；但 leader 5 日最大回撤 −1.96% vs −2.58%、下行波动更小 → **防守性成立**。建议只用「相对强但不极端」档（D4–D8，胜率 64–66%），**不追极端最强**（D10 胜率 58.6%、5% 分位 −11.84%） |
---

## 16. L1-C2 v3 运行记录（2026-09-11）——**结论翻转：v2 的正面发现不成立**

**改法**（针对 §12 的两个问题）：
1. **判据横截面化**：用当日全部概念的 excess 分位（前 50% = 跌得少）→ 分布必然均衡（修 v2 的 76.5% 不均衡）；
2. **结果变量横截面化**：未来 5 日收益 − **当日全部概念未来 5 日收益均值**（彻底剔除大盘与截面共同因子）；
3. **锚点稳健性对照**：同时算「基准峰值」与「**各自峰值**」两个版本的跌幅；
4. 预计算累计序列（v2 在日内循环里重复 cum，慢）。

### 16.1 锚点偏置被证实

| 口径 | 「跌得少」占比 |
|---|---|
| 锚点 = 基准峰值（v2） | **76.5%** |
| 锚点 = **各自峰值** | **37.6%** |

⇒ 锚点选择把标签占比从 76.5% 拉到 37.6%，**偏置假设成立**（这也是 v2 标签退化的主因）。

### 16.2 修正后：判据**没有截面选择能力**

结果 = 当日横截面相对收益（去 beta）：

| 判据变体（锚点=基准峰值 ∧ 横截面分位） | n | 中位 | 胜率 |
|---|---|---|---|
| 跌得少(前50%) ∧ 弹得早(前50%) | 64,073 | −0.36% | 44.8% |
| 只跌得少 | 27,990 | −0.44% | 42.0% |
| 只弹得早 | 24,786 | −0.35% | 44.2% |
| 都不在前 50% | 67,093 | −0.44% | 41.4% |

→ **四组几乎无差异**（中位差 <0.1pp、胜率差 <4pp，且不单调）。
十分位呈 **U 型**：D1 −0.16%/47.6% → **D4–D5 最差 −0.57%/39.7%** → D9 −0.33% → D10 −0.11%/48.9%。
⇒ v2 的「中间甜蜜区（D4–D8 胜率 64–66%）」**是绝对口径（vs 全市场中位）里的大盘/截面共同因子造成的**，
不是横截面选择能力。

### 16.3 v2 的「防守性」也不成立

| 横截面判据（平衡样本） | 双前 50% | 双后 50% |
|---|---|---|
| 5 日最大回撤（中位） | **−2.45%** | −1.89% |
| 绝对收益（不去 beta） | +0.16% / 51.4% / 5%分位 **−9.54%** | −0.10% / 48.8% / −6.20% |

→ 前 50% 的**回撤更大、尾部更差**。v2 里「leader 回撤 −1.96% vs 都不满足 −2.58%」是
**130,450 vs 8,471 的严重失衡对比**，属选择性假象。

### 16.4 结论（覆盖 §12 与 §15 中对 C2 的判断）

1. **修正锚点偏置 + 横截面结果口径后，C2 判据没有正向选择能力**（四组无差异、十分位 U 型、防守性反向）；
2. 唯一稳健的方向是 **弱反向**：「满足条件」组在两种锚点下都略差（−0.36~−0.50 vs −0.18~−0.28），幅度小；
3. ⇒ **C2 不得用于选方向，也不应影响仓位/选股**；作为他原话的**提示**可以保留（`WOLF_THEME_RESILIENCE` 仍在其
   纪律上下文里输出），但**必须标注"未经回测支持"**；
4. 这条也是本批"**先跑 L1 再进 L2/L3**"价值的第二次体现：一个看起来有防守性证据的判据，
   换成严格口径后证据消失 —— 若直接进 L2/L3 会白跑数天。
---

## 17. L2-A5 验收级样本（重建 254）—— 结论与之前相反，且发现口径偏差

**为什么要重建**（§14.3 的瓶颈）：`t_triggers` 只回溯到 2026-08-25（13 个交易日），
A5 适用腿型的 T+5 根本不可测。本轮按**生产权威定义**在 m5 上重建：

- 触发定义取 `jobs/rotation_switch_arm.py:341 BUY_254_EXPR`：
  `dip_prev_low ∧ 0 < vol_ratio ≤ 0.9 ∧ average>0 ∧ current>0`；
  `dip_prev_low` = 当日 5min 最低 ≤ 前日 5min 最低 × 1.005（口径取自 `t_monitor._stock_dip_prev_low`）
- 数据：brze `stk_mins` 5min **区间查询**（实测一次可覆盖 2026-06-01→09-11 共 3552 根）
- 池 351 只（主题成分 333 + t_triggers 出现过的标的）→ **触发 3,746 次 / 70 个交易日**

### 17.1 v1 口径（量比外推）结果：**窗内反而更差**

| 分组 | n | T+5 超额中位 | 胜率 |
|---|---|---|---|
| ★ 窗内（A5 放行） | 536 | **−0.77%** | **43.7%** |
| ★ 窗外（A5 拦掉） | 3,210 | −0.53% | 45.8% |

时段细分：窗口1 09:45–10:00 **+0.75%/56.2%**（n=32，太小）；**窗口2 14:00–14:30 −0.85%/42.9%
（比被拦掉的平稳时段 −0.51%/46.1% 还差）**；尾盘 14:30–15:00 −0.96%/43.4%（最差）；开盘前段 −0.53%/46.9%。
⇒ 与 §14 基于 13 天真实触发（n=310）得出的"窗内更好"**相反**。

### 17.2 但 v1 重建口径有**时段偏差**，结论不可用

我用"成交量 × 240/已开盘分钟 ÷ 近 5 日**日均**量"近似生产的"换手率 × 240/已开盘分钟 ÷ 同刻基准"。
A 股成交量前置 → 该比值**上午被系统性高估** → 触发密度严重不均：
**09:45–10:00 仅 2.1 条/分钟，而 14:00–14:30 有 16.8 条/分钟** → 时段与样本构成混杂。
⚠️ 因此 17.1 的差异**不能归因于 A5**，也不能作为验收。

### 17.3 v2 口径（无时段偏差）已在跑

改用**同刻累计量比**：当日累计量(t) ÷ 近 5 日**同一时刻**累计量均值 ≤ 0.9 —— 这既是无偏口径，
也正好是**他 2025-04-15 条件4「与上一日的量能**实时**对比」**的原话口径。
脚本 `bt254_v2.py`（产物 `bt254v2_triggers.csv`），结果待本轮之后补记。

### 17.4 阶段结论

1. **A5 的证据目前是"中性偏负"**（v1 口径下窗内更差，但口径有偏）；
2. **在 v2 无偏口径跑完之前，不要据此改动生产开关**（保持 `WOLF_TRADE_WINDOW=1` 现状）；
3. §14 的"方向支持 A5"**已被推翻为"未确认"**（13 天样本是选择偏差 + 臂 n<100）；
4. 这条再次印证设计 §7 的纪律：**样本小 + 口径不严时得到的"支持"结论，换成验收级口径后经常翻转**。
---

## 18. L2-A5 v2（同刻累计量比口径）结果 —— 与 v1 方向一致：**A5 没有正向效果**

**v2 口径**：当日累计量(t) ÷ 近 5 日**同一时刻**累计量均值 ≤ 0.9（无时段系统性偏差，
也正是他 2025-04-15 条件4「与上一日的量能**实时**对比」的口径）。样本 **9,784 次触发 / 70 个交易日**。

| 分组 | n | T+5 超额中位 | T+5 超额均值 | 胜率 | T+1 超额中位 |
|---|---|---|---|---|---|
| ★ 窗内（A5 放行） | 1,151 | **−0.61%** | +0.09% | **45.8%** | −0.16% |
| ★ 窗外（A5 拦掉） | 8,633 | −0.24% | +0.25% | 48.2% | −0.19% |

时段细分（**注意：与 v1 互相矛盾 → 时段层结论不可用**）：

| 时段 | v2 n | v2 中位/胜率 | v1 中位/胜率 |
|---|---|---|---|
| 开盘前段 09:30–09:45 | 6,638 | −0.20% / 48.4% | −0.53% / 46.9% |
| 窗口1 09:45–10:00 | 1,033 | −0.57% / 46.0% | +0.75% / 56.2% |
| 平稳 10:00–14:00 | 1,842 | −0.28% / 47.6% | −0.51% / 46.1% |
| 窗口2 14:00–14:30 | 118 | −0.66% / 44.1% | −0.85% / 42.9% |
| 尾盘 14:30–15:00 | 148 | −0.47% / 46.6% | −0.96% / 43.4% |

### 18.1 结论

1. **两种独立近似口径都显示：A5 的时间窗没有让买入的腿更好，窗内反而略差**
   （v1 中位差 −0.24pp／胜率 −2.1pp；v2 中位差 −0.37pp／胜率 −2.4pp）。样本量 3,746 与 9,784，
   两侧臂均 > 500 → 不再是"样本太小"能解释的。
2. **时段层面的结论不可用**：触发集构成对近似口径极度敏感（v1 下午偏重、v2 开盘偏重占 68%），
   两种口径下的"最好时段"互相矛盾。
3. **A5 的生产口径无法被严格回测**：生产用「换手率 × 240/已开盘分钟 ÷ 同刻基准换手」，
   而我们**没有分钟级累计换手历史**（tushare 只有日频 `daily_basic.turnover_rate`）→ 只能近似。
4. ⇒ **§14「方向支持 A5」正式推翻**：那是 13 天、310 条真实触发的选择偏差 + 臂 n<100。
   现在的证据是「**未验证、且偏负**」。

### 18.2 据此的建议（更新 §15）

| 机制 | 原建议 | **改后建议** |
|---|---|---|
| A5 日内做T时间窗（`WOLF_TRADE_WINDOW=1`，把日内T腿压到每天 45 分钟） | 保留 | **建议关闭**（`WOLF_TRADE_WINDOW=0`）：两种近似口径下都没有正贡献，且它是**硬收紧**（砍掉 85% 的时段）。用户若要保留他原话的"只做两个时段"，应知悉**该规则在本批样本上未被支持** |
| A9 谨慎4条（同属"拦截"类，依赖同一批腿） | 已落（硬门） | **同样待验**：其分钟级黄白线历史缺失（设计 §8 已列不可回测）→ 保持默认开但标注未验证 |
---

## 19. 开关决策（2026-09-11，用户拍板，已落地生产）

| 机制 | 开关 | 决策 | 落地 |
|---|---|---|---|
| A5 日内做T时间窗 | `WOLF_TRADE_WINDOW` | **关闭（=0）** | 生产 `.env` 已设 0；`allowed()` 放行全部时段；`directive()` 改为明确输出「已关闭」而**不再**显示「时段…允许」（避免被读成仍在生效） |
| C2 方向层跌得少弹得早 | `WOLF_THEME_RESILIENCE` | **完全关闭（=0）** | 生产 `.env` 已设 0；`directive()` 返回空串（**修了一处假开关**，见下） |

**决策依据**：A5 在两种独立近似口径下窗内都无正贡献（§17/§18，n=3,746 与 9,784，两侧臂均 >500）；
C2 修掉锚点偏置后横截面四组无差异、十分位 U 型、防守性反向（§16）。

**顺带修掉一处「假开关」**：`wolf_theme_resilience.directive()` 原来**不检查 `enabled()`** →
把开关置 0 只能让盘后 job 停跑，**状态文件里的旧内容仍会照常注入上下文**。
现在关闭时直接返回空。同类问题在 A5 也修了（关闭后仍输出「允许」会误导）。两处均补了定向测试。

**未关闭但同上待验的项**：A9 谨慎4条（同属「拦截」类、依赖分钟级黄白线，**不可回测**）保持默认开，
但在代码注释与本节标注为「未验证」。
---

## 20. L3 组合层（事件式口径）运行记录（2026-09-11 夜）

**口径选择**：完整组合净值需要批次 1 Stage 0（gate 逐日回放 + 账户模拟 + 分批/涨跌停建模），
而本批要回答的是「这些**拦截/卖出**机制叠起来对**单笔结果**有没有改善」→ 用**事件式**口径直接在
**同一批 254 触发**（§17/§18 重建的 9,784 笔，本次可比 9,573 笔）上做叠加，同池同日可比、
不受组合层资金/换手混淆。

**实现**：`data/_bt_batch2/l3_combos.py`（生产容器内跑）；为触发标的补了个股/ETF 日线 OHLC
（`bt254_ohlc.pkl.gz`，351 只 × 250 根）。卖出规则的 BOLL 轨**一律用 ≤前一日收盘算**（PIT）；
中轨清仓带他 2025-05-13 的「**顶部阶段**」前提（上证近 120 日区间上 20% 分位）+ 放量 + 浮盈。

### 20.1 各变体（同一样本集，可直接比）

| 变体 | n | 中位 | 均值 | 胜率 | 5% 分位 | 最差单笔 |
|---|---|---|---|---|---|---|
| 基线（持有到 T+5） | 9,573 | −0.06% | +0.17% | 49.1% | −12.83% | −37.88% |
| **+A10 上轨减半** | 9,573 | **+0.21%** | +0.19% | **50.9%** | −12.73% | −37.88% |
| **+A10 中轨全止盈**（仅触发 167 笔） | 9,573 | **+0.13%** | **+0.27%** | **50.4%** | −12.80% | −37.88% |
| +A9 日级代理（白线在上不入场） | 4,776 | −0.29% | +0.09% | 47.9% | −12.38% | −37.88% |

边际（vs 基线，同口径）：**+A10 上轨 Δ中位 +0.26pp / Δ胜率 +1.8pp**；
**+A10 中轨 Δ中位 +0.18pp / Δ胜率 +1.3pp / Δ均值 +0.091pp**。

### 20.2 ⚠️ 两个必须写清楚的口径陷阱

1. **A9 是"过滤器"，会改变样本集** → 它的变体 n=4,776 与基线 n=9,573 **不可直接比**。
   正确做法有两条，都做了：
   - **全样本口径**（被拦按 0% 收益计入）：+A9 Δ**均值 −0.130pp**、Δ胜率 −25.2pp（记账效应，
     近半机会被记成 0）→ **组合层面为负**；组合（A9+上轨+中轨）Δ均值 **−0.032pp**，即
     **A9 把两个 A10 的正贡献拖回负值**。
   - **同集口径**（A9 放行子集内比"有/无上轨中轨"）：无 → 中位 −0.29%/47.9%；
     有 → **中位 +0.40%/52.6%** → 两个 A10 机制**在子集内是 +0.69pp / +4.7pp 胜率的正贡献**。
2. **"最差单笔 −37.88%" 在所有变体里完全不变** → **A10 的卖出规则没有改善尾部风险**
   （最差那笔要么没触发上轨、要么触发后继续跌）。**不要指望 A10 降低最大单笔亏损。**

### 20.3 A9 的过滤方向**是反的**（本轮最意外的发现）

| 子集（基线口径持有到 T+5） | n | 中位 | 均值 | 胜率 |
|---|---|---|---|---|
| A9 **放行**（日级代理判"黄线在上"） | 4,776 | **−0.29%** | +0.09% | 47.9% |
| A9 **拦掉**（日级代理判"白线在上"） | 4,797 | **+0.11%** | +0.26% | **50.3%** |

⇒ 在我们这批 **254 低吸腿**上，「白线在上 → 不加仓」恰好**把更好的那半拦掉了**。
这与狼大 2025-04-15 条件3「预判白线高于黄线…**减少做T**（成功率低）」**方向不一致** ——
可能的解释是：白线在上 = 权重强/小盘弱 = 避险风格，而低吸腿买的正是"跌下来的票"，风格上更匹配；
也可能是我们的**日级代理**（只用 spread 符号）与他的**分钟级**判据（"迅速下穿/交织 + 量能"）不是一回事。

⚠️ **口径限制（务必带结论一起看）**：生产 A9 用**分钟级**黄白线判"迅速下穿/交织"，
而我们**没有指数分钟级历史**（设计 §8 已列为不可回测）→ 本测只能用**日级代理**，
属**弱验证**，不能等同于否定生产 A9。但它足以说明：**A9 目前没有任何正面证据，且日级近似的方向是反的。**

### 20.4 L3 阶段结论

1. **A10 上轨减半：正贡献**（+0.26pp 中位 / +1.8pp 胜率）→ 与 §13 的 L1 结论一致 → **保留**。
2. **A10 中轨全止盈：正贡献**（+0.091pp 均值 / +1.3pp 胜率，仅 167 次触发）→ **保留**（带顶部前提）。
3. **两个 A10 机制叠加后仍为正**（同集 +0.69pp 中位 / +4.7pp 胜率），**但改善不了尾部**。
4. **A9（日级代理）：负贡献** → **建议关闭 `WOLF_GAP_CAUTION`**；但按用户 2026-09-11 的约定
   **本轮不改动生产开关**，留待用户决定。
5. **C1 利润垫在事件式口径下不可测**（它只影响仓位规模，不影响单笔收益率）→ 未纳入，需在组合净值口径下测。

---

## 21. Stage 0（gate 逐日回放）可行性与保真度（2026-09-11 夜）

**结论：链路可跑，但不能声称"复现生产"。**

### 21.1 已跑通

沙箱 `data/_bt_batch2/sandbox/`（**不碰生产 DATA**），按生产链路顺序跑三件套：

| 步骤 | 命令 | 产物 |
|---|---|---|
| ① 结构 GATE | `trend_confirm.py --as-of <d> --hist <sandbox>/concept_long.json --json ...` | `trend_confirm_<d>_long.json` |
| ② 热度 | `heat_v2.py --date <d>` | `heat_v2_<d>.json` |
| ③ 主线闸 | `mainline_gate.py --date <d>` | `mainline_gate_<d>.json` |

**结果：2026-06-01 → 2026-09-11 共 74 个交易日，74/74 全部成功、0 失败，平均 15.3s/天（合计约 19 分钟）。**
产出的 gate 历史连贯且合理（三段主题轮换）：
`半导体/芯片 06-24→07-07` → `农业 08-18→08-31` → `传媒/游戏+农业+消费/内需 09-01→09-11`；
74 天中有 **11 天** 至少有一个主题 PASS。

**两个"以为缺输入"的问题其实都不是问题**：
- `trend_confirm.load_by_name()` **原生支持 `concept_long.json` 格式**（`{series:{name:{dates,close}}}`）→
  无需另造 `concept_hist.json`；
- `concept_hist.json` **生产本来就有**（7.9MB，Sep 8），`mainline_gate` 直接用它 ——
  上一轮"缺文件"是我 `ls` 输出被截断造成的**误判**。

### 21.2 ⚠️ 保真度：主题级约 73%

以**唯一能对比的 20260909**（生产当日也有产物）为例：

| | 回放 | 生产 |
|---|---|---|
| gate=PASS 主题 | 传媒/游戏、农业、消费/内需 | 农业、医药、消费/内需、稳增长/基建、资源/周期 |
| 15 个主题的 gate 一致性 | **一致 10 / 不一致 4（73%）** | — |

**根因（已定位）**：回放缺 **`main_line_state_<date>.json`（研报 catalyst）历史** →
`mainline_gate` 走 `state={'catalyst': {}}` 兜底 → `theme_signals` 的 fund/rel/conc 与 `gate_ratio` 偏移
→ 部分主题 PASS/FAIL 翻转。

**已知且已确认无影响的前视**：`wave_state.json` 只有当日快照（本仓**无写入方**，无法逐日 as-of 重建），
但 `load_wave_env()` 的 docstring 明确「**不参与主线资格判定**（Wolf: 调整浪内也做主线）」→
只污染 `wave_env` 字段，**不影响 confirmed 主题**（L3 要用的是后者）。

### 21.3 完整组合净值 L3 的剩余前置（明确清单）

| 前置 | 现状 | 影响 |
|---|---|---|
| ① `main_line_state_<date>.json`（研报 catalyst）历史 | **缺**（只有近期） | gate 保真度 73% → 直接决定组合结论可信度 |
| ② 逐日 `wave_state`（大盘浪） | 无写入方、无法 as-of 重建 | 仅风险提示字段；若要用 `index_level_stop` 类机制须先解决 |
| ③ **腿级 m5** | 已重建 **254**（351 只 × 70 天，`bt254v2_triggers.csv`）；**253 / low_buy / custom 等腿型未重建** | 组合净值需要全部腿型 |
| ④ 账户/成交模拟（资金闸分批、涨跌停不可成交、滑点、100 股底仓） | 批次 1 §8 已列为受限项 | 组合绝对收益不可信，只能看**相对比较** |

⇒ **建议**：补齐 ① 之前不要拿"完整组合净值"当结论；机制层面的问题用**事件式口径**（§20/§22）回答更可靠。

---

## 22. L3-b：用 Stage 0 的 gate 历史检验「布腿链」（gate × 254 分层）—— **本轮最强的正向结果**

**问的问题**：只在**前一日 gate 确认的主题**上做 254 低吸，是否优于全池低吸？
（生产布腿链的核心假设：gate 收盘产出 → 次日 09:20 布腿 → 254/253 触发）

**PIT 对齐**：D 日的触发只用 **D−1 日** 的 gate（`mainline_gate_<D-1>.json`）—— 布腿发生在 D 日 09:20，
用的是前一晚的产物；绝不使用 D 日或之后的 gate。

### 22.1 分层结果（T+5，n=9,454 触发×主题）

| 分组 | n | 超额中位 | 超额均值 | 胜率 | 5% 分位 | **最差单笔** |
|---|---|---|---|---|---|---|
| ★ **已确认主题**（布腿链会做） | 440 | **+1.74%** | **+3.04%** | **67.7%** | −4.11% | **−11.45%** |
| ★ 未确认主题（布腿链不做） | 9,014 | −0.41% | +0.12% | 46.9% | −11.11% | **−31.27%** |

绝对收益：已确认 中位 **+3.00%**／胜率 **73.9%**／最差 −11.95%；未确认 −0.25%／48.1%／−34.96%。

**逐日面板**（同日横截面对照，12 天可比）：中位差 **+0.67pp**、均值差 **+1.46pp**、
**确认组更好的天数占比 75.0%**。

### 22.2 排除「主题动量」混淆：主题内对照

窗口内只有**农业**同时拥有确认日与未确认日样本（其他主题的确认日样本为 0）：

| 分组 | n | 超额中位 | 胜率 |
|---|---|---|---|
| 农业 · **gate 确认日** | 440 | **+1.74%** | **67.7%** |
| 农业 · 未确认日 | 2,102 | +0.34% | 52.3% |
| **主题内差** | — | **+1.40pp** | +15.4pp |

⇒ 差异**在同一主题内部依然存在**（保留约 65%；合并口径是 +2.15pp）→ **不完全是"这个主题本来就好"**，
gate 具备**条件性择时**能力。⚠️ 但只有单主题（农业）可做主题内对照 → **泛化性受限**。

### 22.3 对整批机制的意义

| 机制 | 本轮口径下的贡献 |
|---|---|
| **gate 确认主题 × 254 低吸（布腿链骨架）** | **胜率 +20.8pp、中位 +2.15pp、最差单笔改善 20pp** ← 唯一的大幅正贡献 |
| A10 上轨减半 | +0.26pp 中位 / +1.8pp 胜率（§20） |
| A10 中轨全止盈（带顶部前提） | +0.18pp 中位 / +1.3pp 胜率（§20） |
| A5 时间窗 | 无正贡献（§17/§18）→ 已关闭 |
| A9 谨慎 4 条（日级代理） | **负贡献**，且过滤方向是反的（§20.3） |
| C2 跌得少弹得早 | 无选择能力（§16）→ 已关闭 |

⇒ **与我们一直纠结的"日内择时"相比，"选对方向"（gate 确认主题）的贡献高一个量级** ——
这与狼大 2026-01-27「在**几百个板块**里面找到唯一对的那个」所体现的流程主次完全一致。

---

## 23. 按层级重跑（2026-09-12）—— **两处重要更正**

**做法**（用户要求：先摸清生产数据流，再仿真重跑）：
1. 先产出 `docs/production-dataflow.md`：按 `config/tasks.yaml` 逐环追出生产顺序，
   确认主线链 = `mainline_gate_daily.py` 的 **6 步**（含 `build_etf_flow`、`build_inst_flow`）。
2. 沙箱内**逐日重跑 74 个交易日**（2026-06-01→09-11），分片并行（3 gate worker + 3 wave worker，
   各自独立沙箱；宿主仅 2 核/1.7GB，故用独立容器避开 backend 的 512MB 限制）。
3. 波浪层用 `wave_agent.py --date=<d>` **逐日重放**（71/74 天成功；3 天连接重置）。
4. 三层条件化重跑 254 触发样本（9,454 笔可分层）。

### 23.1 更正一：§22「布腿链是最强正向结果」**作废**

**根因**：v1 回放调 `mainline_gate.py --date d` 时**漏传 `--fusion-json heat_v2_<d>.json`**
（生产 `mainline_gate_daily.py` 第 4 步是**必传**的），于是脚本走了 `fm_load('main_line_state_<d>.json')`
失败 → `state={'catalyst':{}}` + `theme_signals(concept_hist, …)` 的**兜底路径**（而 concept_hist 又冻结在 Sep 8）
→ gate 变得极稀疏（74 天里仅 11 天有 PASS）→ 只有 440 笔触发落在"确认"里 → 形成**假的高区分度**。

**用生产同序回放（v2）后**：

| 第 1 层 主线（前一日 gate 是否确认其主题） | n | 中位 | 胜率 | 最差 |
|---|---|---|---|---|
| 主线确认 | 4,922 | −0.37% | **47.5%** | −28.48% |
| 主线未确认 | 4,532 | −0.20% | **48.3%** | −31.27% |

⇒ **几乎无差异（未确认甚至略好）** ⇒ §22 的「已确认 67.7% vs 未确认 46.9%」是 v1 口径产物，**撤回**。
⚠️ 但 v2 回放 gate 在 **74/74 天**都有 PASS（平均 PASS 数高），而生产 09-09 只有 5/15 PASS
→ "主线确认"在生产口径下**是否仍有区分度，要等保真度修好才能定论**（见 23.4）。

### 23.2 更正二：**波浪层是真正有区分度的那一层**（本轮主要正面发现）

| 第 2 层 波浪 `operation`（前一日 wave_state，71 天重放） | n | 中位 | 胜率 | 最差 |
|---|---|---|---|---|
| **build**（建仓追） | 73 | **+2.39%** | **63.0%** | −20.40% |
| side（观望调仓） | 3,950 | +0.04% | 50.2% | −30.76% |
| t_only（只做T） | 4,560 | −0.56% | 45.5% | −26.65% |
| **defense**（防御不建仓） | 526 | −0.74% | **42.4%** | −31.27% |

⇒ **单调且方向与狼大语义一致**：建仓档最好、防御档最差。
⚠️ `build` 仅 73 笔（**n<100 → 无结论**）；整窗 `level` 恒为 **d4（大4浪）**（70/71 天），
所以波浪层提供区分度的是 **operation**，不是 level。且**重放的波浪是 LLM 按其规则重跑生成、非当时存档**。

### 23.3 第 3 层（细节机制）—— 在正确层级内的结论

| 机制 | 【该做的】格子（主线确认∧波浪允许 n=2,388） | 【其余】格子（n=6,900） |
|---|---|---|
| 基线（持有 T+5） | 中位 −0.65% / 胜率 45.2% | +0.13% / 50.5% |
| **+A10 上轨减半** | **−0.30% / 47.4%** | **+0.37% / 52.1%** |
| +A10 中轨全止盈 | −0.25% / 47.9% | +0.27% / 51.3% |
| A5 时间窗：窗内 vs 窗外 | **44.9% vs 47.0%**（窗内更差） | **45.9% vs 48.6%**（窗内更差） |

- **A10 上轨减半：两个格子里都为正**（该做的 +0.35pp 中位/+2.2pp 胜率）⇒ **稳健**，与 §13/§20 一致。
- **A5 时间窗：两个格子里窗内都更差（−2pp 左右）** ⇒ 用户"关闭 `WOLF_TRADE_WINDOW`"的决定
  **在条件化口径下同样成立** ✓（不再只是无条件口径的结论）。
- **A9（日级代理）**：见 23.5 的更正文。
- 最差单笔在**所有变体都不变**（−37.88%）⇒ 再次确认 **A10 改善不了尾部**。

### 23.4 保真度（v2 仍约 73%，且根因已排除一项）

| 日期 | 回放 PASS | 生产 PASS | gate 一致/不一致 |
|---|---|---|---|
| 20260909 | 传媒/游戏、农业、消费/内需 | 农业、医药、消费/内需、稳增长/基建、资源/周期 | **10 / 4** |
| 20260910 | 同上 | 同上 | 10 / 4 |
| 20260911 | 同上 | 同上 | 10 / 4 |

⇒ **补上 `build_etf_flow` 后 09-09 的结果与 v1 完全相同** → 说明保真度损失**不是** ETF 因子造成的，
根因仍是**缺 `main_line_state_<date>.json`（研报 catalyst）历史**（回放走空 catalyst 兜底）。
另外 v2 回放的 PASS 数（多日 8–10 个主题）明显高于生产 09-09 的 5 个 → 提示 `heat_v2` 的
fusion proxy 与生产当时的 heat 仍有系统差异（生产 09-09 的 `heat_v2_20260909.json` 我们手上没有，无法逐项对齐）。

### 23.5 更正三：A9 在条件化口径下仍为负（但口径仍是日级代理）

| 分组 | n | 中位 | 均值 | 胜率 | 最差 |
|---|---|---|---|---|---|
| 【该做的】∩ 黄线在上（A9 **放行**） | 1,017 | **−1.03%** | −0.42% | **42.0%** | −24.72% |
| 【该做的】∩ 白线在上（A9 **拦掉**） | 1,470 | **+0.03%** | +1.13% | **50.1%** | −26.65% |
| 【其余】∩ 黄线在上 | 3,618 | −0.05% | +0.39% | 49.8% | −30.76% |
| 【其余】∩ 白线在上 | 3,349 | −0.47% | −0.06% | 46.7% | −31.27% |

⇒ **在正确的层级里，A9 日级代理的过滤方向依然是反的**（该做的格子里：拦掉的 50.1% 反而比放行的 42.0% 更好，
差 **−8.1pp 胜率**）；而在"其余"格子里方向虽对（白线在上 46.7% < 黄线在上 49.8%）但幅度很小。
⇒ 结论：**"白线在上就不加仓"这个日级近似不可用**。
⚠️ 口径限制不变：生产 A9 用**分钟级**"迅速下穿/交织 + 量能"，本测只是**日级代理** → **不能等同于否定生产 A9**；
要真判它必须先用 ETF 分钟代理重建分钟级黄白线（见 §21 与 `production-dataflow.md` §6）。

### 23.6 结论与后续

1. **层级确实存在，但要在正确的对应关系上**：本批数据里**波浪(operation) 有区分度、主线(gate) 在 v2 口径下没有**；
   §22 那个"主线最强"是口径错误（漏 `--fusion-json`）造成的假象 —— **已撤回**。
2. **A5 关闭**得到条件化支持；**A10 上轨/中轨**在两个格子里都为正 ⇒ 保留。
3. **要判定"主线层"到底有没有用**，必须先修保真度：补 `main_line_state_<date>.json`（研报 catalyst）历史，
   并对齐生产当时的 `heat_v2_<d>.json`（至少把 09-09～09-11 三天做到逐项一致）。
4. **波浪层值得进一步做**：把 `wave_agent --date` 的逐日重放扩到更长区间；但要注意它是 LLM 重放、非存档，
   且 `main_line`/`anchors` 两段输入在沙箱内为空。
